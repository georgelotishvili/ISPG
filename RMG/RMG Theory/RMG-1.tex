% RMG-1: Relational Medium Gravity
% Internal core draft, version 6.2

\documentclass[11pt]{article}
\usepackage{amsmath,amssymb,amsthm}
\usepackage[margin=1in]{geometry}

\newtheorem{postulate}{Primitive Postulate}
\newtheorem{assumption}{Working Assumption}
\newtheorem{result}{Result}
\newtheorem{target}{Low-Energy Target}
\newtheorem{openthm}{Open Theorem}
\newtheorem{gate}{Gate}

\begin{document}

\begin{center}
{\Large RMG-1: Relational Medium Gravity}\\
\vspace{0.4em}
{\large Gravity as the low-energy relational geometry of a stable base-medium}\\
\vspace{0.4em}
Internal core draft, version 6.2\\
2026-05-11
\end{center}

\section*{Purpose}

RMG formulates gravity as the low-energy relational geometry of a stable
base-medium.

Core sentence:
\[
\boxed{
\text{gravity is the low-energy relational geometry of a stable base-medium.}
}
\]

The starting object is a base-medium $\mathcal M$ with a stable low-energy
phase.  The tested GR/ADM structure is required to emerge as the low-energy
fixed point of $\mathcal M$.

The protected low-energy phase admits only operational relational observables,
not a separate local medium-frame field.

A central core gate is that all physical low-energy principal operators share
one kinetic metric kernel.

A known no-go result is addressed at the outset.  The Weinberg--Witten
theorem forbids a massless spin-2 particle from emerging as a composite
in a relativistic quantum field theory carrying a Lorentz-covariant
conserved stress-energy tensor on a fixed background.  RMG is not such a
theory: it has no fixed background spacetime and no Lorentz-covariant
stress-energy tensor defined on one.  Spacetime, rods, clocks and the
operational metric are themselves emergent relational records of
$\mathcal M$, in the sense of Operational Relational Covariance.  The
background-independence of RMG is therefore the intended route around
the Weinberg--Witten obstruction.  This escape is necessary but not
sufficient: the positive construction of exactly two
transverse-traceless tensor radiation modes, with no extra physical
scalar or vector gravitational mode, remains an open requirement
carried by the record-sector factorization and spin-2 emergence gates.

\section*{Status labels}

Every statement in RMG-1 carries one of the following labels:
\textit{Primitive Postulate} (minimal starting property of the base-medium);
\textit{Primitive Protection Principle} (low-energy relational rule excluding
operators that violate the protection requirements);
\textit{Working Assumption} (temporary bridge requiring deeper derivation);
\textit{Result} (statement derived from listed assumptions);
\textit{Low-Energy Target} (structure required by observation and adopted as
the low-energy fixed point);
\textit{Open Theorem} (claim still to be proven from $\mathcal M$);
\textit{Failure Gate} (condition whose failure invalidates the local core).

\section{Core Terms}

RMG (Relational Medium Gravity) is a gravitational framework in which a single
base-medium $\mathcal M$ gives rise to matter modes, rods, clocks, signals and
operational geometry.  The deeper task is to explain why the universal
low-energy geometry is the ADM/GR fixed point.

$\mathcal M$ is the base-medium; it is not assumed to be an ordinary scalar
field.  $\mathcal W$ is the relational medium-history.  $X_F$ is a foliation
or embedding description of the same history $\mathcal W$.

$s(x)$ is the local operational factor shared by clocks, rods and signals in
the weak-sector bridge description.  $u$ is the compression/lapse potential,
with $s=e^{-u}$; it enters the action as a constraint multiplier, not as an
independent propagating field.

$g_{\mu\nu}^{\rm eff}$ is the effective metric seen by low-energy matter
modes.  $N,N^i,h_{ij}$ are the ADM lapse, shift and spatial metric variables
in the low-energy target, and $h_{ij}^{TT}$ is the transverse-traceless
tensor/shear radiation sector.  $u^\mu_{\mathcal M}$ denotes a putative
independent local medium-frame field; it must not appear as an independent
physical field in the tested RMG core.

$Q_{AB}^{\mu\nu}$ is the highest-derivative low-energy kinetic response matrix
for perturbations of matter and geometry modes.  $G^{\mu\nu}$ is the common
principal metric kernel, identified with $g_{\rm eff}^{\mu\nu}$ if the
factorization gate succeeds.  $K_{AB}$ is an internal/species kinetic matrix:
it carries allowed differences between particle families, tail shapes,
polarizations, phases and internal geometries.  $B_{AB}^{\mu\nu}$ is the
non-factorizing failure matrix; it must vanish on physical low-energy
principal modes, up to gauge, internal or suppressed correction terms.

$\Delta_{\mathcal M}$ is the residual medium contribution to the low-energy
Hamiltonian/constraint algebra after the ADM/EH target terms are separated;
the tested core requires $\Delta_{\mathcal M}=0$, or suppression outside all
tested regimes.

$\mathcal R$ is the record sector: low-energy modes that can participate in
operational measurement as rods, clocks, detector components, memory or
record components, exchanged signals or stable external matter excitations.
$\mathcal I$ is the internal sector: confined internal modes, gauge
directions, constraint variables, off-shell auxiliary modes, short-range
internal responses and high-energy modes outside the tested local regime.
Internal modes may contribute to mass, binding energy, charge, spin, colour,
tail shape, effective couplings and total energy, but must not define a
separate external spacetime cone in the tested local core.

\section{Primitive Postulates}

\begin{postulate}[One base-medium]
All low-energy matter modes, rods, clocks, signals and geometric excitations are
collective modes of one base-medium $\mathcal M$.
\end{postulate}

\medskip\noindent\textbf{Remark (mathematical status of \(\mathcal M\)).}
This postulate fixes only the ontological commitment of RMG-1: one substrate
whose low-energy observable structures are collective or coarse-grained modes.
It does not assume that \(\mathcal M\) is an ordinary scalar field, nor does it
by itself specify a state space, action, symplectic structure, internal
redundancy group or record observables.  Those data belong to the later
base-medium input package and candidate order-parameter construction.  The
postulate therefore asserts existence and exhaustiveness; the microscopic
content remains an open construction target.

\begin{postulate}[Stable low-energy phase]
The base-medium has a stable low-energy phase in which matter modes, rods,
clocks, signals and operational geometry are well-defined.
\end{postulate}

\begin{postulate}[Minimal operational node scale]
The base-medium as one ontological object has no ordinary microscopic radius.
A length becomes physical only when the base-medium is distinguishable as
recordable nodes.  The first distinguishable node/record scale is denoted
\[
\ell_*.
\]
Below \(\ell_*\), two nodes are not yet separate operational objects: there is
no independent distance, delay, clock comparison or mass comparison.  Above
\(\ell_*\), medium dilution also rescales rods, clocks and the visible node
scale.  Thus \(\ell_*\) is an operational closure scale, not the radius of a
mechanical particle.

In the present RMG-1 core, \(\ell_*\) is identified with the observed
Planck-order minimal record scale.  This is not a second medium and not a
species-dependent parameter; it is the universal first readable separation of
the one base-medium.
\end{postulate}

\begin{postulate}[Operational Relational Covariance]
Local low-energy observables are invariant under re-embedding of the same
relational medium-history:
\[
\mathcal W
\sim
X_F(\mathbb R\times\Sigma)
\sim
X_{F'}(\mathbb R\times\Sigma).
\]
Thus coordinates, slicings and internal clock choices are descriptions, not
extra physical structures.  In particular, no independent local preferred-frame
field $u^\mu_{\mathcal M}$ appears in the tested core.
\end{postulate}

\medskip\noindent\textbf{Remark (operational content of ORC).}
The re-embedding statement above has three explicit operational consequences
that load the closure of the gravitational core:
\begin{enumerate}
\item Substrate-internal variables that do not enter operational record
  comparisons are gauge / bookkeeping, not physical observables.  In
  particular, no hidden substrate clock, no preferred rest frame, and no
  additional substrate-internal label distinguish records.
\item All stable record-forming matter reads the same local operational
  geometry.  No substrate-internal mode produces a distinguishable second
  operational time, second operational length, or second principal cone.
\item Background uniform substrate states (e.g.\ the uniform phase advance
  $\langle\theta\rangle=\nu_0 t$ on the stable phase $\mathcal M_0$) are
  operationally hidden; only the fluctuations on top of such backgrounds
  are physical.  Only fluctuations relative to the background carry operational
  content; this is the relational form of Galilean and Lorentz invariance.
\end{enumerate}
ORC is the principal axiom that closes the three local-gravity roots
(Root~A: one carrier; Root~B: ADM/EH closure; Root~C: total-energy source)
into derived results.  ORC itself is taken here as a primitive postulate; its
derivation from a deeper micro principle remains a long-term research target.

\begin{postulate}[Positive physical energy]
Physical excitations of $\mathcal M$ have Hamiltonian bounded below:
\[
H-H_{\rm vac}\ge0.
\]
All physical degrees of freedom carry positive kinetic energy.
The trace/volume direction of the DeWitt supermetric belongs to the
constrained sector and does not propagate as an independent mode.
\end{postulate}

\begin{postulate}[Local effective hierarchy]
The low-energy theory is local and organized by a derivative or scale expansion:
\[
\mathcal L_{\rm eff}
=
\mathcal L_{\rm core}
+\ell^2\mathcal L_4
+\ell^4\mathcal L_6
+\cdots.
\]
Corrections may exist, but they must be suppressed or forbidden in tested local
regimes.
\end{postulate}

\section{Operational Bridge}

This section gives the minimal bridge needed to recover the weak gravitational
sector; the microscopic derivation is treated separately.

The universal factor $s(x)$ is a bridge variable, not a primitive object.  Its
deeper origin is the universal principal symbol of all low-energy matter modes.

\begin{assumption}[Universal operational factor]
In the slowly varying low-energy regime, all stable matter modes share one local
operational factor:
\[
s=s(x).
\]
Equivalently, all matter modes see one effective metric
$g_{\mu\nu}^{\rm eff}$.
\end{assumption}

\begin{assumption}[Reciprocal clock/rod response]
Local clocks and rods respond as:
\[
d\tau=sdt,
\qquad
d\ell=s^{-1}dx.
\]
\end{assumption}

\begin{result}[Square signal law]
If the local physical signal speed is $c=d\ell/d\tau$, then:
\[
c
=
\frac{s^{-1}dx}{sdt}
=
s^{-2}\frac{dx}{dt}.
\]
Therefore:
\[
\boxed{
\frac{c_{\rm coord}}{c}=s^2.
}
\]
\end{result}

\begin{result}[Coherent endpoint-counting route to the square law]
The reciprocal clock/rod bridge is not produced by a longer courier path.  A
longer path at fixed carrier speed gives only a linear delay.  A square response
is obtained when the stable record sector is controlled by one coherent
base-medium amplitude
\[
\boxed{
\rho:=|\Phi_{\mathcal M}|=\sqrt n
}
\]
and the local record coefficients scale as
\[
\boxed{
C_t:C_0:C_x=1:\rho:\rho^2.
}
\]
Here \(C_t\) is the common-rhythm time coefficient, \(C_0\) is the local stable
gap coefficient and \(C_x\) is the spatial link-stiffness coefficient.  Define
\[
\boxed{
s:=\rho^{1/2}=n^{1/4}.
}
\]
Then
\[
C_t:C_0:C_x=1:s^2:s^4.
\]
For a stable quadratic record mode,
\[
\omega_0\propto\sqrt{\frac{C_0}{C_t}}=s,
\qquad
\ell_{\rm coord}\propto\sqrt{\frac{C_x}{C_0}}=s,
\qquad
c_{\rm coord}\propto\sqrt{\frac{C_x}{C_t}}=s^2c.
\]
Thus the coherent endpoint-counting completion derives
\[
\boxed{
d\tau=sdt,\qquad d\ell=s^{-1}dx,\qquad \frac{c_{\rm coord}}{c}=s^2.
}
\]
The exponent origin is:
\[
\boxed{
\text{site/local gap} \sim \rho,
\qquad
\text{link/spatial stiffness} \sim \rho_a\rho_b\to\rho^2.
}
\]
\end{result}

\begin{assumption}[Additive compression and multiplicative response]
Weak compression potentials add:
\[
u_{12}=u_1+u_2.
\]
Process factors multiply:
\[
s_{12}=s_1s_2.
\]
\end{assumption}

\begin{result}[Exponential layering]
The continuous solution with weak-field normalization is:
\[
\boxed{
s=e^{-u}.
}
\]
\end{result}

\begin{result}[ADM lapse identification]
For a normal ADM observer with zero shift displacement,
\[
d\tau=Ndt.
\]
The RMG bridge clock response gives:
\[
d\tau=sdt.
\]
Therefore, inside the operational bridge,
\[
\boxed{
N=s=e^{-u}.
}
\]
The remaining task is to derive the universal factor \(s\) and the
exponential layering from \(\mathcal M\).
\end{result}

\begin{result}[Weak operational metric]
The bridge metric is:
\[
\boxed{
ds^2=-e^{-2u}c^2dt^2+e^{2u}\delta_{ij}dx^idx^j.
}
\]
\end{result}

\section{One-Kernel Factorization Gate}

The universal matter metric must derive from a common highest-derivative
kinetic kernel.  For low-energy perturbations $\delta\Phi^A$, denote by
$Q_{AB}^{\mu\nu}$ the principal kinetic response.  For a covector $k_\mu$,
define the principal symbol:
\[
\boxed{
P_{AB}(k)=Q_{AB}^{\mu\nu}k_\mu k_\nu.
}
\]
Let \(\Pi_{\mathcal R}\) project onto the physical record subspace after
gauge and constraint directions are removed.

The labels \(A,B\) range over every stable record-forming excitation relevant
to the local core: elementary medium excitations, effective matter modes,
photons/signals, composite records such as atoms, rods and clocks, and
detector components.  Modes outside this class may be internal or confined,
but they must not act as external spacetime carriers.

The principal symbol is extracted in the relevant low-energy effective regime,
on the physical on-shell branch after gauge and constrained directions are
removed.  It is not a blind UV limit outside the record theory:
\[
\boxed{
k\ll\Lambda_{\rm UV},
\qquad
\delta\Phi\in\Pi_{\mathcal R},
\qquad
\text{dispersion evaluated on a physical record branch}.
}
\]

\begin{openthm}[Factorization gate]
Derive from the protected base-medium phase:
\[
\boxed{
Q_{AB}^{\mu\nu}
=
G^{\mu\nu}K_{AB}.
}
\]
Then all physical low-energy modes share one principal metric:
\[
\boxed{
G^{\mu\nu}=g_{\rm eff}^{\mu\nu}.
}
\]
\end{openthm}

Common characteristic cones do not suffice: the physical principal symbol must
also be free of unsuppressed species or polarization birefringence, and a
single base medium does not by itself prevent internal or confined modes from
defining separate cones.  The RMG statement is therefore restricted:
\[
\boxed{
\text{one stable record-forming low-energy phase}
\Rightarrow
\text{one operational spacetime cone for records}.
}
\]
Modes outside the record sector may have different internal dispersions, but
they must not define separate external rods, clocks, signals or spacetime
metrics.
The pass condition reads:
\[
\boxed{
\Pi_{\mathcal R}P(k)\Pi_{\mathcal R}
=
\left(G^{\mu\nu}k_\mu k_\nu\right)K_{\mathcal R}.
}
\]
Allowed remainders are gauge directions, constraint variables, internal
algebra, lower-derivative terms or suppressed corrections.  The local core
fails if the physical principal symbol contains:
\[
\boxed{
P_{AB}^{\rm phys}(k)
=
\left(G^{\mu\nu}k_\mu k_\nu\right)K_{AB}
+
B_{AB}(k),
\qquad
B_{AB}(k)\neq0,
}
\]
where \(B_{AB}\) produces an observable species-private cone, polarization
splitting, preferred-frame tensor or clock-dependent redshift.

\medskip\noindent\textbf{Why this is non-trivial in RMG.}
In GR, one metric for all matter is usually imposed by minimal coupling.  Root
A is not allowed to assume that result.  It must derive why all stable
record-forming modes read one external cone from the base-medium spectrum and
record projection.  If this fails, later ADM closure or source universality
cannot repair the local core.

\begin{openthm}[Record-sector no-birefringence principle]
All stable modes that can form local records, rods, clocks and exchanged
signals must belong to one operational record sector.  In that sector:
\[
\boxed{
B_{AB}^{\rm phys}(k)=0
}
\]
for unsuppressed principal spacetime response.  Internal differences may remain
in \(K_{AB}\), masses, charges, spin, color, topology, phase and tail geometry;
they must not appear as separate external spacetime cones.  This is a
protection principle in RMG-1 and still requires derivation from
\(\mathcal M\).
\end{openthm}

\begin{openthm}[Record-phase route to factorization]
Derive the chain:
\[
\boxed{
\text{stable record phase}
\Rightarrow
\text{common operational records}
\Rightarrow
\text{no observable species-private spacetime cone}
\Rightarrow
\text{no physical birefringence}
\Rightarrow
Q_{AB}^{\mu\nu}=G^{\mu\nu}K_{AB}.
}
\]
This chain remains to be derived from \(\mathcal M\).
\end{openthm}

\begin{openthm}[One-tensor factorization lemma]
For any candidate common record tensor \(G^{\mu\nu}\), decompose the
principal kinetic matrix as:
\[
\boxed{
Q_{AB}^{\mu\nu}
=
G^{\mu\nu}K_{AB}
+
B_{AB}^{\mu\nu}.
}
\]
If the stable record phase contains only one observable rank-two spacetime
principal tensor, then the non-factorizing part \(B_{AB}^{\mu\nu}\) has no
unsuppressed physical projection on external record-forming modes:
\[
\boxed{
\Pi_{\mathcal R}B_{AB}^{\mu\nu}\Pi_{\mathcal R}=0
}
\]
up to gauge, confined, gapped, damped, unstable or high-energy suppressed
pieces.  Otherwise \(B_{AB}^{\mu\nu}\) would define a second observable cone,
anisotropy, preferred-frame tensor or species-private clock response.  This is
an open lemma: RMG must still derive the one-observable-tensor premise from the
base medium.
\end{openthm}

\begin{gate}[Observable tensor basis gate]
In the tested local record core, the physical principal symbol must contain no
unsuppressed observable rank-two tensor other than the common record tensor
\(G^{\mu\nu}\).  Equivalently, any decomposition
\[
\boxed{
Q_{AB}^{\mu\nu}
=
G^{\mu\nu}K_{AB}
+
B_{AB}^{\mu\nu}
}
\]
must satisfy
\[
\boxed{
\Pi_{\mathcal R}B_{AB}^{\mu\nu}\Pi_{\mathcal R}=0
}
\]
at leading principal order, except for gauge, constraint, internal/confined,
gapped, damped, unstable, high-energy suppressed or explicitly large-scale
correction sectors.  Forbidden local-core pieces include unsuppressed
\[
\boxed{
U^\mu U^\nu,\quad
v^\mu v^\nu,\quad
\sigma^{\mu\nu},\quad
\omega^\mu{}_\alpha\omega^{\alpha\nu},\quad
\nabla^\mu u\nabla^\nu u,\quad
S_A^{\mu\nu}.
}
\]
If any such tensor survives in physical record propagation, RMG predicts a
preferred frame, anisotropic cone, species-private metric or birefringence.
This gate is open from \(\mathcal M\).
\end{gate}

\begin{openthm}[Master gate: no-birefringent record carrier]
The master local-core gate is:
\[
\boxed{
\text{all low-energy record-forming tails project onto one common carrier}
}
\]
so that the physical record-sector principal symbol has no unsuppressed
species or polarization birefringence:
\[
\boxed{
P_{AB}^{\rm phys}(k)
=
\left(G^{\mu\nu}k_\mu k_\nu\right)K_{AB}.
}
\]
If this gate closes, then one operational cone, one universal clock factor
\(s(x)\), the ADM lapse bridge \(N=s=e^{-u}\), the total-energy source gate
and the compact internal-energy/no-external-cone split become linked
consequences of the same record-sector protection.

Here ``one common carrier'' means one shared base-medium vibration channel, not
one elementary scalar field and not one tail substance per particle.  Different
sources may imprint different local patterns on the same medium; the tested
local core allows only one external record carrier/cone.
\end{openthm}

\begin{openthm}[Stable-tail common-carrier route]
For every stable record-forming oscillon or matter mode \(A\), decompose its
long tail as:
\[
\boxed{
T_A=\Pi_{\rm common}T_A+\Pi_{\rm internal}T_A.
}
\]
The theorem target is:
\[
\boxed{
\operatorname{principal}(\Pi_{\rm common}T_A)
=
G^{\mu\nu}k_\mu k_\nu
\quad
\text{for all record-forming }A,
}
\]
while \(\Pi_{\rm internal}T_A\) may carry source-specific frequency, phase,
spin, charge, topology, multipoles and short-range structure, but must not
define a separate external spacetime cone.  \(T_A\) is a source imprint in the
same base-medium vibration, not a separate long-range field assigned to species
\(A\).  Modes whose tails cannot project onto the common carrier are internal,
confined, gapped, constrained or non-record-forming.
\end{openthm}

\begin{openthm}[Universal operational factor from one cone]
For a clock family \(A\), write:
\[
d\tau_A=s_A(x)dt.
\]
If the one-kernel factorization gate holds, then all record-forming clock
families read the same spacetime kernel \(G^{\mu\nu}\).  Hence:
\[
\boxed{
s_A(x)=C_A\,s(x)\,[1+\epsilon_A(x)].
}
\]
Here \(C_A\) is a constant internal clock calibration, while
\(\epsilon_A(x)\) is the species-dependent environmental response that would
violate the universality requirement.
The tested core requires:
\[
\boxed{
\epsilon_A(x)=0
}
\]
or suppression outside tested regimes.  After constant calibration,
\[
\boxed{
s_A(x)=s_B(x)=s(x).
}
\]
Equivalently,
\[
\boxed{
\nabla_\mu\ln\frac{s_A}{s_B}=0
}
\]
up to suppressed corrections.  If this fails, the failure is the clock-level
version of a nonzero \(B_{AB}^{\mu\nu}\).

The exact target is \(\epsilon_A=0\).  If a candidate leaves a residual
\(\epsilon_A\), it must be bounded against clock-comparison, redshift,
composition-dependence and species-dependent signal-cone tests; it cannot
remain an unconstrained symbolic remainder.
\end{openthm}

Different oscillons may excite the same base-medium with different local
frequencies, phases, polarizations and internal geometries.  The factorization
requirement constrains only the external record carrier of their long-range
tails, which must be the same base-medium vibration channel:
\[
\boxed{
\text{diverse local imprints, one base-medium, one operational principal cone}.
}
\]

The possible failure is:
\[
\boxed{
Q_{AB}^{\mu\nu}
=
G^{\mu\nu}K_{AB}
+
B_{AB}^{\mu\nu},
\qquad
B_{AB}^{\mu\nu}\neq0.
}
\]

In the tested core, $B_{AB}^{\mu\nu}$ must not contain an independent
preferred-frame tensor, species-private metric, or geometry-sector cone
different from the matter cone.

\section{Tail Superposition And Projection Gate}

Different oscillons may produce different local tail imprints in the same
base-medium.  Gravity does not read the full microscopic imprint.  It reads the
common pressure/refraction projection:
\[
\boxed{
\Pi_{\rm grav}(T_A)=q_A\mathcal G.
}
\]
Here $T_A$ is the source imprint associated with particle family $A$,
\(\mathcal G\) is the common base-medium pressure carrier, and \(q_A\) is the
pressure-deficit strength.  \(T_A\) must not be read as a separate long-range
field or separate spacetime channel.

\begin{openthm}[Universal gravitational projection]
For all stable low-energy record-forming matter modes, derive:
\[
\boxed{
q_A\propto E_A/c^2.
}
\]
Here $E_A$ means total physical energy of the source mode or composite system:
\[
\boxed{
E_A
=
E_{\rm rest}
+E_{\rm kinetic}
+E_{\rm binding}
+E_{\rm internal}.
}
\]
Confined/internal modes do not need their own free long gravitational tails;
they contribute through the total energy of the composite external tail.

Then many different tails source one pressure profile:
\[
\boxed{
u(\mathbf x)=\sum_i u_i(\mathbf x),
\qquad
\nabla^2u\sim-\rho_E
}
\]
in the weak-field regime, and the same $u$ determines the effective geometry.
\end{openthm}

Equivalently, the far long-range pressure tail must have an energy monopole:
\[
\boxed{
u_A(r,\Omega)
=
\frac{Q_A^{(0)}}{r}
+O(r^{-2}),
\qquad
Q_A^{(0)}=\eta\,E_A/c^2.
}
\]
The theorem target is that the common pressure source density equals total
physical energy density in mass units:
\[
\boxed{
\sigma_{\rm grav}
=
\alpha_E\,\rho_E/c^2.
}
\]
This is expected because the long common pressure channel should couple to the
conserved scalar generator of stable medium evolution, namely total energy.

A more specific source theorem target is:
\[
\boxed{
S_u(x)
:=
\frac{1}{N\sqrt h}\frac{\delta S_{\rm m}}{\delta u(x)}
=
\rho_E(x)
+\hbox{suppressed corrections}.
}
\]
Here \(S_u\) is the source seen by the compression/lapse variable \(u\).
The reason is that \(u\) changes the local operational process rate, and the
generator of local process-rate change is the Hamiltonian density.  Thus the
common pressure channel should read \(\rho_E=\mathcal H_{\rm phys}\), not a
species-private tail detail.

Species-specific tail structure may remain in $K_{AB}$ or in internal sectors
(spin, charge, colour, phase, polarization, topology), but must not create a
second long-range spacetime carrier.  If an unsuppressed species-private
source term remains in $S_u$, the equivalence-principle source gate fails.

\begin{openthm}[Matter-coupled closure and source universality]
Beyond vacuum ADM closure, the matter-coupled constraints must close as well:
\[
\boxed{
\mathcal H_{\rm tot}
=
\mathcal H_{\rm grav}
+
\mathcal H_{\rm matter}
\approx0,
\qquad
\mathcal H_{i,\rm tot}
=
\mathcal H_{i,\rm grav}
+
\mathcal H_{i,\rm matter}
\approx0.
}
\]
The closure target is:
\[
\boxed{
\{H_{\rm tot}[N],H_{\rm tot}[M]\}
=
D_{\rm tot}\!\left[h^{ij}(N\partial_jM-M\partial_jN)\right].
}
\]
This requires stable matter families to use the same record metric and the
same total-energy source:
\[
\boxed{
S_{\rm matter}=S_{\rm matter}[g,\Psi],
\qquad
S_u=\rho_E.
}
\]
No unsuppressed species-private gravitational source may remain:
\[
\boxed{
\rho_{\rm grav}
\neq
\rho_E+\sigma_{\rm species}
\quad
\text{with }\sigma_{\rm species}\text{ physical and unsuppressed}.
}
\]
Internal/confined modes, including binding and gluon-like internal energy,
must contribute to \(E_{\rm total}\) while not defining an independent external
spacetime carrier.  This gate is open from \(\mathcal M\).
\end{openthm}

For the ADM matter action
\[
\boxed{
S_{\rm m}
=
\int dt\,d^3x\,
\left(
\pi_A\dot\psi_A
-N\mathcal H_{\rm m}
-N^i\mathcal H_i
\right),
\qquad
N=e^{-u},
}
\]
variation at fixed \(h_{ij}\) gives
\[
\boxed{
\delta_u S_{\rm m}
=
\int dt\,d^3x\,N\mathcal H_{\rm m}\,\delta u,
\qquad
\frac{1}{N\sqrt h}\frac{\delta S_{\rm m}}{\delta u}
=
\rho_E.
}
\]
Thus the low-energy ADM bridge already identifies the \(u\)-source with the
Hamiltonian density.  It remains to derive \(N=e^{-u}\) and the universal ADM
bridge from the base-medium.

\section{Delta-M Candidate Theorem}

\begin{openthm}[Conditional Delta-M theorem]
If the protected low-energy phase has:
\[
\boxed{
\text{one stable operational record phase with a common carrier/principal cone},
}
\]
\[
\boxed{
\text{exact Operational Relational Covariance},
}
\]
\[
\boxed{
Q_{AB}^{\mu\nu}=G^{\mu\nu}K_{AB},
}
\]
and a local two-derivative low-energy hierarchy, then the tested core permits no
unsuppressed independent medium residue:
\[
\boxed{
\Delta_{\mathcal M}=0
}
\]
up to suppressed high-energy or large-scale correction windows.
\end{openthm}

\section{Static Weak-Field Sector}

\begin{assumption}[Hamiltonian source]
In the weak static regime, the compression/lapse constraint is sourced by total
physical energy density $\rho_E$:
\[
\rho_E(\mathbf x)\ge0.
\]
\end{assumption}

The inertial mass of a localized source is:
\[
M
=
\frac{1}{c^2}\int d^3x\,\rho_E(\mathbf x).
\]

\begin{assumption}[Base-node calibration of \(G\)]
The source coefficient in the weak static sector is fixed by the minimal
operational node scale:
\[
\boxed{
G_{\rm RMG}
=
\frac{c^3\ell_*^2}{\hbar}.
}
\]
Equivalently, the gravitational stiffness is
\[
\boxed{
\frac{c^4}{4\pi G_{\rm RMG}}
=
\frac{\hbar c}{4\pi\ell_*^2}.
}
\]
When the universal operational node scale is identified with the observed
Planck-order length, \(G_{\rm RMG}\) equals the measured Newton constant.  The
remaining deeper task is not to choose a smaller length, but to derive
\(\ell_*\) itself from the stable base-medium record dynamics.
\end{assumption}

Below, \(G\) denotes this calibrated \(G_{\rm RMG}\) unless explicitly stated.

Use the static energy functional:
\[
\mathcal E[u]
=
\frac{c^4}{8\pi G_{\rm RMG}}\int d^3x\,|\nabla u|^2
-
\int d^3x\,\rho_Eu.
\]

\begin{result}[Static source equation]
Varying $u$ gives:
\[
\boxed{
\nabla^2u
=
-
\frac{4\pi G_{\rm RMG}}{c^4}\rho_E.
}
\]
\end{result}

With $u(\infty)=0$:
\[
u(\mathbf x)
=
\frac{G_{\rm RMG}}{c^4}
\int d^3x'\,
\frac{\rho_E(\mathbf x')}{|\mathbf x-\mathbf x'|}.
\]

Far from the source:
\[
\boxed{
u(r)=\frac{G_{\rm RMG}M}{rc^2}+O(r^{-2}).
}
\]

Thus the gravitational charge is inertial mass because the source is total
energy.

\section{PPN Weak-Field Check}

Using:
\[
U=c^2u,
\qquad
U(r)=\frac{G_{\rm RMG}M}{r},
\]
the metric gives:
\[
g_{00}
=
-e^{-2u}
=
-1+2u-2u^2+O(u^3),
\]
\[
g_{ij}
=
e^{2u}\delta_{ij}
=
(1+2u+O(u^2))\delta_{ij}.
\]

Therefore:
\[
\boxed{
\gamma=1,
\qquad
\beta=1.
}
\]

\section{Constraint And Radiation Targets}

The low-energy dynamic description must be expressible in ADM variables:
\[
ds^2
=
-N^2c^2dt^2
+h_{ij}(dx^i+N^idt)(dx^j+N^jdt).
\]

Weak split:
\[
N=1-u+O(2),
\]
\[
h_{ij}=(1+2u)\delta_{ij}+h_{ij}^{TT}+O(2),
\]
\[
N_i=\mathcal A_i+O(2).
\]

\begin{target}[Scalar and vector sectors]
The scalar/compression variable and the vector/flow variable must be
constraint or near-zone variables at low energy, not independent vacuum
radiation modes.
\end{target}

The absence of scalar/vector radiation is protected only if lapse and shift
remain non-propagating constraint variables.

Target form:
\[
\nabla^2u=-4\pi G\rho/c^2,
\]
\[
\nabla^2\mathcal A_i
=
-
\kappa_VG\mathcal P_i^T/c^3.
\]

\begin{target}[Tensor/shear radiation]
The only propagating vacuum gravitational modes at low energy must be:
\[
h_+,
\qquad
h_\times.
\]
They must have positive energy and propagate on the single record cone:
\[
c_{\rm GW}=c.
\]
Here \(c\) is not one of several speeds tuned to agree; it is the unique local
signal speed defined by the common record cone after Root~A closes.
\end{target}

Target wave equation:
\[
\left(
\frac{1}{c^2}\partial_t^2-\nabla^2
\right)h_{ij}^{TT}
=
\kappa_TGT_{ij}^{TT}/c^4.
\]

Vacuum:
\[
\left(
\frac{1}{c^2}\partial_t^2-\nabla^2
\right)h_{ij}^{TT}=0.
\]

The propagation target also excludes massive vector modes, massive spin-2
leakage, a second metric tensor mode, and any extra shear channel in the
tested local sector.  Propagation alone is not sufficient: compact-binary
emission must use the same protected tensor channel, with no leading
scalar-dipole or scalar-monopole excess in binary-pulsar or waveform tests.

\begin{openthm}[GW emission and radiation-reaction audit]
Propagation and emission are separate gates.  The local TT channel must not
only propagate on the record cone; it must also carry the outgoing energy flux
emitted by compact binaries.  The sourced radiative target is
\[
\boxed{
\left(\frac{1}{c^2}\partial_t^2-\nabla^2\right)h_{ij}^{TT}
=
\Pi_{TT}\,{\cal S}_{ij}[g_{\rm rec},T_{\mu\nu}]
+\hbox{suppressed corrections},
}
\]
where \(T_{\mu\nu}\) comes from \(S_{\rm m}[g_{\rm rec},\Psi]\) and
\(\rho_E=T_{\mu\nu}n^\mu n^\nu\).  The static and radiative constants must be
the same:
\[
\boxed{
G_{\rm Newton}=G_{\rm radiation}=G_{\rm record},
\qquad
c_{\rm matter}=c_{\rm GW}=c_{\rm record}.
}
\]
A mismatch between the Newtonian coupling, the conservative near-zone coupling
and the radiative flux normalization is a GW-emission failure.

The emission audit proceeds in the local asymptotically flat binary regime:
\[
\boxed{
r\gg R_{\rm source},\qquad r>\lambda_{\rm GW},\qquad
\lambda_{\rm GW}=c/f_{\rm GW}.
}
\]
The weak-field near zone must be matched to the wave zone; leading tensor
radiation reaction is the first dissipative correction, while full high-PN
waveform templates, merger and ringdown are deferred.

The leading multipole hierarchy must be derived rather than inserted:
\[
\boxed{
\text{monopole conserved},\qquad
\text{dipole removed by center-of-energy/sensitivity universality},\qquad
\text{quadrupole first radiative TT source}.
}
\]
The dipole-zero condition inherits Root~C:
\[
\boxed{
\alpha_A=\frac{q_A}{E_A/c^2},
\qquad
\alpha_A=\alpha_B
}
\]
or a bound/suppression on \(\alpha_A-\alpha_B\).  Without this, scalar-like
dipole radiation is not excluded.

The outgoing tensor flux must be positive and must equal the loss of near-zone
mechanical energy:
\[
\boxed{
P_{TT}=\int r^2d\Omega\,{\cal F}_{TT}[\dot h^{TT}],
\qquad
\frac{dE_{\rm orbit}}{dt}=-P_{TT}.
}
\]
For eccentric binaries and timing systems the angular-momentum balance must
also hold:
\[
\boxed{
\frac{dL_{\rm orbit}}{dt}=-{\cal F}^{\rm ang}_{TT}.
}
\]
The leading chirp mass is built from Root~C source masses
\[
\boxed{
m_A=E_A/c^2,\qquad
{\cal M}_c=\frac{(m_1m_2)^{3/5}}{(m_1+m_2)^{1/5}}.
}
\]
Non-tensor channels -- scalar monopole, scalar dipole, vector dipole, massive
spin-2 leakage, second metric flux or pure breathing radiation -- must be
constraint, gauge, near-zone, short-range, gapped, damped, high-energy
suppressed or below the relevant binary-pulsar and waveform bounds.  If such a
classification is not supplied, the channel is an emission anomaly.
\end{openthm}

\medskip\noindent\textbf{Quantitative emission MVP.}
The first concrete emission target is a binary-pulsar timing comparison, with
PSR~B1913+16 as a canonical benchmark.  The table to be filled by the
calculation is:
\[
\boxed{
\begin{array}{c|c|c}
\text{quantity} & \text{RMG MVP target} & \text{failure mode}\\
\hline
\dot P_b & \text{from }dE_{\rm orbit}/dt=-P_{TT} & \text{wrong damping}\\
m_A & E_A/c^2\text{ from Root C} & \text{source mass mismatch}\\
\alpha_A-\alpha_B & 0\text{ or below timing bounds} & \text{scalar dipole loss}\\
\text{eccentricity effects} & dL_{\rm orbit}/dt=-{\cal F}^{\rm ang}_{TT} &
\text{wrong timing evolution}
\end{array}
}
\]
The benchmark observational scale is the Hulse--Taylor orbital decay,
\(\dot P_b\sim-2.4\times10^{-12}\,{\rm s/s}\), after the standard timing and
environmental corrections.  RMG must reproduce the tensor energy-loss channel
without adding a private scalar/vector flux.  For GW speed, the multimessenger
benchmark remains
\[
\boxed{
\left|c_{\rm GW}/c-1\right|\lesssim10^{-15}
}
\]
as a propagation gate that emission must not disturb.

\begin{gate}[GW detection: common-mode cancellation versus shear signal]
A uniform or adiabatic compression of the local record system rescales the
apparatus, clocks, rulers and light standards together; the scalar/compression
channel \(u\) therefore couples to an interferometer signal only through
differential terms.  The local Michelson-like difference channel projects out
the common-mode part:
\[
\boxed{
\Pi_{\rm det}\,\delta g_{\rm common}=0.
}
\]

The detector-visible gravitational-wave channel must instead be a differential
record deformation:
\[
\boxed{
\Pi_{\rm det}\,\delta g_{\rm rec}
=
\Pi_{TT}\,\delta g_{\rm rec}
+
\Pi_{\mathrm{shear/defect}}\,\delta u_{\rm noncommon}.
}
\]
The first term is the protected tensor/shear sector \(h_{ij}^{TT}\).  The
second term is allowed only as a controlled conversion of breathing/compression
into detector-visible shear, tidal or symmetry-defect response.  It must not
survive as an unsuppressed local scalar radiation mode.

Here ``defect projection'' means a detector-visible differential record
deformation which has a TT/shear component after projection onto the detector
response.  It is a placeholder for a later detector-response calculation, not
a license to re-label a pure scalar breathing mode as a tensor wave.

A local apparatus cannot register a uniform rescaling of the medium from which
its rods, clocks and light standards are built; it can register only
anisotropic or differential record deformation.  Observed gravitational-wave
signals must therefore be carried by the protected TT/shear response or by a
suppressed noncommon conversion channel, not by a pure common-mode scalar
breathing.
\end{gate}

\begin{openthm}[Constraint protection route]
Derive from the base-medium that the local low-energy variables enter in
ADM-like form:
\[
\boxed{
S_{\rm local}
=
\int dt\,d^3x
\left(
\pi^{ij}\dot h_{ij}
-
N\mathcal H
-
N^i\mathcal H_i
\right)
}
\]
with no independent low-energy kinetic terms
\[
\boxed{
\dot N^2,\qquad
\dot N_i\dot N^i,\qquad
\dot u^2,\qquad
\dot{\mathcal A}_i\dot{\mathcal A}^i
}
\]
for physical scalar/vector gravitational variables.  If \(N\) and \(N^i\)
impose first-class Hamiltonian and momentum constraints which close under time
evolution, then \(u\) and local flow/shift variables are constraint/gauge or
near-zone variables, not independent radiation.  The vacuum gravitational
record sector then has only
\[
\boxed{
h_+,\qquad h_\times
}
\]
as physical propagating modes.  This is the local mechanism behind flow
decoupling and the preferred-frame null target.
\end{openthm}

\section{ADM/EH Fixed Point}

\begin{target}[Nonlinear low-energy completion]
The nonlinear low-energy fixed point is:
\[
\boxed{
S_{\rm low}
=
\frac{c^4}{16\pi G}
\int d^4x\sqrt{-g_{\rm rec}}\,(R[g_{\rm rec}]-2\Lambda)
+
S_{\rm matter}[g_{\rm rec},\Psi]
+
S_{\rm suppressed}.
}
\]
\end{target}

\medskip\noindent\textbf{Coefficient status.}
In the ADM filter below, \(\alpha\) is a temporary curvature coefficient.  The
Einstein--Hilbert target fixes \(\alpha=1\) after the normalization of the
measured Newton constant \(G\) is chosen.  The cosmological term \(\Lambda\) is
kept as a local-core background parameter in the MVP, but in the full program
it must be derived, regulated, or absorbed into the cosmological-background
closure.  It may not be used to repair a local ADM/EH or source-universality
failure.

ADM form:
\[
S_{\rm ADM}
=
\int dt\,d^3x
\left(
\pi^{ij}\dot h_{ij}
-
N\mathcal H
-
N^i\mathcal H_i
\right).
\]

Hamiltonian and momentum constraints:
\[
\mathcal H\approx0,
\qquad
\mathcal H_i\approx0.
\]

The target constraint algebra includes:
\[
\{H[N],H[M]\}
=
D\!\left[h^{ij}(N\partial_jM-M\partial_jN)\right].
\]

Operational Relational Covariance makes ADM closure plausible, but it does not
replace the proof.

\begin{openthm}[Main closure theorem]
Derive from the base-medium:
\[
\boxed{
\mathcal M
\Rightarrow
\text{universal principal symbol}
\Rightarrow
\text{ADM constraint closure}
\Rightarrow
S_{\rm EH}+S_{\rm matter}.
}
\]
\end{openthm}

\begin{openthm}[Hypersurface-deformation closure filter]
The local Hamiltonian constraint must pass the ADM deformation-algebra filter.
For the two-derivative ansatz
\[
\boxed{
\mathcal H
=
\frac{1}{\sqrt h}
\left(
\pi^{ij}\pi_{ij}-\lambda\pi^2
\right)
-
\sqrt h(\alpha R-2\Lambda)
+
\Delta_{\mathcal M},
}
\]
the target bracket is:
\[
\boxed{
\{H[N],H[M]\}
=
D\!\left[h^{ij}(N\partial_jM-M\partial_jN)\right].
}
\]
This filter requires, in the protected local core:
\[
\boxed{
\lambda=\frac12,
\qquad
\Delta_{\mathcal M}=0
\quad
\text{or constrained/gauge/suppressed},
}
\]
and no unsuppressed higher-derivative curvature terms at the tested
two-derivative fixed point.  If this closure filter is derived from
\(\mathcal M\), the trace/volume direction of the DeWitt supermetric belongs to the
constrained sector and does not propagate as an independent scalar mode, and
the local two-derivative fixed point is \(S_{\rm EH}+S_{\rm matter}\) up to
suppressed corrections.
\end{openthm}

\begin{openthm}[Constraint preservation and matter-coupled Dirac closure]
ADM form is not enough.  The Hamiltonian and momentum constraints must be
preserved by the evolution they generate:
\[
\boxed{
\dot{\mathcal H}\approx0,
\qquad
\dot{\mathcal H}_i\approx0.
}
\]
The matter-coupled Dirac analysis must show that primary constraints lead to
the Hamiltonian and momentum constraints, that secondary constraints close
without making lapse or shift physical, and that any second-class remnants are
solved or remove only nonphysical variables.  If matter coupling turns the
first-class ADM system into a non-closing algebra, the local target action
fails even if the vacuum bracket has the desired form.
\end{openthm}

\section{Local Gravity Closure Package}

The many open local-core gates reduce to three root theorems.

\begin{openthm}[Root A: one external record carrier]
Derive from the base-medium stable record phase:
\[
\boxed{
\text{stable external record-forming mode}
\Rightarrow
\text{one common external long-tail carrier channel}.
}
\]
All non-common branches must be internal, confined, gapped, damped,
constrained, gauge or unstable.  If Root A closes, it gives one operational
principal cone, no low-energy birefringence and the factorization
\[
\boxed{
Q_{AB}^{\mu\nu}=G^{\mu\nu}K_{AB}
}
\]
on physical record modes.
\end{openthm}

\begin{openthm}[Root A spectral/resonance closure]
Root A must be proven by a spectral statement around the stable record phase
\(\mathcal M_0\).  Linearize the base-medium dynamics:
\[
\boxed{
\delta{\cal E}[\mathcal M_0]
=
{\cal L}_{\mathcal M_0}\delta\Phi+\cdots.
}
\]
The required split is:
\[
\boxed{
\mathrm{Spec}({\cal L}_{\mathcal M_0})
=
{\cal B}_{\rm rec}
\cup
{\cal B}_{\rm int},
\qquad
N_{\rm cone}({\cal B}_{\rm rec}^{\rm gapless})=1
}
\]
as one external record carrier channel.  This is a one-common-carrier condition,
not a one-scalar-mode condition.  The common channel may contain shared
polarization, shear or tensor patterns, but it must not split into
species-private external carriers.  Stable record-forming tail imprints must
phase-lock to the base-medium rhythm \(\nu_0\) or its allowed harmonic channel:
\[
\boxed{
\Pi_{\rm rec}T_A^{\rm long}
=
Z_A\mathcal C+\hbox{internal/lower-order details},
\qquad
\operatorname{principal}(\mathcal C)
=
G^{\mu\nu}k_\mu k_\nu.
}
\]
All other branches must be internal, confined, gapped, damped, constrained,
gauge, unstable, high-energy suppressed or large-scale-only.  This is the
microscopic calculation still needed for Root A.

\medskip\noindent\textbf{Suppression catalogue.}
These escape categories are not free passes.  Each requires a structural or
quantitative reason why it is absent from local record observables:
\begin{itemize}
\item high-energy suppressed: corrections scale with \(E/\Lambda_{\rm UV}\) or
  \(k/\Lambda_{\rm UV}\) and are below laboratory, Solar-System and GW bounds;
\item large-scale-only: the term activates only outside the local tested
  regime and does not affect Solar-System, binary-pulsar or local GW tests;
\item confined or internal: the mode cannot propagate as an external rod,
  clock, signal or detector record;
\item gapped or damped: the mode has no coherent long-range low-energy
  propagation in the tested regime;
\item gauge or constrained: the mode is removed by \(\Omega_{\mathcal M}\),
  a first-class generator or an elliptic/near-zone constraint;
\item unstable: the branch is excluded from the stable phase or decays before
  record formation.
\end{itemize}
If no such suppression or removal test is available, the branch remains a
Root~A fail-risk rather than an allowed remainder.
\end{openthm}

\medskip\noindent\textbf{Root A--Root B compatibility.}
Root A must not assume ADM closure, but its record/internal projectors and the
gauge/constraint reduction induced by \(\Omega_{\mathcal M}\) must remain
compatible with the canonical analysis needed in Root~B.  A one-cone spectrum
obtained by a projection that destroys the later ADM phase space is not a
valid Root~A pass.

\medskip\noindent\textbf{Remark (Root A closure status under ORC).}
Conditional on the Operational Relational Covariance postulate of
Section~``Primitive Postulates'', the Root~A open theorem closes as a
derived Result.  The argument proceeds in three steps:
(i)~Goldstone--Bogoliubov spectral counting on the stable phase
$\mathcal M_0$ with broken $U(1)_\nu$ rhythm yields one type-I scalar
Goldstone (the phonon) plus two transverse-traceless tensor modes;
(ii)~the ``one operational geometry'' clause of ORC forces
$c_{\rm phase}=c_{\rm graviton}$, so all gapless record modes share the
single principal symbol $G^{\mu\nu}k_\mu k_\nu$;
(iii)~stable record-forming oscillons project on this single channel with
species-dependent amplitudes $Z_A$ but a common cone.  Without ORC the
closure remains open; the microscopic derivation of ORC itself is left as
the deeper open program.

\begin{openthm}[Root B: record-embedding ADM closure]
Derive that changes of slicing, labels and record parametrization are gauge
redundancies of the same physical record history.  These redundancies must
generate first-class constraints:
\[
\boxed{
\mathcal H\approx0,
\qquad
\mathcal H_i\approx0,
}
\]
with the ADM hypersurface-deformation algebra.  If Root B closes, local
scalar/vector gravitational sectors are constrained, flow is shift-like in the
tested core, only \(h_+,h_\times\) propagate, \(\lambda=1/2\) is selected and
the two-derivative fixed point is \(S_{\rm EH}\).
\end{openthm}

\begin{openthm}[Root B record-action construction bridge]
Root B may audit a projected record action, but it cannot treat that action as
a magic input.  The medium package must supply the chain
\[
\boxed{
\mathcal D_{\mathcal M}
\longrightarrow
S_{\rm eff}^{\rm rec}
\longrightarrow
S_{\mathcal R}[g_{\rm rec},\Psi_{\rm rec}]
+\text{controlled remainders}.
}
\]
The object delivered to Root B must be local up to controlled nonlocal tails,
have a well-defined two-derivative truncation, match the projected record
spectrum \(({\cal L}_{\mathcal M_0},\Pi_{\mathcal R},\Pi_{\mathcal I})\), give
declared tensor or density type to every record variable, remove visible
preferred-frame leaks through \(\mathcal G_{\rm int}\), and introduce matter
through record observables rather than a private lapse, shift, source or
metric.  If this bridge is missing, Root B remains a target audit rather than
a medium-derived ADM closure proof.
\end{openthm}

\begin{openthm}[Root B embedding/symplectic closure]
Root B requires showing that the physical object is a relational record
history \(W_{\mathcal R}\), while a foliation/embedding \(X_F\) is only a
description of that history:
\[
\boxed{
X_F\sim X'_F,
\qquad
\Gamma_{\rm phys}=\Gamma_{\mathcal R}/\mathcal G_{\rm emb}.
}
\]
This equivalence must be a true gauge degeneracy of the record symplectic
structure:
\[
\boxed{
\iota_{\delta_\xi}\Omega_{\mathcal R}\approx0.
}
\]
Then the embedding generators must take the ADM form
\[
\boxed{
G[\xi]=D[N^i]+H[N],
\qquad
D[N^i]=\int d^3x\,N^i\mathcal H_i,
\qquad
H[N]=\int d^3x\,N\mathcal H,
}
\]
with
\[
\boxed{
\mathcal H_i\approx0,
\qquad
\mathcal H\approx0.
}
\]
The composition of two embedding changes must be the hypersurface-deformation
algebra:
\[
\boxed{
\{D[N^i],D[M^i]\}=D[[N,M]^i],
}
\]
\[
\boxed{
\{H[N],D[M^i]\}=H[\mathcal L_MN],
}
\]
\[
\boxed{
\{H[N],H[M]\}
=
D\!\left[h^{ij}(N\partial_jM-M\partial_jN)\right],
}
\]
where the structure function \(h^{ij}\) is the same inverse spatial metric
seen by the record sector.  Lapse and shift are then embedding multipliers,
not extra physical medium particles:
\[
\boxed{
S_{\rm loc}
=
\int dt\,d^3x\,
\left(\pi^{ij}\dot h_{ij}-N\mathcal H-N^i\mathcal H_i\right),
\qquad
\dot N^2=\dot N_i\dot N^i=0.
}
\]
This no-kinetic lapse/shift structure is a target output, not a starting
assumption.  It must follow from the projected record action and its
presymplectic form: the momenta conjugate to \(N\) and \(N^i\) must be primary
constraints, and the Dirac algorithm must preserve them without making lapse
or shift physical.  It may not be imposed merely by choosing an ADM-looking
gauge.
If this theorem closes, the scalar and vector gravitational sectors are
constraints/near-zone bookkeeping, local flow is shift-like, only two tensor
modes propagate, the DeWitt value \(\lambda=1/2\) is selected and the
two-derivative local fixed point is \(S_{\rm EH}\).
\end{openthm}

\begin{openthm}[Root B finite sufficiency checklist]
Root B is reduced to five sufficient conditions on the low-energy record
sector:
\[
\boxed{
\begin{array}{ll}
B1 & \text{no observable absolute record time},\\
B2 & \text{one record geometry }h_{ij},\\
B3 & \text{embedding changes are presymplectic null directions},\\
B4 & \text{null directions have local generators }D[N^i],H[N],\\
B5 & \text{generator composition is anomaly-free}.
\end{array}
}
\]
The algebraic danger is a residual anomaly in the normal-normal bracket:
\[
\boxed{
\{H[N],H[M]\}
=
D\!\left[h^{ij}(N\partial_jM-M\partial_jN)\right]
+A[N,M].
}
\]
Root B requires
\[
\boxed{
A[N,M]=0
}
\]
on the local tested record sector.  A nonzero \(A\) signals an unsuppressed
preferred-frame, scalar, vector or non-record tensor structure in the local
sector.
Equivalently, lapse/shift or local flow has become physical rather than
embedding bookkeeping.  The action-level check is:
\[
\boxed{
\dot N^2,\quad \dot N_i\dot N^i,\quad
\text{unsuppressed independent scalar/vector kinetic terms}
\quad\text{are absent}.
}
\]
Again, this is a derived condition: lapse/shift no-kinetic status must be
obtained from the presymplectic/Dirac audit, not inserted as a gauge choice.
Thus Root B is not an aesthetic GR assumption.  It is the finite test:
\[
\boxed{
B1+B2+B3+B4+B5
\Rightarrow
\text{ADM local closure}.
}
\]
\end{openthm}

\begin{openthm}[Root B physical motivation status]
The base-medium picture supports Root B at the following graded level:
\[
\boxed{
\begin{array}{c|c}
\text{condition} & \text{motivation status}\\
\hline
B1 & \text{strong support: time/measurement are operational and relational}\\
B2 & \text{partial: requires Root A one-carrier closure}\\
B3 & \text{open: requires a record presymplectic form }\Omega_{\mathcal R}\\
B4 & \text{open: requires local moment-map generators }D,H\\
B5 & \text{open: requires anomaly cancellation }A[N,M]=0
\end{array}
}
\]
A single undivided base medium has no internal ruler, delay or comparison
until it self-differentiates into distinguishable record-forming nodes.  This
strongly supports the absence of an observable absolute record time.  It does not by itself prove the canonical statement
\[
\boxed{
\iota_{\delta_\xi}\Omega_{\mathcal R}\approx0
\quad\Rightarrow\quad
G[\xi]=D[N^i]+H[N].
}
\]
The next Root B step is to construct the low-energy record phase space
\(\Gamma_{\mathcal R}\), its presymplectic form \(\Omega_{\mathcal R}\), the
embedding-variation vector fields \(\delta_\xi\) and the local generators
\(D,H\).  The pass condition remains
\[
\boxed{
\{H[N],H[M]\}
-
D\!\left[h^{ij}(N\partial_jM-M\partial_jN)\right]
=0
}
\]
in the tested local record sector.
\end{openthm}

\begin{openthm}[Record phase space and presymplectic Root B construction]
The non-circular Root B construction starts from the covariant record phase
space
\[
\boxed{
\Gamma_{\mathcal R}
=
\{\Phi_{\mathcal R}\ {\rm solving\ the\ record\ equations}\}/\mathcal G_{\rm int},
\qquad
\Phi_{\mathcal R}=(g_{\rm rec},\Psi_{\rm rec}),
}
\]
where \(\mathcal G_{\rm int}\) removes internal/non-record redundancies but
does not yet quotient embedding freedom.  Let the low-energy record action
satisfy
\[
\boxed{
\delta L_{\mathcal R}
=
E_{\mathcal R}\delta\Phi_{\mathcal R}
+
d\theta_{\mathcal R}(\delta\Phi_{\mathcal R}).
}
\]
Define the covariant presymplectic current
\[
\boxed{
\omega_{\mathcal R}(\delta_1\Phi,\delta_2\Phi)
=
\delta_1\theta_{\mathcal R}(\delta_2\Phi)
-
\delta_2\theta_{\mathcal R}(\delta_1\Phi)
}
\]
and the slice form
\[
\boxed{
\Omega_{\mathcal R,\Sigma}
=
\int_\Sigma \omega_{\mathcal R}.
}
\]
On shell and without boundary flux, \(d\omega_{\mathcal R}=0\), so
\[
\boxed{
\Omega_{\mathcal R,\Sigma}
=
\Omega_{\mathcal R,\Sigma'}.
}
\]
This is the formal version of one record history described by different
slices.  An embedding deformation acts as
\[
\boxed{
\delta_\xi\Phi_{\mathcal R}
=
\mathcal L_\xi\Phi_{\mathcal R}
\quad
\text{up to internal gauge compensation}.
}
\]
This statement must be evaluated field by field:
\[
\boxed{
\begin{array}{rcl}
\delta_\xi g_{\rm rec} &=& {\cal L}_\xi g_{\rm rec},\\
\delta_\xi \varphi &=& \xi^\mu\partial_\mu\varphi,\\
\delta_\xi T &=& {\cal L}_\xi T\quad\text{for its declared tensor type},\\
\delta_\xi \rho &=& {\cal L}_\xi \rho\quad\text{including density weight}.
\end{array}
}
\]
If a record variable has no declared tensor or density type, the embedding
audit is not yet computable.
Root B requires this direction to be presymplectically null:
\[
\boxed{
\Omega_{\mathcal R}(\delta\Phi,\delta_\xi\Phi)\approx0.
}
\]
Equivalently, the Noether current
\[
\boxed{
J_\xi
=
\theta_{\mathcal R}(\mathcal L_\xi\Phi_{\mathcal R})
-
\xi\cdot L_{\mathcal R}
}
\]
must split as
\[
\boxed{
J_\xi=C_\xi+dQ_\xi,
\qquad
\int_\Sigma C_\xi=H[N]+D[N^i],
\qquad
\xi=Nn+N^ie_i.
}
\]
The pass condition is therefore
\[
\boxed{
\Omega_{\mathcal R}(\delta\Phi,\mathcal L_\xi\Phi)
=
\delta\!\left(H[N]+D[N^i]\right)
}
\]
with \(H\approx0\) and \(D\approx0\) in the tested local record sector.  Any
bulk remainder that is not a constraint or boundary charge is the Root B
preferred-frame/scalar/vector anomaly.

\medskip\noindent
The brackets must be computed from \(\Omega_{\mathcal R}\), not copied from the
target algebra.  Canonical variables and momenta are read from
\(\theta_{\mathcal R}\); second-class constraints require the Dirac bracket;
auxiliary fields must be eliminated before the physical mode count; and
boundary terms must be separated before any bulk anomaly \(A[N,M]\) is
declared.
\end{openthm}

\begin{openthm}[Noether split and background-free record-action gate]
The Noether split required by Root B follows if the local tested record action
is background-free:
\[
\boxed{
L_{\mathcal R}^{\rm local}
=
L_{\mathcal R}[g_{\rm rec},\Psi_{\rm rec}]
}
\]
up to constraints, gauge choices, boundary terms, integrated-out internal
contributions, high-energy suppressed corrections and large-scale-only
flow/MOND terms.  In that case an embedding deformation is a record
diffeomorphism:
\[
\boxed{
\delta_\xi L_{\mathcal R}
=
\mathcal L_\xi L_{\mathcal R}
=
d(\xi\cdot L_{\mathcal R}),
}
\]
and the Noether current can be written as
\[
\boxed{
J_\xi=C_\xi+dQ_\xi,
\qquad
C_\xi=N\mathcal H+N^i\mathcal H_i.
}
\]
The forbidden local failure is a non-record background structure \(B\):
\[
\boxed{
L_{\mathcal R}^{\rm local}
=
L_{\mathcal R}[\Phi_{\mathcal R};B].
}
\]
If \(B\) is not a gauge, constraint, boundary, integrated-out, suppressed or
large-scale-only object, then the embedding variation leaves a bulk anomaly:
\[
\boxed{
\mathcal A_\xi
\sim
\frac{\delta L_{\mathcal R}}{\delta B}\mathcal L_\xi B.
}
\]
The local-core forbidden \(B\)'s are:
\[
\boxed{
T_{\mathcal M},\quad
U^\mu_{\mathcal M},\quad
v^\mu v^\nu,\quad
\text{species-private metric/clock},\quad
\dot N^2,\quad
\dot N_i\dot N^i,\quad
\text{extra record cone}.
}
\]
Every bulk remainder in \(J_\xi\) must therefore be classified as exactly one
of:
\[
\boxed{
\text{constraint/gauge/boundary/internal/gapped/damped/suppressed/large-scale-only/anomaly}.
}
\]
Root B requires the final class to vanish in the local tested sector:
\[
\boxed{
\mathcal A_\xi^{\rm local}=0
\quad\Longleftrightarrow\quad
A[N,M]=0.
}
\]
The allowed classes require a structural or scale test.  High-energy
remainders must scale below the relevant observation row, for example as
\((E_{\rm test}/\Lambda_{\rm UV})^2\) or
\((k_{\rm test}/\Lambda_{\rm UV})^2\); local PPN-sensitive terms need at least
the corresponding weak-field bound.  Large-scale-only terms must activate only
outside Solar-System, binary-pulsar and local GW regimes; gapped or damped
terms must lack coherent long-range propagation through the tested baseline.
\end{openthm}

\begin{openthm}[Background-free two-derivative record-action fixed point]
If Root A supplies one record metric \(g_{\rm rec}\), Root B removes all
local non-record background structures, and the tested local regime admits a
two-derivative effective hierarchy, then the local record action has the form
\[
\boxed{
L_{\mathcal R}^{\rm local}
=
\sqrt{-g_{\rm rec}}
\left[
\alpha R[g_{\rm rec}]
-2\Lambda
+L_{\rm m}(g_{\rm rec},\Psi_{\rm rec})
\right]
+dB
+O(\partial^4/M_*^2).
}
\]
At the two-derivative fixed point this is
\[
\boxed{
S_{\rm EH}+S_{\rm matter}
}
\]
up to normalization, cosmological term, boundary terms and suppressed
higher-derivative corrections.  The two-derivative ansatz is itself part of
the audit: higher-derivative operators such as \(R^2\),
\(R_{\mu\nu}R^{\mu\nu}\), Weyl-squared terms or preferred-frame curvature
combinations must be suppressed by the hierarchy scale, not used to repair a
failed ADM bracket.  The leading curvature operator must be the EH operator,
with \(\alpha\) absorbed into the measured \(G\) or identified as a
deviation/failure.  The forbidden local background leaks are:
\[
\boxed{
U^\mu_{\mathcal M}U^\nu_{\mathcal M}\nabla_\mu\Psi\nabla_\nu\Psi,\quad
v^\mu v^\nu R_{\mu\nu},\quad
\dot N^2,\quad
\dot N_i\dot N^i,\quad
g_A^{\mu\nu}\partial_\mu\Psi_A\partial_\nu\Psi_A,
\quad
C^{\mu\nu}k_\mu k_\nu\not\propto g_{\rm rec}^{\mu\nu}k_\mu k_\nu.
}
\]
Each such term must be absent, constrained, gauge, boundary-only,
integrated-out/internal, high-energy suppressed or large-scale-only.  Otherwise
the two-derivative fixed point is not GR and Root B fails in the local tested
core.  Thus the local closure chain is
\[
\boxed{
\text{one record metric}
+\text{background-free action}
+\text{two-derivative hierarchy}
\Rightarrow
S_{\rm EH}+S_{\rm matter}.
}
\]
\end{openthm}

\medskip\noindent\textbf{Remark (Root B closure status under ORC).}
Conditional on the Operational Relational Covariance postulate of
Section~``Primitive Postulates'', the Root~B open theorem closes as a
derived Result.  The chain is:
(i)~ORC's ``slicing/labels/parametrization are descriptions, not extra
physical structures'' forces lapse $N$ and shift $N^i$ into the role of
Lagrange multipliers, so $\delta S/\delta N=0$ and $\delta S/\delta N^i=0$
yield $\mathcal H\approx 0$, $\mathcal H_i\approx 0$;
(ii)~ORC's exclusion of unobservable substrate handles forbids the anomaly
$A[N,M]$, so the hypersurface-deformation algebra closes;
(iii)~by Kuchar--Teitelboim's ``geometrodynamics regained'' theorem (1976),
the unique local two-derivative Hamiltonian with first-class HDA
closure and Riemannian configuration space is Einstein--Hilbert plus
minimally coupled matter, with DeWitt coefficient $\lambda=1/2$;
(iv)~at the linearized level, $h_+,h_\times$ are the only propagating
gravitational modes; scalar/vector/breathing sectors are constrained.

\begin{openthm}[Root C: total-energy source projection]
Derive the far-tail/source theorem:
\[
\boxed{
Q_A^{(0)}=\eta E_A/c^2,
\qquad
S_u=\rho_E,
}
\]
where \(E_A\) includes rest, kinetic, binding and internal/confined energy.
No unsuppressed species-private source may remain.  If Root C closes, the
equivalence-principle source gate and compact internal-energy effacement gate
close.
\end{openthm}

\medskip\noindent\textbf{Root C inheritance from Roots A and B.}
Root C uses, but does not re-prove, the previous root outputs:
\[
\boxed{
\begin{array}{lll}
\text{one record metric }g_{\rm rec} &\to& C1\text{ matter action target},\\
N=e^{-u}\text{ or equivalent bridge} &\to& C2\text{ lapse variation},\\
\text{ADM matter form with }N\text{ multiplier} &\to& C3\text{ source theorem},\\
\text{no scalar/vector vacuum radiation} &\to& C5\text{ anomaly control},\\
\Pi_{\mathcal R}\text{ record/internal split} &\to& \text{internal energy accounting}.
\end{array}
}
\]
The equivalence principle, \(q_A\propto E_A/c^2\), and
\(\sigma_{\rm species}=0\) are outputs of Root~C, not inputs.

\begin{openthm}[Root C finite source chain]
Root C reduces to five source conditions:
\[
\boxed{
\begin{array}{ll}
C1 & S_{\rm m}=S_{\rm m}[g_{\rm rec},\Psi]\quad\text{universal matter action},\\
C2 & N=e^{-u}\quad\text{or equivalent compression/lapse bridge},\\
C3 & S_u=(N\sqrt h)^{-1}\delta S_{\rm m}/\delta u=\rho_E,\\
C4 & \nabla^2u=-\kappa\rho_E\quad\text{far-tail Gauss law},\\
C5 & \sigma_{\rm species}^{\rm long,local}=0.
\end{array}
}
\]
The bridge in \(C2\) must come from the Root~B handoff: \(N\) is the
operational lapse of the record metric, expressed through the compression
variable \(u\).  An ``equivalent'' bridge means a redefinition that produces
the same shared lapse variation for every stable matter family; it cannot hide
a species-dependent lapse.
Then an isolated source obeys
\[
\boxed{
\oint_{\partial B_R}\nabla u\cdot dS
=
-\kappa\int_{B_R}\rho_E\,d^3x,
\qquad
u(r)=\frac{Q^{(0)}}{r}+O(r^{-2}),
}
\]
and therefore
\[
\boxed{
Q^{(0)}=\eta\,E_{\rm total}/c^2.
}
\]
This far-tail regime means \(r\gg R_{\rm source}\), \(r\gg\ell_*\), and a
convergent multipole expansion with higher multipoles suppressed by the
relevant powers of \(R_{\rm source}/r\).  A fitted \(1/r\) tail may not be used
backwards to infer the local source theorem.
The substantive content of Root C is not the far \(1/r\) integral but the
requirement that the compression/lapse source equals the total physical energy
for all stable record matter:
\[
\boxed{
S_u=\rho_E
}
\]
where the source density in the ADM theorem is measured by the lapse normal,
\[
\boxed{
\rho_E=T_{\mu\nu}n^\mu n^\nu,
}
\]
with \(n^\mu\) the ADM unit normal in the record metric.  For radiation and
other traceless fields, the source is this energy density, not the trace
\(T^\mu{}_\mu\).
with no unsuppressed species-private remainder:
\[
\boxed{
S_u\neq\rho_E+\sigma_{\rm species}
\quad
\text{for physical long-range local }\sigma_{\rm species}.
}
\]
Species-specific structure is allowed only if it is an internal divergence,
gauge, boundary multipole, high-energy suppressed, short-range/confined or
already included in \(\rho_E\).  Internal, binding, gluon-like and confined
energy must contribute to \(E_{\rm total}\) while not producing an independent
external cone:
\[
\boxed{
E_{\rm total}
=
E_{\rm rest}+E_{\rm kinetic}+E_{\rm binding}+E_{\rm internal}.
}
\]
Here binding includes self-gravitational binding in the strong-equivalence
audit.  Self-gravitating bodies must not fall differently merely because a
different fraction of their total mass is stored as gravitational binding
energy.  In local tests this is the Nordtvedt-type failure mode; in compact
binaries it appears as a sensitivity/dipole-radiation audit.

Self-gravitational binding is not assumed to be an ordinary local matter
density.  Root C treats it through the canonical total energy or quasi-local
charge appropriate to the record metric and through compact-object
sensitivities.  The pass condition is that this binding changes the external
monopole only through \(E_{\rm total}\), not through an extra
composition-readable source charge.
\end{openthm}

\begin{openthm}[C3 lapse-variation source theorem]
Assume the stable record matter sector satisfies
\[
\boxed{
S_{\rm m}=S_{\rm m}[g_{\rm rec},\Psi],
\qquad
N=e^{-u},
}
\]
and has no unsuppressed explicit species-private \(u\)-coupling outside the
record metric.  In canonical form,
\[
\boxed{
S_{\rm m}
=
\int dt\,d^3x\,
\left(
\pi_A\dot\psi_A
-N\mathcal H_{\rm m}
-N^i\mathcal H_{i,\rm m}
\right).
}
\]
At fixed \(h_{ij},N^i,\psi_A,\pi_A\), treating \(u\) as the independent
compression variable and \(N\) as fixed by the bridge \(N=e^{-u}\),
\[
\boxed{
\delta N=-N\delta u,
\qquad
\delta_uS_{\rm m}
=
\int dt\,d^3x\,N\mathcal H_{\rm m}\delta u.
}
\]
Therefore
\[
\boxed{
S_u
=
\frac{1}{N\sqrt h}\frac{\delta S_{\rm m}}{\delta u}
=
\frac{\mathcal H_{\rm m}}{\sqrt h}
=
\rho_E.
}
\]
This is the Root C bridge: changing compression/lapse changes the local
process rate, and the generator of local process-time translation is the
Hamiltonian density.  Internal, binding, gluon-like and confined energy are
included because
\[
\boxed{
\mathcal H_{\rm m}
=
\mathcal H_{\rm rest}
+\mathcal H_{\rm kinetic}
+\mathcal H_{\rm binding}
+\mathcal H_{\rm internal}.
}
\]
For quantum matter, \(\mathcal H_{\rm m}\) means the expectation value of the
matter Hamiltonian density in the relevant matter state.  Vacuum subtractions
must be handled consistently with the cosmological term in the target action;
vacuum energy cannot be silently counted in local source tests and then
removed in the cosmology window.
Root C fails only if a physical long-range local remainder survives:
\[
\boxed{
S_u=\rho_E+\sigma_{\rm species}^{\rm long,local},
\qquad
\sigma_{\rm species}^{\rm long,local}\neq0.
}
\]
Thus the C3 gate is: all stable record matter must see \(u\) only through the
same record metric/lapse, except for gauge, constrained, boundary, internal,
short-range or suppressed terms already accounted for in \(\rho_E\).
\end{openthm}

\begin{openthm}[Metric-exhaustion and no species-private \(u\)-coupling]
If the stable local matter sector is exhausted by one record metric,
\[
\boxed{
S_{\rm m}=S_{\rm m}[g_{\rm rec},\Psi],
}
\]
then all allowed local \(u\)-dependence is metric/lapse dependence.  Species
labels may appear in masses, charges, spin, internal Hamiltonians, confined
sectors, kinetic matrices, multipoles and finite-size corrections, but not as
a separate long-range local term
\[
\boxed{
\Delta S_A
=
\int d^4x\,N\sqrt h\,F_A(u,\Psi_A).
}
\]
If such a term is physical and unsuppressed, then
\[
\boxed{
S_u=\rho_E+\sigma_A,
}
\]
so \(u\) becomes a species-readable scalar background rather than just the
record lapse/compression variable.  This is a fifth-force/source anomaly: two
systems with the same total energy but different composition would source or
fall differently.  Therefore the local Root C gate is
\[
\boxed{
F_A^{\rm long,local}=0
\quad\Longleftrightarrow\quad
\sigma_{\rm species}^{\rm long,local}=0,
}
\]
unless the term is already included in \(\rho_E\), or is gauge, constrained,
boundary, internal, short-range/confined or high-energy suppressed.  In the
common pressure projection language,
\[
\boxed{
\Pi_{\rm grav}(T_A)=q_A\mathcal G,
\qquad
q_A=\eta E_A/c^2
}
\]
must hold at monopole order; species-specific tail data may survive only as
spin, charge, color, phase, multipole, internal, short-range or suppressed
structure.

The allowed classifications are not labels of convenience.  A candidate
\(\sigma_{\rm species}\) term must either be part of the physical Hamiltonian
density \(\rho_E\), vanish on reduced observables, be a boundary/multipole
term rather than a local monopole source, be internal/confined with no
external \(u\)-tail, be short-range, or be suppressed below the relevant
equivalence-principle, fifth-force, Solar-System or binary-pulsar gate.  If no
such test is supplied, the term remains a Root~C anomaly.
\end{openthm}

\begin{openthm}[Root C matter-family and sensitivity audit]
Root C must be checked for pointlike massive matter, photons and fields, bound
systems, internal/confined sectors, composite macroscopic bodies,
self-gravitating bodies and compact objects.  Photons and other traceless
fields source \(u\) through \(T_{\mu\nu}n^\mu n^\nu\), not through a trace
coupling.  Bound, internal, confined and field energy must enter
\(\mathcal H_{\rm m}\) and hence \(\rho_E\), while not creating a private
external cone or private \(u\)-source.

For compact binaries define the long-range source charge per unit total
energy
\[
\boxed{
\alpha_A=\frac{q_A}{E_A/c^2}.
}
\]
The Root~C target is \(\alpha_A=\alpha_B\) for all stable compact sources, or
else \(\alpha_A-\alpha_B\) must be gauge, constrained, short-range, gapped,
damped or below binary-pulsar and waveform bounds.  Otherwise the binary loses
energy through a leading scalar dipole or monopole channel rather than through
the protected tensor emission channel.
\end{openthm}

\medskip\noindent\textbf{Remark (Root C closure status under ORC).}
Conditional on the Operational Relational Covariance postulate, the Root~C
open theorems close as derived Results once Root~A and Root~B are in place.
The argument is:
(i)~Root~A under ORC yields a single record metric $g_{\rm rec}^{\mu\nu}$;
Root~B under ORC yields $S=S_{\rm EH}[g_{\rm rec}]+S_{\rm matter}[g_{\rm rec},\Psi]$;
(ii)~the variational stress-energy tensor
$T^{\mu\nu}=(2/\sqrt{-g})\,\delta S_{\rm matter}/\delta g_{\mu\nu}$ is the
unique gravitational source compatible with the diffeomorphism invariance of
$S_{\rm matter}$ and contains rest, kinetic, binding, internal/confined and
field energies of every stable matter species;
(iii)~ORC's ``one operational geometry for every observer'' clause forbids
species-private cones and species-private $u$-coupling, so $C5$
($\sigma_{\rm species}^{\rm long,local}=0$) is automatic;
(iv)~$\nabla^2u=-\kappa\rho_E$ and $Q^{(0)}=\eta E_{\rm total}/c^2$ then
follow from the linearized Einstein equation in the weak static limit.
Residual quantitative items --- the numerical value of $G$, the smallness of
$\Lambda$, and Standard-Model species-by-species verification of the
universal coupling --- remain open as part of the broader G-closure and
matter-emergence programs.

\begin{openthm}[Local gravity core closure]
If Root A, Root B and Root C are derived from \(\mathcal M\), then the local
RMG gravity core is closed:
\[
\boxed{
\text{one operational spacetime}
\quad+\quad
S_{\rm EH}+S_{\rm matter}
\quad+\quad
\text{universal total-energy source}.
}
\]
The remaining sectors -- strong-field compact interiors, MOND reentry,
cosmology and detailed matter emergence -- then become extension windows, not
loose foundations of the local gravitational core.
\end{openthm}

\medskip\noindent\textbf{Remark (full gravity-core closure under ORC).}
Conditional on the Operational Relational Covariance postulate, the three
roots close as derived Results (remarks above on Root~A, Root~B, Root~C),
and therefore so does this ``Local gravity core closure'' openthm.  The
unified statement is: under ORC plus the minimal RMG structural
commitments (one base medium $\mathcal M$ with stable phase $\mathcal M_0$;
locality and two-derivative low-energy hierarchy), the low-energy
operational sector is exactly
\[
\boxed{
S_{\rm low}
=
\frac{c^4}{16\pi G}\!\int d^4x\,\sqrt{-g}\,(R-2\Lambda)
+
S_{\rm matter}[g,\Psi]
+
\text{suppressed corrections},
}
\]
with one record cone, two TT graviton modes at $c_{\rm GW}=c$, no scalar
or vector gravity waves, no preferred frame, universal free fall, and
gravitational source equal to the total physical energy-momentum.  This is
the tested local GR core.
\smallskip

\noindent\textbf{Status note.}
The ORC postulate is the single controlling axiom of this closure; its
microscopic derivation from a deeper relational substrate remains the
central long-term research program.  Candidate routes include emergent
gauge redundancy from a UV principle, ORC as an IR fixed point of a
nonlinear relational substrate, and consistency arguments showing that
ORC-violating theories produce internal pathologies.  No such derivation
has yet been completed.  Until then, the gravitational core is closed
conditional on ORC.

\begin{openthm}[Non-circular local-core proof order]
The local gravity proof must close in the following order:
\[
\boxed{
\mathcal M_{\rm stable}
\Rightarrow
\text{Root A}
\Rightarrow
g_{\rm rec}
\Rightarrow
\text{Root B}
\Rightarrow
S_{\rm EH}+S_{\rm matter}
\Rightarrow
\text{Root C}
\Rightarrow
S_u=\rho_E.
}
\]
Equivalently:
\[
\boxed{
\begin{array}{rcl}
\text{Root A} &:& \text{one common external record carrier/metric},\\
\text{Root B} &:& \text{record re-embedding gauge }\Rightarrow\text{ ADM/EH},\\
\text{Root C} &:& \text{metric/lapse matter coupling }\Rightarrow\text{ total-energy source}.
\end{array}
}
\]
The proof must obey the no-circularity rules:
\[
\boxed{
\begin{array}{l}
\text{do not assume ADM to prove Root A},\\
\text{do not assume Einstein equations to prove Root B},\\
\text{do not assume the equivalence principle to prove Root C},\\
\text{do not use MOND/cosmology to compensate for a local-core failure}.
\end{array}
}
\]
The microscopic object still needed from the base medium is an effective record
action
\[
\boxed{
S_{\rm eff}^{\rm rec}[\mathcal M_0]
}
\]
with one record metric, no local background leak, a covariant presymplectic
form, universal matter metric coupling and no explicit species-private
\(u\)-coupling.  This is the next exact derivation target.
\end{openthm}

\section{Leading-Order Relational Base Derivation}

The current internal result is a leading-order derivation of the tested local
gravity core from a relational base-medium action candidate.  It is not the
final microscopic theory: the exact coefficients, matter spectrum, MOND scale
and strong-field branch remain to be computed.

\begin{result}[Relational base action candidate]
The base-medium is represented without external rods, clocks or background
space by relational variables
\[
\boxed{
R_{ab},\qquad \vartheta_{ab},\qquad K_{ab}.
}
\]
Here \(R_{ab}\) denotes adjacency/separation records, \(\vartheta_{ab}\) the
common rhythm/phase records, and \(K_{ab}\) internal coupling/shape records.
The candidate base action has Jacobi/constraint form
\[
\boxed{
S_{\mathcal M}^{\rm rel}
=
\int d\lambda\,
\left[
\Theta_{\mathcal M}
(R,\vartheta,K;\dot R,\dot\vartheta,\dot K)
-
\alpha C_0
-
\beta^a C_a
-
\gamma C_\nu
\right].
}
\]
\(\lambda\) is only a bookkeeping parameter, not physical time.  The
constraints have the intended meanings
\[
\boxed{
C_0=\text{common process constraint},\qquad
C_a=\text{relabel/re-embedding constraint},\qquad
C_\nu=\text{one-rhythm/harmonic selection constraint}.
}
\]
Thus the base does not begin with spacetime.  Spacetime variables are allowed
only as coarse-grained record variables.
\end{result}

\begin{result}[Continuum core route]
Under smooth coarse-graining, local isotropy, one common record carrier and no
physical background-label leak, the continuum record variables are
\[
\boxed{
h_{ij}=h_{ij}[R,\vartheta,K],
\qquad
\pi^{ij}=\pi^{ij}[R,\vartheta,K].
}
\]
Relabel/re-embedding redundancy gives the momentum constraint:
\[
\boxed{
C_a\ \longrightarrow\ H_i.
}
\]
Small relational loop defects give curvature:
\[
\boxed{
\Omega_{\rm loop}-1\ \longrightarrow\ R^i{}_{jkl}(h),
\qquad
V_{\rm loop}\ \longrightarrow\ \int d^3x\,\sqrt h\,(R(h)-2\Lambda).
}
\]
The relational kinetic term decomposes into physical shear plus constrained
volume/gauge directions.  In the protected continuum limit this gives the
DeWitt kinetic form
\[
\boxed{
T_{\rm rec}
\longrightarrow
\frac{1}{\sqrt h}
\left(
\pi_{ij}\pi^{ij}
-\frac12\pi^2
\right).
}
\]
Therefore the leading-order local record Hamiltonian is
\[
\boxed{
H
=
\frac{1}{\sqrt h}
\left(
\pi_{ij}\pi^{ij}
-\frac12\pi^2
\right)
-\sqrt h\,(R(h)-2\Lambda)
+H_{\rm matter}.
}
\]
Together with \(H_i\), this is the ADM/Einstein-Hilbert local fixed point at
leading order.
\end{result}

\begin{result}[Radiation and source consequence]
The metric perturbation splits operationally as
\[
\boxed{
\delta h_{ij}
=
s_{ij}
+\frac13 h_{ij}b
+\text{gauge/constraint pieces}.
}
\]
The common breathing variable \(b\) is not a separate free radiation field in
the tested local sector; it is hidden by common-mode self-calibration and by
the Hamiltonian constraint.  The observable radiation is the shear sector
\[
\boxed{
s_{ij}=h_{ij}^{TT},
}
\]
which carries two tensor modes.  Matter couples to the same record metric, so
the local source is total energy rather than a species-private tail charge.
\end{result}

\begin{gate}[Leading-order status]
RMG currently has a conditional leading-order route
\[
\boxed{
\mathcal M_{\rm rel}
\Rightarrow
\text{record geometry}
\Rightarrow
\text{ADM/EH local core}
\Rightarrow
\text{two tensor GW modes}
\Rightarrow
\text{total-energy source}.
}
\]
This is the present leading-order core result.  It becomes a full derivation
only after the exact coarse-graining map, the constants \(G,\Lambda\), the
stable spectrum, the matter couplings, the MOND reentry scale and the
strong-field branch are computed from the base-medium rather than inserted as
targets.
\end{gate}

\begin{openthm}[\(S_{\rm eff}^{\rm rec}\) derivation pipeline]
Start from a base-medium action/evolution functional
\[
\boxed{
S_{\mathcal M}[\Phi_{\mathcal M}],
\qquad
\Phi_{\mathcal M}=\Phi_0+\delta\Phi,
}
\]
around a stable phase \(\mathcal M_0\).  Linearization gives
\[
\boxed{
\delta^2S_{\mathcal M}
=
\frac12
\langle\delta\Phi,\mathcal L_{\mathcal M_0}\delta\Phi\rangle.
}
\]
The fluctuation space must split into record and internal sectors:
\[
\boxed{
\delta\Phi=R+I,
\qquad
R=\Pi_{\mathcal R}\delta\Phi,
\qquad
I=\Pi_{\mathcal I}\delta\Phi,
}
\]
with
\[
\boxed{
\Pi_{\mathcal R}+\Pi_{\mathcal I}=1,
\qquad
\Pi_{\mathcal R}\Pi_{\mathcal I}=0.
}
\]
The record effective action is then the low-energy functional
\[
\boxed{
e^{iS_{\rm eff}^{\rm rec}[R]}
=
\int{\cal D}I\,e^{iS_{\mathcal M}[R,I]}.
}
\]
Equivalently,
\[
\boxed{
S_{\rm eff}^{\rm rec}[R]
=
-i\log\int{\cal D}I\,e^{iS_{\mathcal M}[R,I]}.
}
\]
The record-sector principal kernel must factorize:
\[
\boxed{
S_{\mathcal R}^{(2)}
\sim
\int R_A\,Q_{AB}^{\mu\nu}\partial_\mu\partial_\nu R_B,
\qquad
Q_{AB}^{\mu\nu}=G^{\mu\nu}K_{AB},
}
\]
so that one record metric is defined by \(g_{\rm rec}^{\mu\nu}\sim
G^{\mu\nu}\).  The internal integration may renormalize constants, add binding
energy, multipoles, finite-size terms, high-energy suppressed operators or
large-scale-only corrections.  It must not leave local unsuppressed
\[
\boxed{
U^\mu_{\mathcal M},\quad
v^\mu v^\nu,\quad
\text{species-private metric},\quad
\text{explicit species-private }u\text{-coupling},\quad
\dot N^2,\quad
\dot N_i\dot N^i,\quad
\text{extra record cone}.
}
\]
If this pipeline succeeds, the same microscopic object supplies the raw
material for Root A, Root B and Root C.  If it fails, the failure is localized
as either non-factorization, background leak, or source anomaly.
\end{openthm}

\begin{openthm}[Minimal base-medium input package]
To compute \(S_{\rm eff}^{\rm rec}\), the base-medium model must provide the
input package
\[
\boxed{
\mathcal D_{\mathcal M}
=
\left(
\Phi_{\mathcal M},
S_{\mathcal M},
\mathcal M_0,
\ell_*,
\Omega_{\mathcal M},
\mathcal G_{\rm int},
\mathcal O_{\rm rec}
\right).
}
\]
Here \(\Phi_{\mathcal M}\) denotes the microscopic/collective medium variables,
\(S_{\mathcal M}\) the action or evolution functional, \(\mathcal M_0\) the
stable low-energy phase, \(\ell_*\) the minimal operational node scale,
\(\Omega_{\mathcal M}\) the symplectic or
presymplectic structure, \(\mathcal G_{\rm int}\) the internal redundancy
group, and \(\mathcal O_{\rm rec}\) the operational record observables.  Around
\(\mathcal M_0\),
\[
\boxed{
\Phi_{\mathcal M}=\Phi_0+\delta\Phi,
\qquad
\delta^2S_{\mathcal M}
=
\frac12
\langle\delta\Phi,\mathcal L_{\mathcal M_0}\delta\Phi\rangle.
}
\]
The spectral target is
\[
\boxed{
\mathrm{Spec}(\mathcal L_{\mathcal M_0})
=
\mathcal B_{\rm rec}\cup\mathcal B_{\rm int},
\qquad
N_{\rm cone}(\mathcal B_{\rm rec}^{\rm gapless})=1
}
\]
as one external record carrier/cone, not one scalar field.  The projectors must be built from
observability and spectrum, not by assuming GR:
\[
\boxed{
\Pi_{\mathcal R}+\Pi_{\mathcal I}=1,
\qquad
\Pi_{\mathcal R}\Pi_{\mathcal I}=0.
}
\]
At quadratic order, internal integration has the block form
\[
\boxed{
S^{(2)}
=
\frac12
\begin{pmatrix}R&I\end{pmatrix}
\begin{pmatrix}
L_{RR} & L_{RI}\\
L_{IR} & L_{II}
\end{pmatrix}
\begin{pmatrix}R\\I\end{pmatrix},
\qquad
L_{\rm eff}=L_{RR}-L_{RI}L_{II}^{-1}L_{IR}.
}
\]
The Schur-complement correction may renormalize the record kernel, binding
energy, multipoles or suppressed operators.  It must not generate an extra
cone, local preferred-frame tensor, species-private metric, explicit
species-private \(u\)-coupling or lapse/shift kinetic terms.  Without this
input package, RMG-1 has a well-defined proof target; the microscopic
derivation remains open.

\medskip\noindent\textbf{Status of the package.}
At this stage \(\mathcal D_{\mathcal M}\) is a required framework, not yet a
completed microscopic specification.  Before Root~A can be calculated, it must
be promoted to at least one explicit ansatz whose quadratic operator,
projectors and record/internal split can actually be computed.

\medskip\noindent\textbf{Additional input requirements.}
The node scale \(\ell_*\) must be assigned a clear status.  In a first toy
model it may be treated as a primitive coarse-graining input; in the final
theory it should be derived or calibrated from \(S_{\mathcal M}\) and stable
record dynamics.  The form \(\Omega_{\mathcal M}\) must identify physical,
gauge, constrained and presymplectic-null directions; the spectrum of
\(\mathcal L_{\mathcal M_0}\) alone is not enough, because a Hessian mode may
be gauge or constrained rather than physical.

The record-internal coupling \(L_{RI}\) is a central physics input.  Its
symmetry, sign, scale and derivative order must be specified.  The correction
\(-L_{RI}L_{II}^{-1}L_{IR}\) may renormalize record dynamics, but it must not
change the principal cone, create a long-range private source, or flip the
sign of a physical kinetic term.
\end{openthm}

\begin{openthm}[Candidate base-medium order-parameter ansatz]
A first calculable microscopic target is a single base-medium order parameter,
not many independent fundamental substances:
\[
\boxed{
\Phi_{\mathcal M}
=
\Psi_{\mathcal M}
=
\sqrt n\,e^{i\theta}\chi.
}
\]
Here \(n\) is local node-density/pressure amplitude, \(\theta\) is phase or
medium rhythm, and \(\chi\) denotes internal shape/orientation data of the same
medium.  This ansatz is allowed only if \(\chi\) does not become an independent
long-range spacetime carrier.  It must be internal, confined, gauge,
constrained, suppressed, or part of record matter sharing the same carrier.
Before Root~A is calculated, \(\chi\) must be assigned a definite mathematical
type: scalar, vector, tensor, group-valued field, constrained composite
variable, or topological label.  Merely saying ``internal shape'' is not yet a
microscopic specification.

A minimal positive-energy candidate functional is
\[
\boxed{
E_{\mathcal M}
=
\int d^3X
\left[
\frac{\kappa_n}{2}(\nabla n)^2
+\frac{\kappa_\theta}{2}n(\nabla\theta)^2
+V(n)
+E_{\rm int}(n,\theta,\chi,\nabla\chi)
\right],
}
\]
with a stable phase
\[
\boxed{
V'(n_0)=0,
\qquad
V''(n_0)>0.
}
\]
Schematically,
\[
\boxed{
S_{\mathcal M}
=
\int d\tau
\left[
\Theta_{\mathcal M}(\Phi_{\mathcal M},\dot\Phi_{\mathcal M})
-E_{\mathcal M}[\Phi_{\mathcal M}]
\right],
}
\]
where \(\Theta_{\mathcal M}\) defines the symplectic structure.  The stable
phase expansion is
\[
\boxed{
n=n_0+\delta n,
\qquad
\theta=\nu_0\tau+\delta\theta,
\qquad
\chi=\chi_0+\delta\chi.
}
\]
The Root A spectral target is still
\[
\boxed{
N_{\rm cone}(\mathcal B_{\rm rec}^{\rm gapless})=1.
}
\]
This means one observable external carrier/cone, not one scalar excitation.
The bookkeeping parameter \(\tau\), any preferred flow, and any extra observable
carrier branch must not survive as local observables in
\(S_{\rm eff}^{\rm rec}\).
The background form \(\theta=\nu_0\tau+\delta\theta\) is allowed only if
\(\tau\) is protected from becoming an observable absolute time.  The required
condition is
\[
\boxed{
\delta_\tau{\cal O}_{\rm rec}\approx0
}
\]
for all physical record observables, up to gauge or constraint terms.  The
allowed routes are a parametrized action with a reparametrization constraint, a
relational clock gauge fixing, or a presymplectic null direction generated by
changes of the time label.

The local Lorentz behavior of the record sector must also be explicit.  Either
it emerges from factorization,
\[
\boxed{
Q_{AB}^{\mu\nu}=G^{\mu\nu}K_{AB},
}
\]
with one common principal cone, or a covariant low-energy record action is
stated as a target fixed point.  It is not enough to assume observable Lorentz
invariance while leaving a physical medium time, medium velocity or
species-private cone in the local record sector.
The long compression variable \(u\) should arise from a pressure/density
deficit, for example \(u\sim-\delta\ln n\), and its weak static equation must be
constraint-like:
\[
\boxed{
\nabla^2u=-\kappa\rho_E+\text{allowed corrections},
}
\]
so that \(u\) remains a constraint variable rather than an independent
propagating scalar mode.  This ansatz is a test model, not a proof:
if it leaves a physical medium time, preferred frame or extra cone, it must be
constrained or replaced.
\end{openthm}

\begin{openthm}[Working toy instantiation of the input package]
The first calculable test bench is allowed to be wrong; its purpose is to make
\(\mathcal D_{\mathcal M}\) concrete enough that the Root~A spectrum and the
spin-2 gate can be tested.  Take
\[
\boxed{
\Phi_{\mathcal M}^{\rm toy}
=
(\phi,\theta,Q_{ij}),
\qquad
\phi=\sqrt n,
}
\]
where \(Q_{ij}\) is a real symmetric traceless internal strain tensor,
\[
\boxed{
Q_{ij}=Q_{ji},
\qquad
Q^i{}_i=0.
}
\]
The toy stable phase is isotropic:
\[
\boxed{
\phi=v,\qquad
\theta=\nu_0\tau,\qquad
Q_{ij}=0.
}
\]
Tensor information, if any, must therefore come from fluctuations of
\(Q_{ij}\) and their record-visible transverse-traceless projection, not from a
preferred background direction.

A minimal schematic toy energy is
\[
\boxed{
E_{\rm toy}
=
\int d^3X
\left[
\frac{k_\phi}{2}(\nabla\phi)^2
+\frac{k_\theta}{2}\phi^2(\nabla\theta)^2
+\frac{\lambda_\phi}{4}(\phi^2-v^2)^2
+\frac{k_Q}{2}(\nabla Q_{ij})^2
+\frac{\mu_Q^2}{2}Q_{ij}Q_{ij}
+\frac{g_Q}{2}\phi^2Q_{ij}Q_{ij}
+E_{\rm mix}
\right].
}
\]
The action must be parametrized or presymplectic, for example
\[
\boxed{
S_{\rm toy}
=
\int d\tau
\left[
\Theta_{\rm toy}
-\alpha C_0
-\beta^a C_a
-E_{\rm toy}
\right],
}
\]
so that the bookkeeping label \(\tau\) and relabeling variables do not become
physical background structures.

Expanding
\[
\boxed{
\phi=v+\eta,
\qquad
\theta=\nu_0\tau+\vartheta,
\qquad
Q_{ij}=q_{ij},
}
\]
the quadratic target has the schematic form
\[
\boxed{
\delta^2E_{\rm toy}
=
\frac12\eta(-k_\phi\nabla^2+m_\eta^2)\eta
+\frac12\vartheta(-k_\theta v^2\nabla^2)\vartheta
+\frac12q_{ij}(-k_Q\nabla^2+m_Q^2)q_{ij}
+L_{\rm mix}^{(2)},
}
\]
with
\[
\boxed{
m_\eta^2=2\lambda_\phi v^2,
\qquad
m_Q^2=\mu_Q^2+g_Qv^2.
}
\]
The conservative spectrum reading is: \(\eta\) is gapped if \(m_\eta^2>0\);
\(\vartheta\) is the candidate common phase/cone branch but not the graviton;
\(q_{ij}\) is gapped if \(m_Q^2>0\); and \(L_{\rm mix}^{(2)}\) must not change
the principal cone.  For the conservative Root~A toy, \(m_Q^2>0\) is required
unless the \(Q\)-sector is explicitly being tested as the shared-cone TT
candidate.  An unintended massless \(Q_{ij}\) branch carries five
symmetric-traceless components and can create extra long-range branches,
threatening \(N_{\rm cone}=1\).  This version may help Root~A while still
failing to produce tensor gravity.

Allowed toy mixing terms must be derivative-order and symmetry controlled, for
example
\[
\boxed{
a_1\eta(\nabla\vartheta)^2,\quad
a_2\eta Q_{ij}Q_{ij},\quad
a_3Q_{ij}\partial_i\vartheta\,\partial_j\vartheta,\quad
a_4\eta\,\partial_kQ_{ij}\partial_kQ_{ij}.
}
\]
They are allowed only if, after expansion and internal integration, they do
not split the record cone, introduce a preferred tensor or create a long-range
species-private source.

To test the tensor gate, decompose
\[
\boxed{
q_{ij}
=
q_{ij}^{TT}
+\partial_iV_j+\partial_jV_i
+\left(\partial_i\partial_j-\frac13\delta_{ij}\nabla^2\right)B
+\frac13\delta_{ij}H.
}
\]
The tensor candidate passes only if
\[
\boxed{
\operatorname{rank}K_{Q{\rm -strain},TT}=2,
\qquad
\text{positive kinetic sign},
\qquad
c_{TT}=c_{\rm rec},
}
\]
and all scalar/vector pieces are gauge, constrained, near-zone or suppressed.
Rank zero means no tensor gravity; rank greater than two means extra local
shear/scalar/vector radiation; a wrong sign, mass term or different cone also
fails.  Thus the toy model localizes failure instead of hiding it: extra cones,
ghosts, preferred frames or species-private sources identify which part of
\(\mathcal D_{\mathcal M}\) must be changed.
\end{openthm}

\begin{result}[Stable-record coherent amplitude completion]
The ansatz \(\Phi_{\mathcal M}=\sqrt n e^{i\theta}\chi\) by itself is not yet
a microscopic proof of the square law.  The square law is derived if the
projected long-range stable record sector has the coherent endpoint-counting
completion
\[
\boxed{
\rho:=|\Phi_{\mathcal M}|=\sqrt n
}
\]
and the first-order record action
\[
\boxed{
S_{\rm rec}
=
\int d\lambda\,d^3q
\left[
P_A\dot R_A
-\alpha\,{\cal C}_{\rm rec}
\right],
}
\]
with
\[
\boxed{
{\cal C}_{\rm rec}
=
\frac12
\left[
K^{AB}P_AP_B
+K_{AB}
\left(
c^2\rho^2\nabla_{\rm rec}R_A\nabla_{\rm rec}R_B
+m_A^2\rho R_AR_B
\right)
\right].
}
\]
The multiplier \(\alpha\) is the common process multiplier, not an external
time.  In local common-rhythm proper gauge, \(d\tau=\alpha d\lambda\), the
momenta eliminate to
\[
\boxed{
S_{\rm rec}^{(2)}
=
\frac12
\int d\tau\,d^3q\,
K_{AB}
\left[
\partial_\tau R_A\partial_\tau R_B
-c^2\rho^2\nabla_{\rm rec}R_A\nabla_{\rm rec}R_B
-m_A^2\rho R_AR_B
\right].
}
\]
Therefore
\[
\boxed{
C_t=1,\qquad C_0=\rho,\qquad C_x=\rho^2.
}
\]
The local dispersion relation is
\[
\boxed{
\omega^2=c^2\rho^2k^2+m_A^2\rho.
}
\]
Consequently, with \(s=\rho^{1/2}=n^{1/4}\),
\[
\boxed{
\omega_0\sim s,\qquad
\ell_{\rm coord}\sim s,\qquad
c_{\rm coord}=cs^2.
}
\]
This is the stable-record completion underlying the reciprocal clock/rod
response.  The order parameter alone does not yield the result; the
endpoint-counting record completion is the additional calculable structure.
\end{result}

\begin{result}[Endpoint-counting exponent selection]
The powers in the coherent completion have a relational node-link origin.  A
local gap term has one active endpoint; hence its leading coherent amplitude
weight is
\[
\boxed{
L_a\propto \rho_a.
}
\]
A spatial link has two active endpoints and must vanish if either endpoint is
inactive.  Endpoint symmetry and minimal coherent order then give
\[
\boxed{
J_{ab}\propto \rho_a\rho_b.
}
\]
In a smooth patch, \(\rho_a,\rho_b\to\rho\), so the stable record sector has
\[
\boxed{
\text{gap}\sim\rho,\qquad \text{link stiffness}\sim\rho^2.
}
\]
This selects \(C_t:C_0:C_x=1:\rho:\rho^2\), and therefore the square law
\(c_{\rm coord}/c=s^2\) with \(s=\rho^{1/2}\).
\end{result}

\begin{openthm}[Candidate stable-phase spectrum audit]
For the candidate
\[
\boxed{
\Phi_{\mathcal M}=\sqrt n\,e^{i\theta}\chi,
}
\]
write
\[
\boxed{
n=n_0+\eta,
\qquad
\theta=\nu_0\tau+\vartheta,
\qquad
\chi=\chi_0+\xi.
}
\]
The quadratic spectrum should split schematically as
\[
\boxed{
\delta^2S_{\mathcal M}
=
S_{\eta\eta}^{(2)}
+S_{\vartheta\vartheta}^{(2)}
+S_{\xi\xi}^{(2)}
+S_{\rm mix}^{(2)}.
}
\]
The desired Root A pattern is:
\[
\boxed{
\begin{array}{c|c}
\text{sector} & \text{required low-energy status}\\
\hline
\eta & \text{gapped, constrained, near-zone or suppressed}\\
\vartheta & \text{common rhythm/cone variable, not by itself the graviton}\\
\xi & \text{internal/confined/gauge/gapped/suppressed or shared-cone shear/matter}\\
S_{\rm mix} & \text{renormalization only; no extra cone}
\end{array}
}
\]
The phase branch may have
\[
\boxed{
\omega^2=c_{\rm rec}^2k^2+O(k^4),
\qquad
G^{\mu\nu}k_\mu k_\nu=0,
}
\]
and can serve as the candidate common record cone.  But this is only Root A
input.  It does not by itself prove gravitational dynamics:
\[
\boxed{
\text{one cone}\neq\text{two tensor gravitons}.
}
\]
Identifying the phase/acoustic branch with the graviton would reduce the
framework to scalar acoustic gravity, which is excluded by the
two-tensor-mode requirement.  The local-GR route is
\[
\boxed{
\text{one common record cone}
\xrightarrow{\text{Root B}}
\text{ADM constraints}
\xrightarrow{\text{two-derivative fixed point}}
S_{\rm EH}
\Rightarrow
\text{two tensor gravitational modes}.
}
\]
This candidate passes the first spectrum check only if the amplitude mode is
not a long-range scalar gravity mode, internal modes do not generate extra
cones, the acoustic cone is universal for record matter, and the tensor sector
is derived by Root B rather than assumed.
\end{openthm}

\begin{openthm}[Composite record metric and spin-2 emergence gate]
The record metric must be a composite response of the base medium:
\[
\boxed{
g_{\rm rec}^{\mu\nu}
=
{\cal G}^{\mu\nu}[\Phi_{\mathcal M}],
\qquad
g_{\rm rec}^{\mu\nu}
=
\bar g^{\mu\nu}+\delta g_{\rm rec}^{\mu\nu}.
}
\]
The phase/acoustic branch may define the common cone, but the gravitational
field is the low-energy dynamics of the composite record geometry, not simply
the scalar phase \(\vartheta\):
\[
\boxed{
\delta g_{\rm rec}^{\mu\nu}
=
\frac{\delta{\cal G}^{\mu\nu}}{\delta\Phi_{\mathcal M}}
\delta\Phi_{\mathcal M}
+\cdots.
}
\]
In spatial language,
\[
\boxed{
\delta h_{ij}
=
h_{ij}^{TT}
+\partial_iV_j+\partial_jV_i
+\left(\partial_i\partial_j-\frac13\delta_{ij}\nabla^2\right)B
+\frac13\delta_{ij}H.
}
\]
Root B must make the scalar and vector pieces constraint, gauge or near-zone,
leaving only
\[
\boxed{
h_{ij}^{TT}
}
\]
as propagating vacuum gravitational data:
\[
\boxed{
\#({\rm propagating\ vacuum\ gravitational\ DOF})=2.
}
\]
Possible microscopic sources of this tensor/shear sector are anisotropic
stress/strain of \(\chi\), collective deformation of the record propagation
kernel \(G^{\mu\nu}\), topological/oscillon-network elasticity, or emergent
embedding geometry after quotienting record histories.  None may create a
second long-range matter cone or private spacetime.  Thus:
\[
\boxed{
\text{acoustic cone = kinematics},
\qquad
\text{ADM closure = gravitational dynamics}.
}
\]
If the candidate supplies only a scalar acoustic mode, it fails the spin-2
gate.
\end{openthm}

\begin{gate}[Tensor-closure reduction]
The spin-2 issue is not closed in RMG-1.  It is reduced to a finite
calculation on the candidate record action.  The candidate may pass only if the
record-visible \(\chi\)-strain has a transverse-traceless projection and its
quadratic kinetic matrix has exactly two positive, luminal, common-cone
radiative modes.  Equivalently:
\[
\boxed{
\text{candidate tensor closure}
\Longleftrightarrow
\mathrm{rank}\,K_{\chi{\rm -strain}}=2
\text{ on the physical record sector}.
}
\]
Rank zero means no tensor gravity; rank greater than two means extra local
shear/scalar/vector radiation; a wrong sign, mass term, or different cone also
fails.  The detailed proof attempt and projector algebra belong in the work
dossier, not in the core file, until the kinetic-rank test is actually
derived from \(S_{\mathcal M}\) or \(S_{\rm eff}^{\rm rec}\).
\end{gate}

This theorem remains open in RMG-1.

\section{Allowed Deviation Windows}

Additional sectors may be introduced only if they do not modify the local GR core.

Allowed windows:
\[
\text{MOND-like low-acceleration response},
\]
\[
\text{cosmological background state of the medium},
\]
\[
\text{matter-sector emergence},
\]
\[
\text{high-energy corrections}.
\]

Rule:
\[
\boxed{
\text{deviations must not violate the local universal metric, }\gamma=\beta=1,
\text{ or }c_{\rm GW}=c.
}
\]

They also must not introduce an independent local $u^\mu_{\mathcal M}$ into the
tested core.

\begin{gate}[Flow/vorticity split]
Medium flow and vorticity are allowed only in two protected roles.
In the tested local regime they must be hidden inside ADM constraint variables
or near-zone fields:
\[
\boxed{
\mathcal A_i,\ u
\quad\text{constraint/near-zone only, not independent vacuum radiation.}
}
\]
In large-scale or low-acceleration regimes they may re-enter as an effective
medium correction:
\[
\boxed{
\mathbf a
=
\mathbf a_{\rm GR}
+
\mathbf a_{\rm med}.
}
\]
The correction must satisfy:
\[
\boxed{
\mathbf a_{\rm med}\to0
\quad
\text{in local tested high-acceleration regimes},
}
\]
while it may become nonzero in galactic low-acceleration or cosmological
background regimes.
\end{gate}

\begin{gate}[Local flow decoupling]
In the tested local core, medium flow and vorticity may enter only as
constraint, shift, near-zone or lower-order transport data.  They must not
modify the physical record-sector principal tensor:
\[
\boxed{
\Delta P_A^{\rm local}(k)
\not\supset
\alpha_A v^\mu v^\nu k_\mu k_\nu
\quad
\text{for external record-forming }A.
}
\]
Equivalently, the preferred-frame PPN target is:
\[
\boxed{
\alpha_1=\alpha_2=\alpha_3=0
\quad
\text{at the GR/local fixed point}.
}
\]
Large-scale medium corrections are allowed only through a reentry function
\(\mathcal F\) satisfying:
\[
\boxed{
\mathcal F\to0
\quad
\text{in lab, Solar-System, binary-pulsar and local GW regimes}.
}
\]
This gate is open from \(\mathcal M\); if it fails, flow/vorticity becomes an
observable preferred frame.
\end{gate}

\begin{openthm}[MOND-safe reentry]
Derive a scale or acceleration gate:
\[
\boxed{
\mathbf a_{\rm med}
=
\mathcal F\!\left(
\frac{|\nabla\Phi|}{a_0},
\frac{L}{L_{\rm bg}},
\omega_{\rm flow}
\right)\mathbf a_{\rm flow},
}
\]
with:
\[
\boxed{
\mathcal F\to0
\quad\text{in local tested regimes},
\qquad
\mathcal F\neq0
\quad\text{in galactic low-acceleration regimes}.
}
\]
The exact function \(\mathcal F\) is not fixed in RMG-1.
\end{openthm}

\section{Strong-Field Compact-Object Contract}

RMG-1 does not yet select the strong-field compact-object branch.  The branch
must be derived from the base-medium equilibrium law and the record/internal
projection.

The currently allowed branches are:
\[
\boxed{
\text{GR/Kerr-like exterior},
\qquad
\text{horizonless exponential branch},
\qquad
\text{hybrid branch},
\qquad
\text{alternative branch}.
}
\]

Candidate strong-field signatures --- horizonless static exteriors,
exponential freezing, curvature-soft deep boundaries, shifted ISCOs and
breathing-mode tests --- are listed as targets, not predictions.  They become
RMG-1 predictions only if they are derived from \(\mathcal M\).

\begin{gate}[Strong-field compact projection]
For compact objects, the internal sector may dominate bound medium structure
and contribute to total energy:
\[
\boxed{
E_{\rm total}=E_{\mathcal R}+E_{\mathcal I}+E_{\rm bind}.
}
\]
However, the external observable cone must be defined by the record projection:
\[
\boxed{
\Pi_{\mathcal R}P_{\rm full}(k)\Pi_{\mathcal R}
=
\left(G^{\mu\nu}k_\mu k_\nu\right)K_{\mathcal R},
}
\]
while internal compact modes may enter the gravitational source only through
conserved total charges such as energy, angular momentum and allowed gauge
charges.  They must not define an independent external spacetime cone.
\end{gate}

\begin{openthm}[Hybrid compact-object program]
Derive, or rule out, the hybrid strong-field branch:
\[
\boxed{
\text{external record geometry is GR/Kerr-like to current precision}
}
\]
while
\[
\boxed{
\text{deep internal base-medium structure may freeze, soften or store energy.}
}
\]
The required proof is:
\[
\boxed{
\Pi_{\mathcal I}\text{ contributes to }H_{\rm phys}
\quad\text{but}\quad
\Pi_{\mathcal I}\text{ does not create a separate external cone.}
}
\]
If this fails, RMG must either reduce to a fully GR-like strong-field branch or
derive and observationally confront an alternative strong-field branch.
\end{openthm}

\section{Statement Status Map}

This section is the integrated WP0 audit map.  It is not a proof.  It records
which statements are postulates, which are conditional bridges, which are open
theorems, and which gates block the first local-gravity MVP.

The sortable audit status vocabulary is restricted to
\[
\boxed{
\text{postulate, conditional result, derived result, target, open theorem, fail-risk}.
}
\]
Free-form context belongs in notes, not in the status label.  Priority is used
only for proof management:
\[
\boxed{
P0=\text{MVP blocker},\quad
P1=\text{first local package},\quad
P2=\text{important later item},\quad
P3=\text{extension window}.
}
\]

\medskip\noindent\textbf{Foundation and local bridge.}
\begin{itemize}
\item \textbf{RMG-001} one base-medium \(\mathcal M\): postulate.
\item \textbf{RMG-002} stable low-energy phase: P0 target.  It is introduced
as a primitive requirement, but the audit treats its physical realization as a
condition to verify before Root A can be calculated.
\item \textbf{RMG-004} Operational Relational Covariance: P0 target.  It must
be derived from microscopic record equivalence or replaced by a more explicit
protection theorem.
\item \textbf{RMG-005} positive physical energy: P0 target.  It is introduced
as a primitive requirement, but physical propagating modes still require a
ghost/gradient-stability audit.
\item \textbf{RMG-007--RMG-014} operational and weak-field bridge: conditional
or derived local results.  The weak bridge gives \(u(r)=G_{\rm RMG}M/(rc^2)\)
and \(\gamma=\beta=1\), but their final status remains conditional on Roots
A--C.
\end{itemize}

\medskip\noindent\textbf{Root blockers.}
\begin{itemize}
\item \textbf{RMG-015} Root A: derive one external record carrier/cone from the
stable phase.  Blocks factorization, no-birefringence, the composite metric,
and the one-cone part of Root B and Root C.
\item \textbf{RMG-016} Root B: derive record-embedding ADM closure, including
\(\Gamma_{\mathcal R}\), \(\Omega_{\mathcal R}\), embedding generators,
ADM brackets, hyperbolicity and no ghost/extra-mode leakage.  Blocks the
ADM/EH fixed point and the two-TT-mode statement.
\item \textbf{RMG-017} Root C: derive the total-energy source projection for
all stable record matter.  Blocks equivalence-principle source universality,
compact-binary source tests and the no-fifth-force gate.
\item \textbf{RMG-018} non-circular proof order: proof discipline target.  Do
not assume ADM to prove Root A, do not assume Einstein equations to prove
Root B, and do not assume the equivalence principle to prove Root C.
\end{itemize}

\medskip\noindent\textbf{Local-core gate clusters.}
\begin{itemize}
\item \textbf{RMG-A-Factorization: RMG-019--RMG-023.}  Prove
\(Q_{AB}^{\mu\nu}=G^{\mu\nu}K_{AB}\), eliminate the failure matrix
\(B_{AB}^{\mu\nu}\), forbid low-energy birefringence, remove extra observable
rank-two tensors, and decouple local flow/vorticity from the tested PPN core.
\item \textbf{RMG-B-ADM: RMG-024--RMG-027.}  Prove
\(\Delta_{\mathcal M}=0\) or suppression, close the hypersurface-deformation
algebra, derive \(\lambda=1/2\), and obtain the ADM/EH fixed point.
\item \textbf{RMG-GW-Tensor: RMG-028--RMG-030.}  Prove exactly two positive,
luminal transverse-traceless tensor modes and a detector-visible TT/shear
response; forbid scalar/vector, massive spin-2, negative-energy or extra shear
radiation in the tested local sector.
\item \textbf{RMG-C-Source: RMG-031--RMG-037.}  Prove
\(q_A\propto E_A/c^2\), the far-tail energy monopole, local
\(\sigma_{\rm grav}\propto\rho_E/c^2\), no species-private source, internal
and binding-energy effacement, no species-private \(u\)-coupling, and
matter-coupled ADM closure.
\item \textbf{RMG-Micro: RMG-038--RMG-041.}  Specify the microscopic input
package, test the candidate order parameter \(\sqrt n e^{i\theta}\chi\),
derive \(S_{\rm eff}^{\rm rec}[\mathcal M_0]\), and construct the composite
record metric \(g_{\rm rec}^{\mu\nu}=\mathcal G^{\mu\nu}[\Phi_{\mathcal M}]\).
\end{itemize}

\medskip\noindent\textbf{Coverage rows added by the audit.}
The following rows are now tracked explicitly, even where the body of RMG-1 has
not yet promoted them to standalone theorems:
\begin{itemize}
\item \textbf{RMG-045} GW emission and radiation reaction: derive tensor
quadrupole energy loss, radiation reaction and compact-binary waveform phase
from the same record metric and total-energy source.  The audit must show
\(G_{\rm Newton}=G_{\rm radiation}=G_{\rm record}\),
\(dE_{\rm orbit}/dt=-P_{TT}\), the angular-momentum flux for eccentric timing
systems, the source-mass identification \(m_A=E_A/c^2\), and no scalar/vector
dipole or monopole channel.  Propagation alone is not enough.
\item \textbf{RMG-046} cosmological background reduction: specify the
large-scale background state, expansion law and any dark-energy-like sector
without using cosmology to cover a local-core failure.
\item \textbf{RMG-047} finite strong-field tests: identify the ISCO, shadow,
ringdown, strong-lensing and late-inspiral observables that must remain
GR/Kerr-like or become explicit falsification targets.
\item \textbf{RMG-048} quantum-extension boundary: state whether quantum
claims are outside the MVP, imported as targets or derived later from the
base-medium record action.
\item \textbf{RMG-049} concrete Solar-System observation map: connect
Newtonian limit, redshift, light bending, Shapiro delay, perihelion/ranging
and frame-dragging tests to explicit RMG gates.
\end{itemize}

\medskip\noindent\textbf{Observational sub-items.}
\begin{itemize}
\item \textbf{OBS-001} Newtonian/orbital limit: checks RMG-012 and
RMG-031--RMG-033.
\item \textbf{OBS-002} gravitational redshift: checks RMG-009, RMG-010 and
RMG-013.  Failure means either species-dependent clock response or a physical
split between the lapse \(N\) and compression \(e^{-u}\).
\item \textbf{OBS-003} light bending and Shapiro delay: checks RMG-008,
RMG-013 and RMG-049.
\item \textbf{OBS-004} perihelion, ranging and PPN \(\beta\): checks RMG-013,
RMG-027 and RMG-049.
\item \textbf{OBS-005} frame dragging and preferred-frame null tests: check
RMG-016, RMG-023, RMG-027 and RMG-049.  This is a conditional-result row:
it is consistent with frame-dragging data if Root~B closes with
\(\Delta_{\mathcal M}=0\) or a suppressed/gauge/constrained remnant.
\item \textbf{OBS-006} binary-pulsar orbital decay: checks RMG-017, RMG-031,
RMG-035 and RMG-045; no leading scalar dipole loss is allowed.  The first
quantitative emission MVP is a PSR~B1913+16-style \(\dot P_b\) comparison,
with tensor damping and no private scalar/vector flux.
\item \textbf{OBS-007} GW propagation: checks RMG-028 and RMG-030; requires
\(c_{\rm GW}=c\) and exactly two tensor polarizations; the multimessenger
benchmark is \(|c_{\rm GW}/c-1|\lesssim10^{-15}\).
\item \textbf{OBS-008} compact-binary waveform phase: checks RMG-028--RMG-030
and RMG-045; propagation and emission must be consistent, with chirp mass built
from \(m_A=E_A/c^2\) and phase evolution from tensor energy balance.
\item \textbf{OBS-009} strong-field finite tests: checks RMG-043, RMG-044 and
RMG-047.
\item \textbf{OBS-010} MOND transition: checks RMG-023 and RMG-042; any
large-scale correction must not leak into the local core.
\item \textbf{OBS-011} cosmological background: checks RMG-046.
\item \textbf{OBS-012} high-energy corrections: any higher-derivative or
  high-energy operator must be below the relevant local bound and may not
  modify local PPN, GW speed or GW emission.
\item \textbf{OBS-013} compact-object branch: finite-size, spin, tidal and
  strong-field internal structure may be tested only after the local core is
  protected; the branch may not create a private cone or scalar/vector loss
  channel.
\item \textbf{OBS-014} quantum-extension boundary: the local-gravity MVP is
  classical.  Quantum extension is future work and may not be used as an
  unsupported premise inside the classical proof.
\end{itemize}

For OBS-004 the chain must be explicit:
\[
\boxed{
\lambda=\frac12
\Rightarrow
\text{first-class ADM closure}
\Rightarrow
\text{EH two-derivative local fixed point}
\Rightarrow
\text{nonlinear weak-field expansion}
\Rightarrow
\beta=1.
}
\]
If any arrow is imported rather than derived or audited, the corresponding
observation row remains conditional.

\medskip\noindent\textbf{Observation matching discipline.}
Every observation row must name its Status Map IDs, proof gate and failure
condition.  The word ``partial'' in the dependency map means background
compatibility, not a completed proof: for example Root~A is partial in the
Newtonian row because one operational metric is needed before a far-tail
source can be read geometrically, while Root~C is only partial in the
Shapiro/light-bending row because source normalization enters the weak-field
potential but the bending coefficient is mainly a Root~A/B gate.  Cosmology
partial rows mean Root~A/B/C must be compatible with an FLRW-like background,
but cosmological dynamics remain an extension window.

\medskip\noindent\textbf{Working observational anchors.}
The following numerical values are working anchors for future calculation
memos, not final data-source citations; each execution artifact must re-check
the exact convention and source before using them:
\begin{itemize}
\item Cassini-style Shapiro: \(\gamma-1=(2.1\pm2.3)\times10^{-5}\).
\item LLR/perihelion-style PPN: \(\beta-1\sim(1.2\pm1.1)\times10^{-4}\) as a
  working anchor.
\item GP-B frame dragging: observed about \(37.2\pm7.2\) mas/yr, with the GR
  target about \(39.2\) mas/yr.
\item GW170817/GRB timing: \(-3\times10^{-15}\lesssim(c_{\rm GW}-c)/c
  \lesssim7\times10^{-16}\) as a working benchmark.
\item PSR~B1913+16-style timing: \(\dot P_b\sim-2.4025\times10^{-12}\,{\rm s/s}\),
  with tensor damping agreement at the sub-percent scale after standard
  corrections.
\item MICROSCOPE-style weak-equivalence anchor:
  \(\eta\sim(-1.5\pm2.3\pm1.5)\times10^{-15}\).
\item LLR Nordtvedt anchor: \(\eta_N\sim(2\pm5)\times10^{-4}\).
\item PSR~J0337+1715-style strong-field free-fall anchor:
  \(|\Delta a/a|<2.6\times10^{-6}\) as a conservative working bound.
\end{itemize}

\medskip\noindent\textbf{MVP observation split.}
The first observation MVP is local and narrow:
\begin{itemize}
\item Newtonian \(1/r\) source with \(M=E_{\rm total}/c^2\);
\item redshift/lapse universality;
\item light bending and Shapiro with one cone and \(\gamma=1\);
\item perihelion/ranging with \(\beta=1\) through EH local closure;
\item no preferred-frame local flow;
\item GW propagation with \(c_{\rm GW}=c\) and two TT modes;
\item binary-pulsar scalar-dipole exclusion from Root~C;
\item binary-pulsar tensor \(\dot P_b\) calculation from the GW-emission gate.
\end{itemize}
Compact-binary high-PN waveform templates, merger/ringdown, strong-field
branch selection, MOND and cosmology are not MVP blockers unless they leak
into these local tests.

\medskip\noindent\textbf{Falsification priority.}
After the local core is protected, RMG must either derive the GR-like local
core from the base medium or produce controlled deviation targets.  Priority
rows are:
\begin{itemize}
\item \textbf{P1} PSR~B1913+16-style timing: tensor \(\dot P_b\) from
  Root~B/C/GW emission without hidden scalar/vector loss.
\item \textbf{P2} high-precision 2PN Shapiro/delay: any calculable suppressed
  post-core correction after Roots~A/B/C close.
\item \textbf{P3} composition tests: \(\eta=0\) from Root~C source
  universality at the \(10^{-15}\) scale.
\item \textbf{P4} GW170817 speed: one record cone gives \(c_{\rm GW}=c\);
  this is not distinctive from GR if the local core passes, but it is a hard
  failure gate.
\item \textbf{P5} neutron-star tidal/compact branch: allowed finite-size
  response must be predicted explicitly and must not hide a local-core
  failure.
\end{itemize}

\medskip\noindent\textbf{Medium-to-target verification interface.}
The microscopic input package must be tested against the low-energy target by
explicit checks: compute \(\mathrm{Spec}(\mathcal L_{\mathcal M_0})\) and count
the record cones; derive \(\Pi_{\mathcal R}\) and \(\Pi_{\mathcal I}\) and
show that internal branches do not become private spacetimes; identify
presymplectic null directions and first-class generators; compute the
Schur-complement correction from internal integration and verify that it does
not alter the local principal tensor or ADM bracket; compute the tensor
kinetic rank, sign, mass and cone; and vary the lapse/metric to check that
rest, kinetic, binding, internal/confined and field energy enter the same
source.  In particular, the chain
\[
\boxed{
\mathcal D_{\mathcal M}\to S_{\rm eff}^{\rm rec}\to
S_{\mathcal R}[g_{\rm rec},\Psi_{\rm rec}]
}
\]
must be explicit enough to run the Root~B presymplectic, bracket and
hyperbolicity audits.

\medskip\noindent\textbf{MVP linearization scope.}
The first local-gravity MVP starts with a Minkowski local core for the
principal-symbol, mode-count and hyperbolicity tests; a weak static local
background is then used for the PPN/nonlinear-coefficient check; FLRW belongs
to the cosmology window.  The first pass requires the quadratic action in
metric perturbations, the linearized constraints and their relevant
quadratic-order closure, leading weak-field matter coupling, exactly two TT
propagating modes, and a leading tensor-emission audit.  Gauge fixing, such as
harmonic gauge, may be chosen only after constraints are derived and may not
hide kinetic lapse/shift or scalar/vector modes.  Higher post-Newtonian terms
and full nonlinear closure remain later targets.

\medskip\noindent\textbf{RMG-distinctive observables.}
The protected local core is deliberately GR-like.  RMG becomes observationally
distinctive only if controlled windows produce falsifiable deviations after
the local core is protected:
\begin{itemize}
\item high-precision delay tests may search for suppressed post-GR corrections
only after Root~A/B/C close;
\item compact-binary waveform residuals, ISCO shifts, shadows and ringdown
features belong to the compact-object branch;
\item galaxy rotation belongs to MOND-like large-scale reentry and must not
leak into Solar-System, binary-pulsar or local GW tests;
\item cosmological acceleration belongs to the background-state closure, where
\(\Lambda\) must be derived, regulated or replaced by a medium background law;
\item preferred-frame null tests remain local-core failure tests, not
deviation-window successes.
\end{itemize}

The immediate P0 blockers are therefore the stable phase, relational
covariance/protection, positive-energy physical modes, the local effective
hierarchy, Roots A--C, factorization, \(\Delta_{\mathcal M}=0\), ADM/EH
closure, total-energy source universality, and the microscopic input package.

\section{MVP Local-Core Proof Tasks}

This section converts the specification-stage memos into an execution plan.
It adds no new physical claim.  Its purpose is to say which calculations must
be performed, in which order, what each calculation must output, and what
counts as failure.  The central execution question is:
\[
\boxed{
\text{Which calculations turn the local gravity mechanism from a specification
into a proof attempt?}
}
\]

The first MVP is deliberately narrower than the full RMG program.  It targets
only the local gravity core: one external record cone; linearized
ADM/Einstein--Hilbert closure; universal total-energy sourcing; no leading
scalar/vector dipole radiation; leading tensor radiation-reaction
consistency; and observation matching only at the level supported by the
completed gates.  MOND, cosmology, strong-field branches and quantum
extensions may not repair a failure of this local core.

\medskip\noindent\textbf{Non-circular execution rules.}
The following rules are stronger than style requirements; if a task violates
one of them, it is not counted as a proof.
\begin{itemize}
\item Root~A may not assume ADM closure.  One record cone must be obtained from
  the medium spectrum and operational record observables.
\item Root~B may not assume Einstein equations.  ADM closure and the EH fixed
  point must come from the projected record action.
\item Root~C may not assume the equivalence principle.  Universal free fall and
  universal source strength are output gates.
\item GW emission may not assume the quadrupole formula.  Tensor flux and
  radiation reaction must be derived from the radiative channel.
\item Observations may not outrun proof gates.  An observation row is not
  marked passed unless every linked local gate is derived or explicitly kept
  as a conditional target.
\end{itemize}

\medskip\noindent\textbf{Dependency chain.}
The execution chain is:
\[
\boxed{
\mathcal D_{\mathcal M}
\to \text{Root A}
\to \text{Root B}
\to \text{Root C}
\to \text{GW emission}
\to \text{observation matching}.
}
\]
If Root~A fails, the local core has no unique record spacetime.  If Root~B
fails, one cone is not enough to obtain the ADM/EH local dynamics.  If Root~C
fails, the metric dynamics may be right but matter sourcing and free fall are
wrong.  If GW emission fails, propagation may be correct while binary-pulsar
and inspiral energy loss fail.  If observation matching fails, the completed
proof gates are insufficient or a local anchor has been missed.

\medskip\noindent\textbf{MVP task blocks.}
The execution program is divided into six artifacts.  The effort estimates are
planning estimates, not proof claims.
\begin{itemize}
\item \textbf{MVP-0: \texttt{MVP\_Input\_Freeze.md}.}  Freeze the exact
  symbols, assumptions, conditional inputs, normalizations and anchors allowed
  in the MVP.  Planning scale: about one week.
\item \textbf{MVP-A: \texttt{Root\_A\_MVP\_Spectrum\_Calculation.md}.}
  Execute the low-energy spectrum audit on the locked toy or candidate
  \(\mathcal D_{\mathcal M}\).  Expected output: one gapless external record
  cone.  Planning scale: about four to eight weeks.
\item \textbf{MVP-B: \texttt{Root\_B\_Linearized\_ADM\_MVP.md}.}  Execute
  the linearized ADM closure audit on the projected record action
  \(S_{\mathcal R}\).  Expected output: first-class linearized constraints,
  two TT modes and record-cone principal symbol.  Planning scale: about
  eight to twelve weeks.
\item \textbf{MVP-C: \texttt{Root\_C\_Source\_MVP\_Calculation.md}.}
  Execute the lapse/source and matter-family audit.  Expected output: the
  source equals total physical energy density and has no species-private tail.
  Planning scale: about six to ten weeks.
\item \textbf{MVP-E: \texttt{PSR\_B1913\_Emission\_MVP.md}.}  Derive leading
  tensor flux and radiation reaction.  The strict input is the completed
  Root~B tensor channel plus the completed Root~C source; a conditional draft
  is allowed only under the fast-track rule below.  Planning scale: about
  six to ten weeks.
\item \textbf{MVP-O: \texttt{MVP\_Observation\_Checklist.md}.}  Compare only
  supported gates against local anchors and keep every row no stronger than
  its proof-gate status.  Planning scale: about two weeks.
\end{itemize}

\medskip\noindent\textbf{MVP-0 input freeze.}
The first artifact prevents later calculations from importing hidden
assumptions.  It must lock: selected medium variables and background; the
coarse-graining scale; dimensional normalizations for
\(G_{\rm RMG}\), \(c\), \(\ell_*\) and \(\Lambda\), or their defining
relations; the record/internal split convention; the allowed internal gauge
group and observables; the target low-energy action form; the primary local
observation anchor; and the exact status vocabulary.

The freeze tasks are:
\begin{itemize}
\item list every primitive input used by the MVP;
\item list every conditional bridge and forbid presenting it later as derived;
\item list every deferred branch and forbid using it to repair a local-core
  failure;
\item lock notation for \(\mathcal D_{\mathcal M}\), \(S_{\mathcal R}\),
  \(g_{\rm rec}\), \(N\), \(u\), \(\rho_E\) and TT fields;
\item lock the toy or candidate \(\mathcal D_{\mathcal M}\): fields, tensor
  types, potential terms, interaction terms, background \(\mathcal M_0\) and
  scale hierarchy;
\item lock the dimensional normalization of \(G_{\rm RMG}\), \(c\), \(\ell_*\)
  and \(\Lambda\);
\item lock PSR~B1913+16 as the primary binary-pulsar emission anchor; other
  pulsars are extension targets, not MVP replacements.
\end{itemize}

\medskip\noindent\textbf{Frozen MVP toy input.}
The first Root~A execution pass uses a conservative toy input, not a final
microscopic ontology:
\[
\boxed{
\Phi_{\mathcal M}^{\rm MVP}=(\phi,\theta,Q_{ij}),\qquad
\phi=\sqrt n,\qquad Q_{ij}=Q_{ji},\qquad Q^i{}_i=0 .
}
\]
The selected phase is
\[
\boxed{
\mathcal M_0:\quad
\phi=v,\qquad \theta=\nu_0\tau,\qquad Q_{ij}=0 .
}
\]
Background isotropy means \(\phi=v\) is spatially constant,
\(\partial_i\theta=0\), and \(Q_{ij}=0\); hence spatial rotations act
trivially on \(\mathcal M_0\).  The coarse-graining scale \(\ell_*\) is a
primitive input for the MVP, not a fit parameter to repair local failures.

The first toy energy is frozen as
\[
\begin{aligned}
E_{\rm toy}=\int d^3X\,\bigg[
&\frac{k_\phi}{2}(\nabla\phi)^2
+\frac{k_\theta}{2}\phi^2(\nabla\theta)^2
+\frac{\lambda_\phi}{4}(\phi^2-v^2)^2\\
&+\frac{k_Q}{2}(\nabla Q_{ij})^2
+\frac{\mu_Q^2}{2}Q_{ij}Q_{ij}
+\frac{g_Q}{2}\phi^2 Q_{ij}Q_{ij}
\bigg],
\end{aligned}
\]
with \(E_{\rm mix}=0\) in the first spectrum calculation.  Cubic and quartic
\(Q\)-invariants, such as \(\mathrm{Tr}(Q^3)\) and
\((\mathrm{Tr}\,Q^2)^2\), are deferred.  Any later nonzero mixing term, for
example \(\eta(\nabla\theta)^2\), \(\eta Q_{ij}Q_{ij}\), or
\(Q_{ij}\partial_i\theta\,\partial_j\theta\), requires a revised
cone-splitting and suppression audit.

\medskip\noindent\textbf{Numerical first-pass regime.}
The first Root~A calculation uses dimensionless units
\[
\boxed{\ell_*=1}
\]
and the locked parameter regime
\[
\boxed{
v\ell_*=1,\quad
\lambda_\phi=\frac12,\quad
k_\phi=k_\theta=k_Q=1,\quad
\mu_Q^2\ell_*^2=4,\quad
g_Q=0 .
}
\]
Therefore
\[
\boxed{
m_\eta^2=2\lambda_\phi v^2=1\,\ell_*^{-2},\qquad
m_Q^2=\mu_Q^2+g_Qv^2=4\,\ell_*^{-2}.
}
\]
The amplitude branch \(\eta\) and the \(Q_{ij}\) branch are gapped in this
conservative first pass, while the phase fluctuation \(\vartheta\) is the
candidate gapless record branch.  These values make the first Hessian
calculation unambiguous; they are not asserted as measured constants.

\medskip\noindent\textbf{Presymplectic working form.}
The toy action is frozen as
\[
\boxed{
S_{\rm toy}=\int d\tau\,d^3X\,
\left[
\pi_\phi\dot\phi+\pi_\theta\dot\theta+\pi_Q^{ij}\dot Q_{ij}
-\alpha C_0-\beta^a C_a-E_{\rm toy}
\right].
}
\]
Both \(Q_{ij}\) and \(\pi_Q^{ij}\) are symmetric traceless \(3\times3\)
tensors with five independent components.  Before imposing constraints, the
toy phase space has \(2+2+10=14\) components.  The working constraint forms are
\[
\boxed{
C_a=\pi_\phi\partial_a\phi+\pi_\theta\partial_a\theta
  +\pi_Q^{ij}\partial_aQ_{ij},
}
\]
and
\[
\boxed{
C_0=\pi_\phi\dot\phi+\pi_\theta\dot\theta+\pi_Q^{ij}\dot Q_{ij}
  -\mathcal L_{\rm toy}.
}
\]
These are reparametrization/relabeling constraint candidates, not a completed
ADM derivation.  Root~B must still test whether the resulting constraint
algebra is first-class.

\medskip\noindent\textbf{Conservative \(Q\)-route firewall.}
The first MVP chooses the conservative route:
\[
\boxed{
Q_{ij}\text{ is gapped/internal in Root A.}
}
\]
This can support a one-cone Root~A pass, but it cannot by itself prove two
tensor gravitons.  The alternative TT-candidate route would allow a controlled
gapless \(q_{ij}^{TT}\) projection only after revising the input freeze and
proving
\[
\boxed{
\operatorname{rank}K_{Q{\rm -strain},TT}=2,\quad
\text{positive kinetic sign},\quad
c_{TT}=c_{\rm record},
}
\]
with no scalar/vector leakage and no second metric tensor mode.  The two
routes are mutually exclusive within a single MVP input freeze.

\medskip\noindent\textbf{Root A start gate.}
Root~A may start only from this package:
\[
\boxed{
\begin{gathered}
(\phi,\theta,Q_{ij}),\quad
\mathcal M_0=(v,\nu_0\tau,0),\quad
E_{\rm toy},\quad E_{\rm mix}=0,\\
\ell_*=1,\quad
v=1,\quad
\lambda_\phi=\tfrac12,\quad
k_\phi=k_\theta=k_Q=1,\quad
\mu_Q^2=4,\quad g_Q=0,\\
m_\eta^2=1\,\ell_*^{-2},\quad
m_Q^2=4\,\ell_*^{-2}.
\end{gathered}
}
\]
The next calculation must output:
\begin{itemize}
\item the explicit quadratic Hessian;
\item the spectrum classification;
\item the record/internal projector candidate;
\item the suppression catalog;
\item the decision whether Root~B may proceed, proceed conditionally or stop.
\end{itemize}
A clean failure is acceptable if it identifies which part of
\(\mathcal D_{\mathcal M}\) must be changed.

\medskip\noindent\textbf{First Root A MVP spectrum result.}
With
\[
\phi=1+\eta,\qquad \theta=\nu_0\tau+\vartheta,\qquad Q_{ij}=q_{ij},
\]
the frozen conservative toy gives the spatial quadratic Hessian
\[
\boxed{
\delta^2E_{\rm toy}
=\frac12\eta[-\nabla^2+1]\eta
+\frac12\vartheta[-\nabla^2]\vartheta
+\frac12q_{ij}[-\nabla^2+4]q_{ij}.
}
\]
The amplitude expansion is
\[
\frac18(\phi^2-1)^2
=\frac12\eta^2+\frac12\eta^3+\frac18\eta^4,
\]
so \(m_\eta^2=1\).  The \(Q\)-sector has
\(m_Q^2=\mu_Q^2+g_Qv^2=4\).  Since \(g_Q=0\), there is no \(\phi\)-\(Q\)
quadratic mixing; any \(\eta q_{ij}q_{ij}\) term would be cubic and is outside
the first Hessian audit.

In Fourier space,
\[
\boxed{
\lambda_\eta(k)=k^2+1,\qquad
\lambda_\vartheta(k)=k^2,\qquad
\lambda_Q(k)=k^2+4
}
\]
with five \(Q\)-components.  The first projector candidate is
\[
\boxed{
\Pi_{\mathcal R}(\eta,\vartheta,q_{ij})=(0,\vartheta,0),\qquad
\Pi_{\mathcal I}(\eta,\vartheta,q_{ij})=(\eta,0,q_{ij}),
}
\]
which satisfies
\[
\Pi_{\mathcal R}+\Pi_{\mathcal I}=1,\qquad
\Pi_{\mathcal R}\Pi_{\mathcal I}=0,\qquad
\Pi_{\mathcal R}^2=\Pi_{\mathcal R},\qquad
\Pi_{\mathcal I}^2=\Pi_{\mathcal I}.
\]
Thus the conservative toy has
\[
\boxed{
B_{\rm rec}^{\rm gapless}=\{\vartheta\},\qquad
N_{\rm gapless}^{\rm spatial}(B_{\rm rec})=1.
}
\]
This is an A4 pass: one gapless branch in the spatial Hessian.  It is not yet
an A5 pass, because a full cone count requires the temporal kinetic/principal
symbol.

\medskip\noindent\textbf{Temporal completion target.}
The recommended next completion is the second-order kinetic test
\[
\delta^2L_{\rm kin}
=\frac12 Z_\eta\dot\eta^2
+\frac12 Z_\theta\dot\vartheta^2
+\frac12 Z_Q\dot q_{ij}\dot q_{ij}.
\]
The canonical first test is
\[
\boxed{Z_\eta=Z_\theta=Z_Q=1,}
\]
which would give
\[
\boxed{
\omega_\eta^2=k^2+1,\qquad
\omega_\vartheta^2=k^2,\qquad
\omega_Q^2=k^2+4,\qquad
c_\eta^2=c_{\rm record}^2=c_Q^2=1.
}
\]
If \(Z_\eta\), \(Z_\theta\) and \(Z_Q\) differ in the leading local regime,
the sectors have different high-frequency cones; that is a Root~A one-cone
failure unless the non-record branch is demonstrably absent from
\(\mathcal O_{\rm rec}\) and cannot form external clocks, signals or detector
records.  At this stage the temporal completion is a conditional bridge, not
a derived result.

K1 status is therefore:
\[
\boxed{
\text{specified for the MVP: yes,\qquad derived from }\mathcal D_{\mathcal M}:
\text{ no.}
}
\]
To remove this conditionality, a later derivation must construct the medium
Lagrangian density from \(\mathcal D_{\mathcal M}\) and show that the leading
kinetic block is sector-universal:
\[
\boxed{Z_\eta=Z_\theta=Z_Q.}
\]
If the medium gives sector-dependent \(Z\)-coefficients in the leading local
regime, the one-cone K1 result fails.

With K1 the branch operators are
\[
\boxed{
P_\eta=-\omega^2+k^2+1,\qquad
P_\vartheta=-\omega^2+k^2,\qquad
P_Q=-\omega^2+k^2+4.
}
\]
The principal symbol is meant in the PDE sense: only the highest-derivative
terms define the principal cone.  The mass terms \(1\) and \(4\) are
lower-order terms; they change low-energy dispersion but not the causal
principal cone.  Thus every branch has
\[
\boxed{
P_{\rm principal}(\omega,k)=-\omega^2+k^2.
}
\]
For gapped branches the phase velocity may exceed one at low momentum,
\[
v_{\rm phase}=\frac{\omega}{k}=\sqrt{1+\frac{m^2}{k^2}}>1,
\]
but the information-carrying group velocity is
\[
v_{\rm group}=\frac{d\omega}{dk}=\frac{k}{\sqrt{k^2+m^2}}\le 1.
\]
So the massive-mode phase velocity is not a superluminal signal.  The
normalization \(c_{\rm record}=1\) is the canonical K1 unit choice; matching it
to the measured speed of light belongs to the later static/radiative
normalization bridge.

The K1 quadratic Hamiltonian is positive semidefinite:
\[
\begin{aligned}
H_\eta&=\frac12\pi_\eta^2+\frac12(\nabla\eta)^2+\frac12\eta^2,\\
H_\vartheta&=\frac12\pi_\vartheta^2+\frac12(\nabla\vartheta)^2,\\
H_Q&=\frac12\pi_Q^{ij}\pi_Q^{ij}
  +\frac12(\nabla q_{ij})(\nabla q_{ij})+2q_{ij}q_{ij}.
\end{aligned}
\]
The only flat direction is the constant phase mode of \(\vartheta\), the
expected shift/phase zero mode.

\medskip\noindent\textbf{Root A MVP verdict.}
The conservative toy passes the static/spatial Hessian and gap audit:
\(\eta\) is gapped with \(m_\eta=1\,\ell_*^{-1}\), \(q_{ij}\) is gapped with
\(m_Q=2\,\ell_*^{-1}\), and \(\vartheta\) is the only spatial gapless record
candidate.  If \(\ell_*\) is microscopic, these gapped branches decouple from
lab, Solar-System and binary propagation; the physical interpretation of that
decoupling remains conditional on the final scale assignment.

The single-branch spatial factorization is trivial:
\[
\Pi_{\mathcal R}L_{\rm spatial}(k)\Pi_{\mathcal R}=k^2,
\qquad K_{\mathcal R}=1.
\]
With K1 the full projected operator is
\[
\boxed{
\Pi_{\mathcal R}P(\omega,k)\Pi_{\mathcal R}=-\omega^2+k^2,
}
\]
equivalently
\[
P(\omega,k)=
\operatorname{diag}(-\omega^2+k^2+1,\,-\omega^2+k^2,\,-\omega^2+k^2+4),
\]
and
\[
\Pi_{\mathcal R}P\Pi_{\mathcal R}
=\operatorname{diag}(0,\,-\omega^2+k^2,\,0).
\]
This does not prove all-species factorization, because photons, atoms, rods,
clocks and detector matter are not included as separate matter families in the
toy.  That part must be audited in Root~C when the matter Hamiltonian and
record observables are included, and in Root~B when the record action is
constructed.

Operational consequences of the K1 one-cone result are limited but concrete:
\begin{itemize}
\item record signals built from the \(\vartheta\) branch propagate with
  \(c=1\) in canonical units;
\item \(\eta\) and \(q_{ij}\) are gapped, with ranges of order \(1/m_\eta\)
  and \(1/m_Q\);
\item at distances much larger than those ranges, only the gapless
  \(\vartheta\) branch remains in the conservative toy;
\item the one-branch record sector has no low-energy birefringence;
\item the isotropic background \(Q_{ij}=0\) introduces no local preferred
  spatial direction.
\end{itemize}
Open operational tests remain: matter species must be shown to couple through
the same record structure; light bending and clock comparison require matter
and clock probes; nonlinear weak-field coefficients require Root~B; and GW
speed/tensor modes require the tensor route, which the conservative \(Q\)
branch does not provide.

Operationally, \(\vartheta=\theta-\nu_0\tau\) is a Goldstone-like
clock/phase fluctuation of the record medium, not yet a metric perturbation by
itself.  A gapless phase mode would be a fifth-force risk if it coupled
directly to matter as a private scalar.  The MVP must later show that
\(\vartheta\) enters matter only through the common record metric/clock
structure, or that a shift/record symmetry forbids species-private matter
coupling.
The most likely conservative-MVP route is the shift/record symmetry
\[
\boxed{\theta\to\theta+\text{constant}.}
\]
Root~C must test that the matter Hamiltonian may depend on derivatives or
record-metric combinations of \(\theta\), but not on a species-private explicit
\(\theta\)-charge.  Such a private explicit phase coupling would fail the
fifth-force gate.

The \(C_0,C_a\) constraint caveat remains open.  Root~B must compute
\[
\boxed{
\{C_0,C_0\},\qquad \{C_0,C_a\},\qquad \{C_a,C_b\},
}
\]
verify closure on the constraint surface, verify that \(\vartheta\) is not
removed as the record branch, verify that no extra scalar/vector propagating
mode is introduced, and verify that the record principal symbol is preserved.

Root~B may proceed only conditionally.  It may import that \(\vartheta\) is the
record candidate and that \(\eta,q_{ij}\) are gapped in the conservative pass,
but it may not claim two tensor modes from the conservative \(Q\)-sector, full
ADM/EH closure, all-species one-cone universality, or a derived
\(g_{\rm rec}\) with both temporal and spatial components.  The recommended
next artifact is the conditional Root~B record-action bridge; deriving K1 from
\(\mathcal D_{\mathcal M}\) first is stricter, but may not rescue the local
core if the Root~B bridge fails even under K1.

\medskip\noindent\textbf{Root B conditional record-action bridge.}
The Root~B bridge executes the MVP-B B0 task: construct a conditional
record-action object precise enough for the Root~B ADM audit without treating
ADM closure, the Einstein--Hilbert action, two TT modes or all-species
universality as already proven.  The next artifact after this bridge is the
linearized Root~B audit, which executes B1--B7.

Root~B may import from Root~A/K1 only the following facts, with the following
status:
\[
\boxed{
\begin{gathered}
\vartheta:\ \text{conditional record branch},\qquad
P_{\mathcal R}(\omega,k)=-\omega^2+k^2,\qquad
c_{\rm record}=1,\\
\eta,\ q_{ij}:\ \text{gapped/internal in the conservative route},\\
K1:\ \text{specified bridge, not yet derived from }\mathcal D_{\mathcal M},\\
\text{all-species universality and }C_0,C_a\text{ closure: open.}
\end{gathered}
}
\]
It may not import ADM closure, an EH action, two TT gravitons or a universal
matter metric.

The bridge object is
\[
\boxed{
\begin{aligned}
S_{\mathcal R}^{\rm bridge}={}&
S_{K1}[\vartheta;\bar g]
+S_{\rm gap}[\eta,q_{ij};\bar g]
+S_{\rm geom}^{\rm cond}[g_{\rm rec}]\\
&+S_{\rm coupling}^{\rm cond}[g_{\rm rec},\Psi_{\rm rec}]
+S_{\rm rem}.
\end{aligned}
}
\]
Here \(S_{K1}\) carries the one-cone phase result; \(S_{\rm gap}\) carries the
gapped internal branches; \(S_{\rm geom}^{\rm cond}\) is the conditional
metric-dynamics sector to be audited by Root~B; \(S_{\rm coupling}^{\rm cond}\)
is only a placeholder for matter/record probes and must not assume Root~C; and
\(S_{\rm rem}\) must be classified as gauge, constrained, boundary, gapped,
high-energy suppressed, large-scale-only or fail-risk.

The K1 cone reconstructs only the local background cone metric:
\[
\boxed{
-\omega^2+k^2=0
\quad\Longleftrightarrow\quad
\bar g^{\mu\nu}=\eta^{\mu\nu}=\operatorname{diag}(-1,1,1,1)
}
\]
in canonical K1 units.  Thus the bridge takes \(\bar g=\eta\) as the natural
local background choice.  A constant conformal rescaling is harmless only if
the measured normalization of \(c_{\rm record}\) is kept fixed.  A nontrivial
conformal factor, anisotropic background or preferred-frame background must be
justified separately and is a possible Root~A/Root~B failure mode.

To run the Root~B audit, the bridge introduces conditional record-geometry
variables
\[
\boxed{
N=1+n,\qquad N^i=n^i,\qquad h_{ij}=\delta_{ij}+\gamma_{ij},\qquad
\pi^{ij}\ \text{conjugate to }h_{ij}.
}
\]
Their tensor types are the standard ADM types: \(N\) is a scalar lapse,
\(N^i\) a spatial vector shift, \(h_{ij}\) a symmetric spatial two-tensor and
\(\pi^{ij}\) the conjugate symmetric tensor density.  The unresolved issue is
not the ADM tensor type.  The unresolved issue is the medium map: whether
\(\vartheta\) maps to \(N\), to \(h_{ij}\), to both through \(g_{\rm rec}\), or
to neither as a directly geometric variable.

Metric reconstruction therefore has three possible routes:
\begin{enumerate}
\item build \(g_{\rm rec}^{\mu\nu}\) from Goldstone-gradient data such as
  \(\partial_\mu\theta\,\partial_\nu\theta\);
\item build \(g_{\rm rec}\) from the K1 dispersion/principal-symbol
  structure;
\item introduce \(g_{\rm rec}\) as a conditional bridge variable and derive it
  later.
\end{enumerate}
The first MVP uses the third route.  The first two routes are deferred to a
later metric-from-medium derivation.

Root~B may use the two-derivative Hamiltonian form only as an audit filter,
not as an assumed result:
\[
\boxed{
\mathcal H_B=
\frac{1}{\sqrt h}\left(\pi^{ij}\pi_{ij}-\lambda\pi^2\right)
-\sqrt h(\alpha R-2\Lambda)+\Delta_{\mathcal M}.
}
\]
The tested target is
\[
\boxed{\lambda=\frac12,\qquad \alpha=1,\qquad \Delta_{\mathcal M}=0}
\]
or \(\Delta_{\mathcal M}\) must be a constraint, gauge, boundary, internal,
gapped, high-energy suppressed or large-scale-only term with a stated control.
For this MVP, the working controls are:
\begin{itemize}
\item high-energy suppression requires a stated \(\Lambda_{\rm UV}\), with
  \(E_{\rm test}/\Lambda_{\rm UV}<10^{-5}\) as the Solar-System PPN-scale
  working threshold unless a sharper bound applies;
\item a gapped branch must satisfy \(m>1/r_{\rm test}\) for the relevant
  experiment;
\item a large-scale-only term is allowed only for \(k<H_0/c\) and may not
  repair a local Solar-System, laboratory, binary-pulsar or GW failure;
\item a boundary term must live at a declared boundary and not change the
  local bracket;
\item a gauge or constraint term must be removed by a stated generator or
  first-class constraint check.
\end{itemize}

The re-embedding audit acts on the record fields by
\[
\boxed{\delta_\xi\Phi_{\mathcal R}=\mathcal L_\xi\Phi_{\mathcal R}},
\qquad
\boxed{\Omega_{\mathcal R}(\delta\Phi,\delta_\xi\Phi)\approx0}
\]
or equivalently by the generators \(H[N]+D[N^i]\).  The field types must be
declared before this calculation: \(\vartheta\) is a scalar with
\(\mathcal L_\xi\vartheta=\xi^\mu\partial_\mu\vartheta\);
\(g_{\rm rec\,\mu\nu}\) is a symmetric tensor with
\(\mathcal L_\xi g_{\mu\nu}=\nabla_\mu\xi_\nu+\nabla_\nu\xi_\mu\);
\(N,N^i,h_{ij}\) inherit their transformations from the spacetime metric
split; and retained \(\eta,q_{ij}\) branches must be treated as
internal/gapped unless a later input freeze revises their role.

The tensor-sector risk is the main conservative-route risk.  Root~A/K1 gives
one scalar-like phase record branch, a gapped \(Q_{ij}\) branch and no derived
TT graviton from conservative \(Q\).  Root~B nevertheless requires
\[
\boxed{
\text{physical DOF}=2,\qquad \operatorname{rank}K_{TT}=2,\qquad
\text{positive tensor kinetic sign},\qquad
P_{TT}=P_{\mathcal R},
}
\]
with no scalar/vector radiation and no second metric.  The allowed sequence is:
\begin{enumerate}
\item first try a conditional TT construction from \(\gamma_{ij}^{TT}\) as a
  record-geometry variable, carrying the K1 cone;
\item if the hyperbolicity or rank audit fails on \(\gamma_{ij}^{TT}\), revise
  the input freeze toward a gapless TT-candidate \(Q\)-route;
\item if only the scalar one-cone sector remains, count this as a blocking
  failure of the local tensor-gravity MVP.
\end{enumerate}
The forbidden move is to rename the gapped conservative \(Q\)-sector as two TT
gravitons.

The fifth-force firewall is also only conditional at this bridge stage.  The
toy action contains derivatives of \(\theta\) but no explicit \(\theta\), so
\[
\boxed{\theta\to\theta+\text{constant}}
\]
is a shift symmetry of the toy action.  The background
\(\theta=\nu_0\tau\) breaks this symmetry in the selected phase, and
\(\vartheta\) is the Goldstone-like fluctuation.  This may protect against a
private scalar force, but only if the later matter Hamiltonian depends on
derivatives or common record-metric combinations of \(\theta\), not on
species-private explicit \(\theta\)-charge.  That test belongs to Root~C and
the record-action bridge; it is not assumed here.

Root~B may proceed to the linearized ADM audit only with the following labels:
K1 is specified rather than medium-derived; the metric/tensor sector is
conditional; two TT modes are an audit target rather than an input; matter
universality is not assumed; and every anomaly or \(\Delta_{\mathcal M}\)
must carry a scale or structural reason.  Root~B must stop if the bridge
requires kinetic lapse, a physical shift, a private preferred-frame tensor,
scalar/vector radiation in the local vacuum sector, a second metric, or the
identification of gapped \(Q\) with TT gravity.  If only some Root~B conditions
fail, each failed condition must be documented separately and the revision
must be assigned either to the bridge, to the input freeze, or to the program
status if only a scalar one-cone conditional theory remains.

The accumulated conditional debt is:
\begin{center}
\begin{tabular}{p{0.18\textwidth}p{0.21\textwidth}p{0.27\textwidth}p{0.23\textwidth}}
\hline
\textbf{Bridge} & \textbf{Source} & \textbf{Risk} & \textbf{Resolution path}\\
\hline
K1 specification & Root~A temporal completion &
\(Z_\eta=Z_\theta=Z_Q=1\) not derived &
derive K1 from the medium\\
\(\bar g=\eta\) & this bridge &
Lorentz invariance not derived from \(\mathcal D_{\mathcal M}\) &
medium symmetry analysis\\
\(N,N^i,h_{ij}\) & this bridge &
metric origin from medium not derived &
metric-from-medium derivation\\
\(\gamma_{ij}^{TT}\) existence & this bridge &
conservative \(Q\) is gapped, so no TT source is derived &
CB-5 revision or record-geometry route\\
\(C_0,C_a\) closure & Root~A temporal completion &
constraint algebra not audited &
Root~B bracket calculation\\
matter universality & this bridge &
matter species not modeled &
Root~C matter-family audit\\
\hline
\end{tabular}
\end{center}
No row in this ledger may be silently counted as derived.  Each row must be
resolved, or explicitly accepted as a final conditional bridge, before any
Root~B pass claim is allowed.

\medskip\noindent\textbf{Root A execution tasks.}
Root~A begins only after the medium choice is locked by MVP-0.  The MVP-A
calculation must verify that the selected \(\mathcal D_{\mathcal M}\) provides
all needed components; check stability of the selected phase \(\mathcal M_0\);
compute \(\mathcal L_{\mathcal M_0}\) and its branches; verify
\(\Pi_{\mathcal R}+\Pi_{\mathcal I}=1\) and
\(\Pi_{\mathcal R}\Pi_{\mathcal I}=0\); extract the low-energy dispersion of
record branches for the selected stable phase; prove
\[
\boxed{N_{\rm cone}(B_{\rm rec}^{\rm gapless})=1}
\]
for that phase; test \(Q_{AB}^{\mu\nu}=G^{\mu\nu}K_{AB}\); build a
suppression catalog for every non-record branch; and translate the cone into
operational clocks, rods and signals.  Alternative stable phases are deferred
unless they are selected in MVP-0.

\medskip\noindent\textbf{Root B execution tasks.}
Root~B must construct or explicitly condition the bridge
\[
\boxed{
\mathcal D_{\mathcal M}\to S_{\rm eff}^{\rm rec}\to S_{\mathcal R}.
}
\]
If any part of this bridge is conditional rather than derived, it must already
appear in the MVP-0 conditional-bridge table.  Root~B then extracts
\(\Theta_{\mathcal R}\) and \(\Omega_{\mathcal R}\), identifies embedding null
directions, derives \(H[N]\) and \(D[N^i]\), and computes
\(\{D,D\}\), \(\{H,D\}\) and \(\{H,H\}\) from the canonical Poisson or Dirac
structure derived from \(\Omega_{\mathcal R}\), not from target ADM brackets.
The pass target is \(A[N,M]=0\) or a controlled boundary/gauge term,
\(\lambda=1/2\), \(\alpha=1\), \(\Delta_{\mathcal M}=0\) or suppressed, two
positive TT modes, record-cone propagation and matter-coupled constraint
preservation.

\medskip\noindent\textbf{Root B linearized ADM MVP scaffold.}
The linearized Root~B MVP is an execution scaffold, not a completed proof.  It
starts from the conditional \(S_{\mathcal R}^{\rm bridge}\) and asks whether
the local record sector can support a linearized ADM phase space with
non-kinetic lapse/shift, first-class constraints, exactly two positive TT
modes, the K1 record cone and no local scalar/vector/preferred-frame leakage.
The current status is:
\[
\boxed{
\begin{gathered}
B0\ \text{bridge imported conditionally},\qquad
\Theta_{\mathcal R},\Omega_{\mathcal R}\ \text{not yet derived},\\
\text{no kinetic lapse/shift: first hard check},\qquad
\text{two TT modes: conditional/fail-risk},\\
\text{ADM bracket closure and EH fixed point: open.}
\end{gathered}
}
\]

The local background is the K1 background
\[
\boxed{\bar g=\eta,\qquad \eta^{\mu\nu}=\operatorname{diag}(-1,1,1,1).}
\]
This is more than notation: the linearized principal operator must be
Lorentz-covariant under \(x^\mu\mapsto\Lambda^\mu{}_\nu x^\nu\).  The K1
choice \(Z_\eta=Z_\theta=Z_Q=1\) gives the common principal symbol
\(-\omega^2+k^2\), and the branch dispersions
\(\omega^2=k^2+m^2\) are Lorentz-covariant at the principal-symbol level.  A
sector-dependent leading \(Z\) would create a preferred cone and would prevent
\(\bar g=\eta\) from serving as the common local background.

The conditional ADM perturbations are
\[
\boxed{
N=1+n,\qquad N^i=n^i,\qquad h_{ij}=\delta_{ij}+\gamma_{ij},\qquad
\pi^{ij}\ \text{conjugate to }\gamma_{ij}.
}
\]
The spatial perturbation is decomposed as
\[
\boxed{
\gamma_{ij}
=\gamma_{ij}^{TT}
+\partial_iV_j^T+\partial_jV_i^T
+\left(\partial_i\partial_j-\frac13\delta_{ij}\partial^2\right)\sigma
+\frac13\delta_{ij}\gamma,
}
\]
with
\[
\boxed{
\partial^i\gamma_{ij}^{TT}=0,\qquad
\delta^{ij}\gamma_{ij}^{TT}=0,\qquad
\partial^iV_i^T=0.
}
\]
The target mode assignment is: \(\gamma_{ij}^{TT}\) supplies two propagating
modes; \(V_i^T,\sigma,\gamma\) are constrained, gauge or near-zone; and
\(n,n^i\) are multipliers with no physical kinetic energy.  Any
unsuppressed scalar, vector, lapse, shift, massive spin-2, second metric or
preferred-frame branch is a local Root~B failure.

The conditional quadratic action to audit is
\[
\boxed{
S_{\mathcal R}^{(2)}
=\int dt\,d^3x\,
\left[
\pi^{ij}\dot\gamma_{ij}
+\pi_\vartheta\dot\vartheta
-n\mathcal H^{(1)}
-n^i\mathcal H_i^{(1)}
-\mathcal H_{\rm phys}^{(2)}
-\Delta_{\mathcal M}^{(2)}
\right].
}
\]
This is an audit target, not an assumed action.  The primary constraints
\[
\boxed{\pi_n\approx0,\qquad \pi_i\approx0}
\]
must arise from the record action itself.  The target linear constraints are
\[
\boxed{
\mathcal H_i^{(1)}=-2\partial_j\pi^j{}_i,\qquad
\mathcal H^{(1)}=-\partial^i\partial^j\gamma_{ij}+\partial^2\gamma.
}
\]
The sign convention is
\[
\boxed{
R^{(1)}=\partial^i\partial^j\gamma_{ij}-\partial^2\gamma,\qquad
\mathcal H^{(1)}=-R^{(1)}
}
\]
up to the overall sign convention for \(H[N]\), normalization and boundary
terms.  Momentum-squared ADM terms are quadratic around the zero-momentum
background and enter the quadratic consistency and bracket audit, not the
first-order scalar constraint.

Possible sources of \(\Delta_{\mathcal M}^{(2)}\) are: \(\eta\)-geometry
mixing after integrating out the gapped amplitude branch; \(q_{ij}\)-geometry
mixing after integrating out the gapped strain branch; higher-derivative
operators suppressed by \(\ell_*\); or residual preferred-frame/medium-flow
terms left by an incomplete projection.  The conservative local target is
\[
\boxed{\Delta_{\mathcal M}^{(2)}=0}
\]
after gapped/internal fields are integrated out, or else every surviving piece
must carry a suppression scale and an observation range.

The B1 presymplectic extraction must compute
\[
\boxed{
\delta L_{\mathcal R}=E_{\mathcal R}\delta\Phi_{\mathcal R}
+d\Theta_{\mathcal R}(\delta\Phi_{\mathcal R}),\qquad
\Omega_{\mathcal R}=\int_\Sigma\delta\Theta_{\mathcal R}.
}
\]
The comparison target is the canonical form
\[
\boxed{
\Theta_{\mathcal R}
=\int d^3x\,\left(\pi^{ij}\delta\gamma_{ij}
+\pi_\vartheta\delta\vartheta\right)
+\text{boundary/constraint/internal terms}.
}
\]
This form must be derived from
\[
\boxed{\mathcal D_{\mathcal M}\to S_{\mathcal R}^{\rm bridge}
\to\Theta_{\mathcal R}\to\Omega_{\mathcal R}.}
\]
If \(\Theta_{\mathcal R}\) contains an unsuppressed
\(\pi_n\delta n\) or \(\pi_i\delta n^i\) physical kinetic sector, Root~B fails
before the bracket audit.

The B2--B4 constraint audit follows the Dirac algorithm:
\begin{enumerate}
\item derive primary constraints from velocity-momentum relations;
\item preserve \(\pi_n\) and \(\pi_i\) in time, producing
  \(\mathcal H\approx0\) and \(\mathcal H_i\approx0\);
\item iterate preservation of \(\mathcal H,\mathcal H_i\);
\item classify all constraints as first class or second class;
\item pass to Dirac brackets if second-class constraints appear;
\item verify that no step creates a propagating lapse, shift, scalar, vector
  or preferred-frame medium mode.
\end{enumerate}
The abelianized linear check is
\[
\boxed{
\{D[L],D[M]\}\ \text{closes on }D,\qquad
\{H[N],D[M]\}\ \text{closes on }H,\qquad
\{H[N],H[M]\}=0
}
\]
at flat linear order.  This is not a full Root~B pass.  The quadratic
consistency check must include the first nonlinear corrections in
\(h_{ij}=\delta_{ij}+\gamma_{ij}\) and verify
\[
\boxed{
\{H[N],H[M]\}
-D[h^{ij}(N\partial_jM-M\partial_jN)]
=A[N,M]
}
\]
with \(A[N,M]=0\) or a controlled boundary/gauge/constraint term.

The B5 filter tests
\[
\boxed{\lambda=\frac12,\qquad \alpha=1,\qquad \Delta_{\mathcal M}=0}
\]
or a controlled \(\Delta_{\mathcal M}\).  \(\lambda\ne1/2\) leaves a scalar
gravitational mode; \(\alpha\ne1\) after measured-\(G\) normalization gives
the wrong curvature response; and an unsuppressed preferred-frame,
scalar/vector, massive-tensor, second-metric or non-ADM operator fails the
local core.

The B6 hyperbolicity certificate must prove
\[
\boxed{
\operatorname{rank}K_{TT}=2,\qquad
\text{positive kinetic sign},\qquad
\text{positive gradient sign},\qquad
P_{TT}=-\omega^2+k^2,
}
\]
with no scalar/vector leakage, no massive spin-2 leakage and no second
metric.  The comparison action is
\[
\boxed{
S_{TT}^{(2)}
=\frac12\int dt\,d^3x\,
\left[
\dot\gamma_{ij}^{TT}\dot\gamma_{ij}^{TT}
-(\partial_k\gamma_{ij}^{TT})(\partial_k\gamma_{ij}^{TT})
\right].
}
\]
This is not a proof unless the TT projector and its positive conjugate
momentum are derived from \(S_{\mathcal R}^{\rm bridge}\).  The possible TT
routes are: identify \(\gamma_{ij}^{TT}\) with a TT projection of \(q_{ij}\)
(blocked in the conservative route because \(q_{ij}\) is gapped); build it as
a composite from \(\phi,\theta\) gradients or other record data; keep it as a
new conditional record-geometry variable; or revise the input freeze toward a
gapless TT-candidate \(Q\)-route.

The B7 matter-preservation check does not prove Root~C, but it must not make
Root~C impossible.  It must test
\[
\boxed{\dot{\mathcal H}\approx0,\qquad \dot{\mathcal H}_i\approx0}
\]
for the matter sectors used in the MVP: massive test bodies, photons/EM field,
bound systems, composite macroscopic bodies and, only after the local audit is
safe, compact-body effective variables.  Matter must not require a private
lapse, private shift, species-private metric, nonzero local \(A[N,M]\), or
explicit species-private \(\theta\)-charge.

The canonical degree-of-freedom count must include lapse/shift momenta:
\[
\boxed{
6(\gamma_{ij})+6(\pi^{ij})+1(n)+1(\pi_n)+3(n^i)+3(\pi_i)=20
}
\]
phase-space variables.  The standard ADM pattern has four primary constraints
\(\pi_n,\pi_i\) and four secondary constraints
\(\mathcal H,\mathcal H_i\).  If all eight are first class, they remove
sixteen phase-space degrees of freedom:
\[
\boxed{20-8\times2=4}
\]
leaving two propagating configuration modes, \(\gamma_{ij}^{TT}\) and its
conjugate momentum.  This count is valid only if B1--B4 actually produce the
required first-class constraints.

The next calculation is triggered only when: \(S_{\mathcal R}^{\rm bridge}\)
is documented; \(\Phi_{\mathcal R}\) is fixed; \(\bar g=\eta\) is locked; the
K1 block \(Z_\eta=Z_\theta=Z_Q=1\) is specified; conditional bridges are
listed; \(\Delta_{\mathcal M}^{(2)}\) sources are enumerated; and the B1 output
format is fixed.  B1 must output an explicit \(\Theta_{\mathcal R}\), an
explicit \(\Omega_{\mathcal R}\), a yes/no decision on \(\pi_n\delta n\) and
\(\pi_i\delta n^i\), a phase-space dimension count and a stop/continue
decision.  If B1 is unclear, the scaffold must be revised before starting the
bracket calculation.

\medskip\noindent\textbf{Root B B1 presymplectic extraction.}
B1 is the first Root~B calculation checkpoint.  Its central question is:
\[
\boxed{
\text{Does }S_{\mathcal R}^{(2)}\text{ produce }
\Theta_{\mathcal R}\text{ and }\Omega_{\mathcal R}
\text{ with no }\pi_n\delta n\text{ or }\pi_i\delta n^i\text{ sector?}
}
\]
The answer is a conditional yes under the no-dot rule for
\(\Delta_{\mathcal M}^{(2)}\):
\[
\boxed{
\begin{gathered}
\Theta_{\mathcal R}\ \text{derived from }S_{\mathcal R}^{(2)}:
\text{ yes, conditional},\\
\Omega_{\mathcal R}\ \text{derived}: \text{ yes, conditional},\\
\pi_n\delta n,\ \pi_i\delta n^i:\text{ absent in the conditional action},\\
\pi_n\approx0,\quad \pi_i\approx0:\text{ primary constraints},\\
B1:\text{ conditional pass; Root~B proof not passed.}
\end{gathered}
}
\]

The conditional nature of the action is essential:
\[
\boxed{
\text{IF }S_{\mathcal R}^{\rm bridge}\text{ produces the no-dot quadratic
form, THEN }\pi_n=0,\ \pi_i=0\text{ at B1 order.}
}
\]
B1 does not prove
\[
\boxed{\mathcal D_{\mathcal M}\to S_{\mathcal R}^{\rm bridge}
\to\text{ no-dot lapse/shift action}.}
\]
If the eventual medium-derived action contains physical \(\dot n\) or
\(\dot n^i\) terms, the B1 conditional pass is revoked.

For B1 the remainder is split as
\[
\boxed{
\Delta_{\mathcal M}^{(2)}
=\Delta_{\rm no-dot}^{(2)}
+\Delta_{\rm lapse-dot}^{(2)}
+\Delta_{\rm shift-dot}^{(2)}
+\Delta_{\rm mixed-dot}^{(2)}.
}
\]
B1 requires
\[
\boxed{
\Delta_{\rm lapse-dot}^{(2)}=
\Delta_{\rm shift-dot}^{(2)}=
\Delta_{\rm mixed-dot}^{(2)}=0
}
\]
or these pieces must be pure boundary/gauge degeneracies with an explicit
generator.  The conservative conditional branch keeps only
\(\Delta_{\rm no-dot}^{(2)}\) at this stage.  Candidate no-dot sources include
\(\eta\)-geometry mixing, suppressed by \(m_\eta=\ell_*^{-1}\);
\(q_{ij}\)-geometry mixing, suppressed by \(m_Q=2\ell_*^{-1}\);
higher-derivative spatial operators suppressed by \(\ell_*\); and possible
\(\vartheta\)-geometry or preferred-flow terms, which remain open until the
B2--B7 and Root~C audits.

The variation uses \(\delta\) as the field-space exterior derivative and
\(d_t\) as the time derivative.  For the metric canonical pair,
\[
\boxed{
\delta(\pi^{ij}\dot\gamma_{ij})
=\dot\gamma_{ij}\delta\pi^{ij}
-\dot\pi^{ij}\delta\gamma_{ij}
+d_t(\pi^{ij}\delta\gamma_{ij}).
}
\]
For the record phase pair,
\[
\boxed{
\delta(\pi_\vartheta\dot\vartheta)
=\dot\vartheta\,\delta\pi_\vartheta
-\dot\pi_\vartheta\,\delta\vartheta
+d_t(\pi_\vartheta\delta\vartheta).
}
\]
For lapse and shift,
\[
\boxed{
\delta(-n\mathcal H^{(1)})
=-\mathcal H^{(1)}\delta n-n\delta\mathcal H^{(1)},\qquad
\delta(-n^i\mathcal H_i^{(1)})
=-\mathcal H_i^{(1)}\delta n^i-n^i\delta\mathcal H_i^{(1)}.
}
\]
There is no term \(d_t(\pi_n\delta n)\) or
\(d_t(\pi_i\delta n^i)\), because \(n\) and \(n^i\) have no time derivatives
in the conditional B1 action.

Collecting time-boundary terms gives
\[
\boxed{
\Theta_{\mathcal R}
=\int d^3x\,
\left(
\pi^{ij}\delta\gamma_{ij}
+\pi_\vartheta\delta\vartheta
\right)
+\Theta_{\rm boundary}
+\Theta_{\rm internal}
+\Theta_{\Delta{\rm no-dot}}.
}
\]
In the no-dot branch,
\[
\boxed{
\operatorname{coeff}_{\delta n}\Theta_{\mathcal R}=0,\qquad
\operatorname{coeff}_{\delta n^i}\Theta_{\mathcal R}=0,
}
\]
so
\[
\boxed{\pi_n=0,\qquad \pi_i=0}
\]
as primary constraints.  Boundary terms may contribute boundary charges,
internal terms may contribute gapped/internal symplectic blocks, and
\(\Theta_{\Delta{\rm no-dot}}\) may contribute classified no-dot corrections;
none may generate a local physical \(\delta n\) or \(\delta n^i\) kinetic
coefficient.

The field-space derivative yields
\[
\boxed{
\Omega_{\mathcal R}
=\int_\Sigma d^3x\,
\left(
\delta\pi^{ij}\wedge\delta\gamma_{ij}
+\delta\pi_\vartheta\wedge\delta\vartheta
\right)
+\Omega_{\rm boundary}
+\Omega_{\rm internal}
+\Omega_{\Delta{\rm no-dot}}.
}
\]
No physical lapse/shift symplectic block appears:
\[
\boxed{
\delta\pi_n\wedge\delta n\ \text{absent},\qquad
\delta\pi_i\wedge\delta n^i\ \text{absent}.
}
\]
Thus \(n\) and \(n^i\) are candidate multiplier variables at B1 order.  This
is not yet a proof that they generate the ADM constraints; B2/B3 must still
show embedding gauge and the secondary constraints
\(\mathcal H\approx0,\mathcal H_i\approx0\).

The B1 bookkeeping has
\[
\boxed{
20\ \text{metric/lapse/shift phase-space variables}
+2\ \text{record-phase variables }(\vartheta,\pi_\vartheta)=22.
}
\]
B1 does not decide whether \(\vartheta\) is a clock variable, gauge direction,
constrained variable, decoupled phase, or a dangerous scalar gravitational
mode.  If B2/B3 do not reduce or decouple it, \(\vartheta\) remains a gapless
scalar-like local mode, and B6/Root~C must show that it does not mediate a
species-dependent fifth force.

B1 must be revoked if a later derivation of \(S_{\mathcal R}^{\rm bridge}\)
produces an unsuppressed term of the form
\[
\boxed{
\dot n^2,\quad
\dot n_i\dot n^i,\quad
\dot n\,\dot\gamma,\quad
\dot n^i\dot\gamma_{ij},\quad
\dot n\,\dot\vartheta,\quad
\dot n^i\partial_i\vartheta,\quad
\dot n^i n_i
}
\]
unless the term is proven to be pure boundary or gauge-degenerate and absent
from \(\Omega_{\mathcal R}\) on physical observables.

The critical forward tests inherited from B1 are:
\begin{itemize}
\item B2/B4 must verify that \(\Delta_{\rm no-dot}^{(2)}\) hides no
  lapse/shift kinetic sector;
\item B2/B3 and Root~C must decide whether \(\vartheta\) is gauge, constrained
  or decoupled from fifth-force sourcing;
\item B7 and Root~C must show that no-dot remainders stay safe under matter
  coupling;
\item B3 must derive \(\mathcal H,\mathcal H_i\) rather than merely use their
  target forms;
\item B4 must close \(\{H,H\}\) beyond the abelian linear check;
\item B6 must make the TT origin compatible with the same presymplectic form.
\end{itemize}
If any of these tests fails, the corresponding part of the B1 conditional pass
fails retroactively.

\medskip\noindent\textbf{Root B B2 embedding null test.}
B2 uses the B1 presymplectic form to test whether linearized re-embedding
deformations are gauge directions rather than physical preferred slicing or
medium-flow degrees of freedom.  The correct canonical test is not off-shell
zero:
\[
\boxed{\Omega_{\mathcal R}(\delta\Phi,\delta_\xi\Phi)\ne0
\quad\text{in general off shell}.}
\]
The correct test is
\[
\boxed{
\Omega_{\mathcal R}(\delta\Phi,\delta_\xi\Phi)
=\delta G[\xi],
\qquad
G[\xi]=H[N]+D[M^i]+\text{boundary},
}
\]
with \(G[\xi]\approx0\) on the constraint surface.  Thus re-embedding becomes
a presymplectic null direction only after B3 derives the constraints
\(H\approx0\) and \(D\approx0\) from the Dirac consistency step.  B2 can
identify the candidate generator; it does not yet prove the constraints.

B2 imports the B1 form
\[
\boxed{
\Omega_{\mathcal R}
=\int d^3x\,
\left(
\delta\pi^{ij}\wedge\delta\gamma_{ij}
+\delta\pi_\vartheta\wedge\delta\vartheta
\right)
+\Omega_{\rm boundary}
+\Omega_{\rm internal}
+\Omega_{\Delta{\rm no-dot}},
}
\]
with no local \(\delta\pi_n\wedge\delta n\) or
\(\delta\pi_i\wedge\delta n^i\) block.  If such a block is later derived, B2
is revoked together with B1.

For a spatial embedding shift \(M^i\), the linearized metric perturbation
transforms as
\[
\boxed{\delta_M\gamma_{ij}=\partial_iM_j+\partial_jM_i.}
\]
The momentum transforms by the Lie action,
\(\delta_M\pi^{ij}={\cal L}_M\pi^{ij}\), but the selected background has
\(\gamma_{ij}=0\), \(\dot\gamma_{ij}=0\) and hence \(\pi^{ij}=0\).  Therefore
\({\cal L}_M\pi^{ij}\) is higher order in the first linearized B2 check and is
restored only in later nonlinear audits.

Using the metric part of \(\Omega_{\mathcal R}\),
\[
\Omega_{\rm metric}
=\int d^3x\,\delta\pi^{ij}\wedge\delta\gamma_{ij},
\]
one obtains
\[
\begin{aligned}
\Omega_{\rm metric}(\delta\Phi,\delta_M\Phi)
&=\int d^3x\,\delta\pi^{ij}
(\partial_iM_j+\partial_jM_i)\\
&=2\int d^3x\,\delta\pi^{ij}\partial_iM_j\\
&=\delta\int d^3x\,M^i(-2\partial_j\pi^j{}_i)
+\text{boundary}.
\end{aligned}
\]
For compactly supported variations the boundary term vanishes; for
non-compact variations it is deferred to ADM momentum-charge bookkeeping.
Thus
\[
\boxed{
D[M^i]=\int d^3x\,M^i{\cal H}_i^{(1)},\qquad
{\cal H}_i^{(1)}=-2\partial_j\pi^j{}_i.
}
\]
Spatial re-embedding is generated by the candidate momentum constraint and
becomes null only on \({\cal H}_i^{(1)}\approx0\).

For a normal deformation \(N\), the linearized metric-sector action is
\[
\boxed{
\delta_N\gamma_{ij}=0,\qquad
\delta_N\pi^{ij}=\partial^i\partial^jN-\delta^{ij}\partial^2N.
}
\]
Then
\[
\begin{aligned}
\Omega_{\rm metric}(\delta\Phi,\delta_N\Phi)
&=-\int d^3x\,
(\partial^i\partial^jN-\delta^{ij}\partial^2N)\delta\gamma_{ij}\\
&=\delta\int d^3x\,N
(-\partial^i\partial^j\gamma_{ij}+\partial^2\gamma)
+\text{boundary}.
\end{aligned}
\]
Therefore
\[
\boxed{
H[N]=\int d^3x\,N{\cal H}^{(1)},\qquad
{\cal H}^{(1)}=-\partial^i\partial^j\gamma_{ij}+\partial^2\gamma.
}
\]
Normal re-embedding is generated by the candidate Hamiltonian constraint and
becomes null only on \({\cal H}^{(1)}\approx0\).

The record-phase sector uses the fixed-background convention
\[
\boxed{\theta=\nu_0\tau+\vartheta,\qquad
\nu_0\tau\ \text{fixed by the input freeze},\qquad
\vartheta\ \text{dynamical}.}
\]
Under this convention
\[
\boxed{{\cal L}_N\vartheta=N\partial_\tau\vartheta,}
\]
so at the selected background \(\vartheta=0\) there is no background
\(N\nu_0\) term in the B2 calculation.  The alternative convention, where
\(\theta\) is dynamical while \(\tau\) is held as a fixed reference label,
can produce an \(N\nu_0\) phase shift; it is not used for the MVP B2 pass.

For spatial shifts,
\[
\boxed{\delta_M\vartheta=M^i\partial_i\vartheta}
\]
has no first-order background contribution because
\(\partial_i\theta_{\rm bg}=0\).  Its later contribution to the full momentum
generator has the schematic form
\[
\boxed{
D_\vartheta[M]=\int d^3x\,\pi_\vartheta M^i\partial_i\vartheta.
}
\]
For normal deformations the corresponding phase contribution is written
schematically as
\[
\boxed{H_\vartheta[N]=\int d^3x\,N{\cal H}_\vartheta,}
\]
with the actual status of \({\cal H}_\vartheta\) deferred.  B2 therefore
passes the record-phase embedding check only conditionally under the
fixed-background convention.  The fifth-force issue is not solved: Root~C and
B6 must still show that shift symmetry and the record/matter bridge prevent
species-private scalar sourcing.

The B2 result is:
\[
\boxed{
\begin{gathered}
\text{spatial metric embedding: conditional pass},\\
\text{normal metric embedding: conditional pass},\\
\text{off-shell null direction: not required and not claimed},\\
\text{null on constraint surface: conditional on B3},\\
\vartheta\text{ embedding: conditional safe at B2},\\
\text{Root~B proof: not passed.}
\end{gathered}
}
\]
Boundary and remainder blocks are classified as follows: \(\Omega_{\rm boundary}\)
vanishes for compactly supported variations and may become an ADM charge for
non-compact variations; \(\Omega_{\rm internal}\) contains the gapped
\(\eta,q_{ij}\) sectors with \(m_\eta=\ell_*^{-1}\) and
\(m_Q=2\ell_*^{-1}\); and \(\Omega_{\Delta{\rm no-dot}}\) must hide no
lapse/shift kinetic block.  Any preferred-frame bulk term, dot-lapse or
dot-shift symplectic block, extra generator not proportional to \(H\) or
\(D\), or species-private metric/clock direction turns the B2 result into a
failure or a B4/B5 anomaly.

The next Root~B step is B3:
\[
\boxed{
\dot\pi_n\approx0\Rightarrow H\approx0,\qquad
\dot\pi_i\approx0\Rightarrow H_i\approx0.
}
\]
If B3 fails, the B2 candidate generators do not become constraints and the
route stops before ADM closure.

\medskip\noindent\textbf{Root B B3 constraint generators.}
B3 uses the B1 primary lapse/shift constraints and the B2 candidate
generators to run the first Dirac consistency step.  The central question is
whether
\[
\boxed{\pi_n\approx0,\qquad \pi_i\approx0}
\]
generate the secondary constraints
\[
\boxed{{\cal H}\approx0,\qquad {\cal H}_i\approx0}
\]
rather than turning lapse and shift into physical or auxiliary variables.  The
B3 verdict is conditional: the secondary constraints are obtained under the
no-algebraic-lapse/shift rule, but first-class status and ADM closure are not
yet proven.

B3 imports from B1
\[
\boxed{\pi_n\approx0,\qquad \pi_i\approx0}
\]
and
\[
\boxed{
\Omega_{\mathcal R}
=\int d^3x\left(
\delta\pi^{ij}\wedge\delta\gamma_{ij}
+\delta\pi_\vartheta\wedge\delta\vartheta
\right)+\text{classified remainders}.
}
\]
It imports from B2
\[
\boxed{
{\cal H}_i^{(1)}=-2\partial_j\pi^j{}_i,\qquad
{\cal H}^{(1)}=-\partial^i\partial^j\gamma_{ij}+\partial^2\gamma,
}
\]
and
\[
\boxed{
D[M^i]=\int d^3x\,M^i{\cal H}_i^{(1)},\qquad
H[N]=\int d^3x\,N{\cal H}^{(1)}.
}
\]
All of these imports remain conditional on \(S_{\mathcal R}^{\rm bridge}\)
and on the B1/B2 no-dot and no-hidden-lapse rules.

The B3 total Hamiltonian is
\[
\boxed{
H_T=\int d^3x\left[
n{\cal H}^{(1)}
+n^i{\cal H}_i^{(1)}
+H_{\rm phys}^{(2)}
+\Delta_{\rm no-dot}^{(2)}
+u\pi_n+u^i\pi_i
\right],
}
\]
where \(u,u^i\) are arbitrary multipliers for the primary constraints.  The
no-dot remainder is split as
\[
\boxed{
\Delta_{\rm no-dot}^{(2)}
=\Delta_{\rm safe}^{(2)}
+\Delta_{\rm alg-lapse}^{(2)}
+\Delta_{\rm alg-shift}^{(2)}
+\Delta_{\rm alg-mixed}^{(2)}.
}
\]
B3 passes only if
\[
\boxed{
\Delta_{\rm alg-lapse}^{(2)}
=\Delta_{\rm alg-shift}^{(2)}
=\Delta_{\rm alg-mixed}^{(2)}=0
}
\]
up to pure boundary or gauge degeneracies.  This is stricter than B1: B1
forbids \(\dot n\) and \(\dot n^i\), whereas B3 forbids algebraic terms such
as \(n^2\), \(n_in^i\), \(nn^i\), or \(n\) times a local field, whenever they
solve for lapse/shift instead of producing constraints.

For example, if
\[
\boxed{\Delta_{\rm no-dot}^{(2)}\supset \frac{m_n^2}{2}n^2,}
\]
then preservation of \(\pi_n\) gives
\[
\boxed{{\cal H}^{(1)}+m_n^2n\approx0,}
\]
which solves for \(n\) rather than producing the Hamiltonian constraint.  This
is a B3 failure.  Physically, an algebraic lapse mass is a local
massive/non-ADM gravity sector; B3 forbids it because the local MVP target is
a massless ADM core.

The circularity is explicit.  B3 is internally consistent if the
no-algebraic-lapse/shift rules hold, but it does not prove that the microscopic
medium produces a Hamiltonian obeying those rules.  Therefore the B3 result is
\[
\boxed{
\begin{minipage}{0.86\linewidth}
If \(S_{\mathcal R}^{\rm bridge}\) has the conditional Hamiltonian form above,
and if \(\Delta_{\rm no-dot}^{(2)}\) has no algebraic lapse/shift pieces, then
the primary constraints generate the secondary ADM candidates.
\end{minipage}}
\]

The B3 canonical brackets are
\[
\boxed{
\{\gamma_{ij}(x),\pi^{kl}(y)\}
=\delta_{(i}^{\,k}\delta_{j)}^{\,l}\delta^3(x-y),
\qquad
\{\vartheta(x),\pi_\vartheta(y)\}=\delta^3(x-y),
}
\]
and
\[
\boxed{
\{n(x),\pi_n(y)\}=\delta^3(x-y),\qquad
\{n^i(x),\pi_j(y)\}=\delta^i{}_j\delta^3(x-y).
}
\]
The sign convention is the standard Hamilton equation
\(\dot p=-\partial H/\partial q\):
\[
\boxed{
\dot\pi_n=\{\pi_n,H_T\}=-\frac{\delta H_T}{\delta n},
\qquad
\dot\pi_i=\{\pi_i,H_T\}=-\frac{\delta H_T}{\delta n^i}.
}
\]

For the lapse primary constraint,
\[
\begin{aligned}
\dot\pi_n
&=\{\pi_n,H_T\}
=-\frac{\delta H_T}{\delta n}\\
&=-{\cal H}^{(1)}
-\frac{\delta\Delta_{\rm no-dot}^{(2)}}{\delta n}.
\end{aligned}
\]
Under the B3 no-algebraic-lapse rule,
\[
\boxed{\frac{\delta\Delta_{\rm no-dot}^{(2)}}{\delta n}=0}
\]
up to boundary/gauge terms, so
\[
\boxed{\dot\pi_n\approx0\quad\Rightarrow\quad
{\cal H}^{(1)}\approx0.}
\]
Thus
\[
\boxed{
{\cal H}^{(1)}
=-\partial^i\partial^j\gamma_{ij}+\partial^2\gamma\approx0.
}
\]
For compactly supported lapse variations, boundary terms vanish.  For lapse
choices nonzero at spatial infinity, the boundary piece belongs to later
ADM-charge bookkeeping, not to the local MVP constraint test.

For the shift primary constraint,
\[
\begin{aligned}
\dot\pi_i
&=\{\pi_i,H_T\}
=-\frac{\delta H_T}{\delta n^i}\\
&=-{\cal H}_i^{(1)}
-\frac{\delta\Delta_{\rm no-dot}^{(2)}}{\delta n^i}.
\end{aligned}
\]
Under the B3 no-algebraic-shift rule,
\[
\boxed{\frac{\delta\Delta_{\rm no-dot}^{(2)}}{\delta n^i}=0}
\]
up to boundary/gauge terms, so
\[
\boxed{\dot\pi_i\approx0\quad\Rightarrow\quad
{\cal H}_i^{(1)}\approx0.}
\]
Thus
\[
\boxed{{\cal H}_i^{(1)}=-2\partial_j\pi^j{}_i\approx0.}
\]
For compactly supported shift variations, boundary terms vanish.  For shifts
nonzero at spatial infinity, the boundary piece belongs to later ADM momentum
bookkeeping.

After B3 the conditional constraint set is
\[
\boxed{
\pi_n\approx0,\quad \pi_i\approx0,\quad
{\cal H}^{(1)}\approx0,\quad {\cal H}_i^{(1)}\approx0.
}
\]
This is \(4\) primary plus \(4\) secondary constraint functions.  B3 has not
proven that all eight are first class.  That requires B4:
\[
\boxed{\{D,D\},\qquad \{H,D\},\qquad \{H,H\}.}
\]
If B4 proves all eight constraints first class, the local phase-space count is
\[
\boxed{
22\ \text{starting variables}
-8\times2
=6\ \text{remaining phase-space variables}.
}
\]
The interpretation is
\[
\boxed{
4\ \text{variables}=2\ \text{TT metric modes with momenta},\qquad
2\ \text{variables}=(\vartheta,\pi_\vartheta).
}
\]
Therefore, if the final MVP target is exactly two radiative tensor modes, the
record phase must later be shown to be gauge, decoupled, or non-radiative in
the gravitational sector.  Otherwise the local sector contains two TT modes
plus one long-range record-phase mode.

At B3, \(\vartheta\) remains a conditional canonical pair:
\[
\boxed{\vartheta,\pi_\vartheta.}
\]
It does not enter the lapse/shift preservation equations unless
\(\Delta_{\rm no-dot}^{(2)}\) contains an algebraic phase coupling.  The B3
safety check is
\[
\boxed{
\Delta_{\rm no-dot}^{(2)}\ \text{contains no}\ n\vartheta
\ \text{and no}\ n^i\partial_i\vartheta\ \text{algebraic coupling}.
}
\]
The protected route is the shift symmetry
\[
\boxed{\theta\mapsto\theta+\text{constant},}
\]
which forbids explicit \(\theta\)-charge in the local Hamiltonian.  Derivative
couplings are not automatically forbidden; they must be classified as gauge,
decoupled, suppressed, or part of the allowed record structure.  B3 therefore
does not prove that \(\vartheta\) is harmless.  That remains a later
scalar-leakage and matter-coupling question for B6 and Root~C.

The B3 pass/fail ledger is:
\[
\boxed{
\begin{array}{lll}
\text{B1 primary constraints} &:& \text{conditional pass},\\
\text{B2 generators }{\cal H},{\cal H}_i &:& \text{conditional pass},\\
H_T\ \text{with primary multipliers} &:& \text{conditional pass},\\
\dot\pi_n\to{\cal H}^{(1)}\approx0 &:& \text{conditional pass},\\
\dot\pi_i\to{\cal H}_i^{(1)}\approx0 &:& \text{conditional pass},\\
\text{first-class status} &:& \text{open until B4},\\
\vartheta\ \text{safety} &:& \text{open},\\
\text{TT origin} &:& \text{open/fail-risk until B6}.
\end{array}}
\]
B3 proves only that, within the conditional Hamiltonian and the
no-algebraic-lapse/shift rule, preserving the primary lapse/shift constraints
produces the secondary candidates.  It does not prove first-class closure,
the hypersurface-deformation algebra, \(\lambda=1/2\), the EH fixed point, two
TT modes, matter preservation of constraints, or derivation of the bridge from
the microscopic input.

The forwarding map is:
\[
\boxed{
\begin{array}{lll}
\text{first-class constraints} &\to& \text{B4 bracket calculation},\\
\text{hypersurface-deformation closure} &\to& \text{B4 anomaly test }A[N,M]=0,\\
\text{nonlinear }\{H,H\}\text{ structure} &\to& \text{B4 quadratic consistency},\\
\lambda=1/2 &\to& \text{B5 EH fixed-point filter},\\
\text{two TT modes} &\to& \text{B6 hyperbolicity certificate},\\
\vartheta\text{ under matter coupling} &\to& \text{B6 and Root~C},\\
S_{\mathcal R}^{\rm bridge}\text{ from medium} &\to&
\text{deferred medium-to-bridge derivation}.
\end{array}}
\]

B3 must stop, or be marked failed, if the conditional Hamiltonian contains
\begin{itemize}
\item \(n^2\) or \(n_in^i\) terms that solve for lapse/shift;
\item \(nn^i\) terms that make lapse/shift auxiliary physical variables;
\item \(n\vartheta\) or \(n^i\partial_i\vartheta\) terms that create a scalar
  fifth-force constraint failure;
\item algebraic preferred-frame terms that make the embedding generator
  background-dependent;
\item a remainder variation that changes \({\cal H}^{(1)}\) or
  \({\cal H}_i^{(1)}\) by an unsuppressed local operator.
\end{itemize}
Here ``unsuppressed'' means that the operator contributes at the local MVP test
scale.  Acceptable suppression must be explicit, for example
\[
\boxed{
\begin{gathered}
(E/\Lambda_{\rm UV})^2<10^{-5}
\quad\text{for Solar-System scale checks,}\\
m>1/r_{\rm test}
\quad\text{for the relevant propagation scale.}
\end{gathered}
}
\]

The B3 reviewer stance is to accept the conditional pass with the
no-algebraic-lapse/shift rule explicit.  B3 is a canonical consistency
calculation, not the derivation of \(\Delta_{\rm no-dot}^{(2)}\) from the
medium.  The stricter alternative is to stop B3 until that derivation is
complete; the MVP instead keeps the conditional bridge visible.

The cumulative bridge audit after B3 is:
\[
\boxed{
\begin{array}{llll}
\text{Rule} & \text{Source} & \text{Statement} & \text{Status}\\
\text{R-B1-1} & \text{B1} &
\Delta_{\mathcal M}^{(2)}\ \text{has no }\dot n X & \text{conditional}\\
\text{R-B1-2} & \text{B1} &
\Delta_{\mathcal M}^{(2)}\ \text{has no }\dot n^i X & \text{conditional}\\
\text{R-B1-3} & \text{B1} &
\Delta_{\mathcal M}^{(2)}\ \text{has no }\dot n\,\dot\gamma & \text{conditional}\\
\text{R-B2-1} & \text{B2} &
\text{re-embedding is on-constraint null} & \text{proven mod constraints}\\
\text{R-B2-2} & \text{B2} &
\vartheta\ \text{uses fixed-background convention} & \text{locked}\\
\text{R-B3-1} & \text{B3} &
\Delta_{\rm no-dot}\ \text{has no algebraic }n^2,n_in^i & \text{conditional}\\
\text{R-B3-2} & \text{B3} &
\Delta_{\rm no-dot}\ \text{has no algebraic }nn^i & \text{conditional}\\
\text{R-B3-3} & \text{B3} &
\Delta_{\rm no-dot}\ \text{has no }n\vartheta & \text{conditional under shift}\\
\text{R-B3-4} & \text{B3} &
\Delta_{\rm no-dot}\ \text{has no preferred-frame operator} & \text{conditional}.
\end{array}}
\]
The B1 and B3 conditional rules must be verified by the deferred
medium-to-bridge derivation.  R-B2-1 is tested by B4 brackets.  R-B2-2 remains
the locked MVP convention.  If any rule fails when the microscopic input is
derived concretely, the corresponding B step must be revisited and its
conditional pass cannot be counted.

The next Root~B artifact is B4:
\[
\boxed{
\text{compute }\{D,D\},\ \{H,D\},\ \{H,H\}
\text{ from the B1--B3 canonical structure.}
}
\]
If B4 fails, the B3 secondary constraints do not form the ADM first-class
system and Root~B fails in the local MVP.

\medskip\noindent\textbf{Root B B4 bracket closure.}
B4 computes the constraint brackets from the B1--B3 canonical structure.  Its
job is to separate a real linear checkpoint from the full
hypersurface-deformation algebra.  The B4 verdict is:
\[
\boxed{
\begin{minipage}{0.86\linewidth}
The primary constraints and the linear secondary constraints close at
abelian linear order.  This is a conditional pass, but it is not ADM closure.
The full ADM/HDA test requires the quadratic supplement B4q.
\end{minipage}}
\]

B4 imports the canonical brackets
\[
\boxed{
\{\gamma_{ij}(x),\pi^{kl}(y)\}
=\delta_{(i}^{\,k}\delta_{j)}^{\,l}\delta^3(x-y),
\qquad
\{\vartheta(x),\pi_\vartheta(y)\}=\delta^3(x-y),
}
\]
and
\[
\boxed{
\{n(x),\pi_n(y)\}=\delta^3(x-y),\qquad
\{n^i(x),\pi_j(y)\}=\delta^i{}_j\delta^3(x-y).
}
\]
Here
\[
\boxed{
\delta_{(i}^{\,k}\delta_{j)}^{\,l}
=\frac12(\delta_i{}^k\delta_j{}^l+\delta_i{}^l\delta_j{}^k)
}
\]
is the symmetric Kronecker projector.  B4 also imports
\[
\boxed{
{\cal H}_i^{(1)}=-2\partial_j\pi^j{}_i,\qquad
{\cal H}^{(1)}=-\partial^i\partial^j\gamma_{ij}+\partial^2\gamma,
}
\]
with
\[
\boxed{
D[M^i]=\int d^3x\,M^i{\cal H}_i^{(1)},\qquad
H[N]=\int d^3x\,N{\cal H}^{(1)}.
}
\]
All smearing functions are compactly supported unless a boundary term is being
classified.  Non-compact smearing belongs to ADM-charge bookkeeping, not to
the local MVP bracket test.  B4 inherits the B1--B3 conditional rules: no
\(\dot n\), no \(\dot n^i\), no algebraic lapse/shift mass, no algebraic
\(n\vartheta\), and no preferred-frame algebraic remainder.  If these fail in
a later medium derivation, B4 is revoked with the affected earlier step.

For the momentum generator,
\[
\boxed{
D[M]=\int d^3x\,M^i(-2\partial_j\pi^j{}_i)
=\int d^3x\,\pi^{ij}(\partial_iM_j+\partial_jM_i)
}
\]
up to boundary.  Therefore
\[
\boxed{
=\frac{\delta D[M]}{\delta\pi^{ij}}
=\partial_iM_j+\partial_jM_i,\qquad
\frac{\delta D[M]}{\delta\gamma_{ij}}=0
}
\]
at linear metric order.  For the Hamiltonian generator,
\[
\boxed{
H[N]=\int d^3x\,N(-\partial^i\partial^j\gamma_{ij}+\partial^2\gamma)
}
\]
and, after integrating derivatives onto \(N\),
\[
\boxed{
H[N]=\int d^3x\,\gamma_{ij}
(-\partial^i\partial^jN+\delta^{ij}\partial^2N)
}
\]
up to boundary.  Thus
\[
\boxed{
=\frac{\delta H[N]}{\delta\gamma_{ij}}
=-\partial^i\partial^jN+\delta^{ij}\partial^2N,\qquad
\frac{\delta H[N]}{\delta\pi^{ij}}=0.
}
\]
This matches the B2 normal deformation:
\[
\boxed{
\delta_N\pi^{ij}
=-\frac{\delta H[N]}{\delta\gamma_{ij}}
=\partial^i\partial^jN-\delta^{ij}\partial^2N.
}
\]

The primary brackets close because the linear secondary constraints are
independent of the lapse/shift variables and their momenta:
\[
\boxed{
\{\pi_n,{\cal H}^{(1)}\}=0,\quad
\{\pi_n,{\cal H}_i^{(1)}\}=0,\quad
\{\pi_i,{\cal H}^{(1)}\}=0,\quad
\{\pi_i,{\cal H}_j^{(1)}\}=0,\quad
\{\pi_n,\pi_i\}=0.
}
\]
The reason is purely canonical: \({\cal H}^{(1)}\) depends only on
\(\gamma_{ij}\), \({\cal H}_i^{(1)}\) depends only on \(\pi^{ij}\), and the
primary lapse/shift momenta have no canonical bracket with those variables.
This pass is conditional on the B3 no-algebraic-lapse/shift rule.

For the linear momentum bracket, \(D^{(1)}[M]\) is \(\pi\)-linear:
\[
\boxed{D^{(1)}[M]=2\int d^3x\,(\partial_jM_i)\pi^{ij}.}
\]
Since
\[
\boxed{\{\pi^{ij}(x),\pi^{kl}(y)\}=0,}
\]
the bracket of two purely \(\pi\)-linear generators vanishes:
\[
\boxed{\{D[L],D[M]\}_{\rm lin}=0.}
\]
This is the abelianized linear result.  It is not the full spatial
diffeomorphism algebra
\[
\boxed{\{D[L],D[M]\}=D[[L,M]],\qquad
[L,M]^i=L^j\partial_jM^i-M^j\partial_jL^i.}
\]
The non-abelian term requires nonlinear corrections such as
\[
\boxed{D^{(2)}[M]\sim\int \gamma\,\pi\,\partial M,}
\]
whose bracket with \(D^{(1)}\) produces the commutator.

For the mixed bracket,
\[
\begin{aligned}
\{H[N],D[M]\}
&=\int d^3x\,
(-\partial^i\partial^jN+\delta^{ij}\partial^2N)
(\partial_iM_j+\partial_jM_i)\\
&=-2\int d^3x\,(\partial^i\partial^jN)(\partial_iM_j)
+2\int d^3x\,(\partial^2N)(\partial_iM^i).
\end{aligned}
\]
With compact support, integration by parts gives
\[
\boxed{
\begin{aligned}
-2\int(\partial^i\partial^jN)(\partial_iM_j)
&\to
2\int M_j\,\partial_i\partial^i\partial^jN,\\
2\int(\partial^2N)(\partial_iM^i)
&\to
-2\int M^i\,\partial_i\partial^2N.
\end{aligned}}
\]
The sum is
\[
\boxed{
2\int d^3x\,M_j
\left(\partial_i\partial^i\partial^jN-\partial^j\partial^2N\right)=0,
}
\]
because spatial derivatives commute on smooth fields.  Therefore
\[
\boxed{\{H[N],D[M]\}_{\rm lin}=0.}
\]
This means that \({\cal H}^{(1)}\) is invariant under the linearized spatial
gauge transformation.  The full ADM expectation,
\[
\boxed{\{H[N],D[M]\}=H[M^i\partial_iN],}
\]
requires nonlinear or density-weight corrections.

For the Hamiltonian bracket, at linear order both smeared Hamiltonian
constraints depend only on \(\gamma_{ij}\):
\[
\boxed{\frac{\delta H[N]}{\delta\pi^{ij}}=0,\qquad
\frac{\delta H[M]}{\delta\pi^{ij}}=0.}
\]
Hence
\[
\boxed{\{H[N],H[M]\}_{\rm lin}=0.}
\]
This result is structurally trivial:
\[
\boxed{\{H^{(1)}[N],H^{(1)}[M]\}=0}
\]
because \(H^{(1)}\) is configuration-only.  Any linearized theory with a
configuration-only linear Hamiltonian constraint has this property.  It does
not distinguish the ADM/EH fixed point from nearby non-ADM theories.

The full target is instead
\[
\boxed{
\{H[N],H[M]\}=D[h^{ij}(N\partial_jM-M\partial_jN)].
}
\]
At the first nontrivial order one must write
\[
\boxed{H=H^{(1)}+H^{(2)}+\cdots,\qquad
D=D^{(1)}+D^{(2)}+\cdots}
\]
and test
\[
\boxed{
\begin{aligned}
A^{(1)}[N,M]
&=\{H^{(1)}[N],H^{(2)}[M]\}
+\{H^{(2)}[N],H^{(1)}[M]\}\\
&\quad
-D^{(1)}[\delta^{ij}(N\partial_jM-M\partial_jN)]
=0
\end{aligned}}
\]
up to boundary/gauge/constraint terms.  This B4q condition is the first real
ADM-vs-non-ADM discriminator.  It must produce the specific structure function
\[
\boxed{h^{ij}(N\partial_jM-M\partial_jN)}
\]
from the same record-geometry variable \(h_{ij}\).  A different coefficient or
a private background tensor is a non-ADM result.

Thus the B4 linear bracket result is
\[
\boxed{
\{D,D\}_{\rm lin}=0,\qquad
\{H,D\}_{\rm lin}=0,\qquad
\{H,H\}_{\rm lin}=0.
}
\]
Together with the primary brackets, the B1--B3 constraint set is first-class
at abelian linear order.  This is a real checkpoint because it excludes an
immediate linear local anomaly, lapse/shift auxiliary equation, or
preferred-frame obstruction.  But it is not Root~B closure.

The record-phase sector remains open.  B4 has used the pure metric linear
constraints, and \(\vartheta\) is harmless at this level only if the Hamiltonian
has no explicit \(\vartheta\)-charge and no algebraic lapse/phase coupling.
The dangerous schematic term is
\[
\boxed{
\{H[N],H[M]\}_\vartheta
\supset
\int
\frac{\delta H[N]}{\delta\vartheta}
\frac{\delta H[M]}{\delta\pi_\vartheta}
-(N\leftrightarrow M).
}
\]
Under the B3 safety rule and the shift symmetry
\[
\boxed{\theta\mapsto\theta+\text{constant},}
\]
there is no explicit \(\vartheta\)-charge in the B4 local metric bracket.
Pure derivative phase couplings must still be classified as boundary, gauge,
decoupled or suppressed.  If they survive as long-range scalar source terms,
B4 passes only in the metric sector and the conservative route fails later at
B6/Root~C.

The B4 anomaly ledger is:
\[
\boxed{
\begin{array}{lll}
\text{primary lapse/shift anomaly} &:& \text{absent under B3 rules},\\
\{D,D\}_{\rm lin}\ \text{anomaly} &:& \text{absent by }\pi\text{-linearity},\\
\{H,D\}_{\rm lin}\ \text{anomaly} &:& \text{absent by IBP},\\
\{H,H\}_{\rm lin}\ \text{anomaly} &:& \text{absent trivially},\\
\text{missing nonlinear structure function} &:& \text{open until B4q},\\
\vartheta\ \text{scalar bracket leak} &:& \text{open until B6/Root~C},\\
\text{TT-origin anomaly} &:& \text{open/fail-risk until B6}.
\end{array}}
\]
No anomaly may be called suppressed unless the scale is explicit.  The working
MVP thresholds are:
\[
\boxed{
\begin{gathered}
\text{Solar-System/PPN local anomaly: below }10^{-5},\\
\text{GW cone-changing anomaly: below }10^{-15}\text{ or absent by structure},\\
\text{binary-pulsar extra loss: below }10^{-3}\text{ at MVP level},\\
\text{gapped internal sector: }m>1/r_{\rm test}
\text{ or explicit EFT decoupling.}
\end{gathered}}
\]
These are working thresholds, not final data-analysis bounds.  They prevent
the word ``suppressed'' from being used without a scale.

B4 therefore proves only:
\begin{itemize}
\item primary constraints commute with the linear secondary constraints;
\item the linear momentum constraints close abelianly;
\item the linear Hamiltonian is invariant under linear spatial gauge;
\item linear Hamiltonian constraints commute with each other;
\item there is no immediate linear bracket anomaly.
\end{itemize}
B4 does not prove the full hypersurface-deformation algebra, the structure
function \(h^{ij}(N\partial_jM-M\partial_jN)\), \(A[N,M]=0\) beyond the
abelian linear test, \(\lambda=1/2\), the EH fixed point, two TT modes,
matter-coupled preservation, or microscopic derivation of the bridge.

The honest next artifact is the quadratic supplement:
\[
\boxed{\texttt{Root\_B\_B4q\_Quadratic\_HDA\_Closure.md}.}
\]
The naming convention is
\[
\boxed{
B4=\text{linear abelian bracket check},\qquad
B4q=\text{quadratic ADM/HDA discriminator}.
}
\]
Together, B4 and B4q complete the MVP bracket-closure calculation; B4 alone
does not.  B4q must derive or explicitly condition \(H^{(2)}\) and \(D^{(2)}\),
then compute \(A^{(1)}[N,M]\).  The expected outcomes are:
\[
\boxed{
\begin{array}{lll}
A^{(1)}=0 &:& \text{conditional ADM closure at MVP order},\\
\delta A\ \text{below bound} &:& \text{conditional non-ADM correction},\\
A^{(1)}=O(1)\ \text{or preferred tensor} &:& \text{conservative route fails},\\
\text{no derivable }H^{(2)} &:& \text{bridge incomplete}.
\end{array}}
\]
For local Solar-System style checks the first compatibility target is
\[
\boxed{|\delta A/A_{\rm ADM}|<10^{-5}.}
\]
Any deviation that changes the GW cone must meet the stronger \(10^{-15}\)
cone/speed target or be absent by structure.

\medskip\noindent\textbf{Root C execution tasks.}
Root~C begins with an inheritance table saying exactly which Root~A/B outputs
are used and with what status.  It must verify universal matter action
\(S_m[g_{\rm rec},\Psi_A]\), define the lapse/compression bridge with \(u\)
varied at fixed \(h_{ij}\), \(N^i\) and matter variables, and mark
\(N=e^{-u}\) or its replacement as derived from Root~B or conditional in
MVP-0.  It must compute
\[
\boxed{
(N\sqrt h)^{-1}\frac{\delta S_m}{\delta u}
  =\rho_E=T_{\mu\nu}n^\mu n^\nu
}
\]
for the included matter sectors; derive the far-tail monopole
\(Q_A^{(0)}=\eta E_A/c^2\); enumerate and eliminate species-private force
terms; audit photons, binding energy, internal energy, composites and compact
objects; define \(\alpha_A=q_A/(E_A/c^2)\) and prove leading-order
\(\alpha_A=\alpha_B\); and perform the Nordtvedt/self-binding audit.

\medskip\noindent\textbf{GW-emission execution tasks.}
The primary MVP anchor is PSR~B1913+16.  PSR~J0737--3039 and
PSR~J1738+0333 are extension anchors.  The emission task must derive the
radiative TT action, the retarded wave-zone solution, the multipole hierarchy,
the tensor flux \(P_{TT}\), radiation reaction
\[
\boxed{\frac{dE_{\rm orbit}}{dt}=-P_{TT}}
\]
and angular-momentum flux where needed.  It must build source masses from
\(m_A=E_A/c^2\), re-check the Root~C no-dipole audit in the binary context,
and produce a quantitative \(\dot P_b\) anchor row.  A correct
\(c_{\rm GW}=c\) propagation result does not by itself pass this emission
gate.

\medskip\noindent\textbf{Observation execution tasks.}
The MVP observation checklist contains only local anchors supported by the
completed gates: Newtonian \(1/r\) with \(M=E_{\rm total}/c^2\);
redshift/lapse universality; light bending and Shapiro with one cone and
\(\gamma=1\); perihelion/ranging with
\(\lambda=1/2\to\) ADM closure \(\to\) EH \(\to\beta=1\); no preferred-frame
local flow; GW propagation with \(c_{\rm GW}=c\) and two TT modes; no scalar
dipole from Root~C; and binary-pulsar tensor \(\dot P_b\) from the
GW-emission gate.

\medskip\noindent\textbf{MVP pass, fail and soft-fail rules.}
The MVP passes only if all six blocks are derived at the stated MVP order, all
six artifacts are completed, and each artifact receives an external reviewer
check before the next artifact is treated as settled.  The MVP fails if it
leaves an uncontrolled private cone, species-private principal metric,
non-closing ADM bracket, extra scalar/vector/massive tensor channel,
lapse/shift kinetic leak, trace-only or species-label source, unequal
\(G_{\rm static}\) and \(G_{\rm radiation}\), or any MOND/cosmology/strong-field
patch for a local-core failure.

Borderline cases are soft failures until numerically controlled: small but
nonzero cone splitting, tiny species-dependent tail charge or small extra
radiative channel require an explicit bound and an observation scale.  They
may not be dismissed by phrases such as ``approximately universal'' or
``effectively suppressed'' without a calculation.

\medskip\noindent\textbf{Execution order and fast track.}
The recommended order is
\[
\boxed{
\begin{gathered}
\text{MVP\_Input\_Freeze}\\
\Downarrow\\
\text{Root\_A\_MVP\_Spectrum}\\
\Downarrow\\
\text{Root\_B\_Linearized\_ADM}\\
\Downarrow\\
\text{Root\_C\_Source}\\
\Downarrow\\
\text{PSR\_B1913\_Emission}\\
\Downarrow\\
\text{MVP\_Observation\_Checklist}
\end{gathered}
}
\]
The first physically blocking calculation is the Root~A spectrum/one-cone
test.  A PSR~B1913+16 emission template may be drafted earlier only after
Root~A passes, and only as a conditional numerical template; it is not a
publishable result before Root~B and Root~C have delivered the tensor channel
and total-energy source.  Every suppression claim in every artifact must name
a scale or bound.

\section{Open Items}

RMG-1 is not yet a closed theory.  The open items group into the following
programs.

\medskip\noindent\textit{Microscopic foundation.}
Derive $S_{\rm eff}^{\rm rec}[\mathcal M_0]$ from the stable base-medium phase;
specify the input package
$\mathcal D_{\mathcal M}=(\Phi_{\mathcal M},S_{\mathcal M},\mathcal M_0,\ell_*,
\Omega_{\mathcal M},\mathcal G_{\rm int},\mathcal O_{\rm rec})$; test the
candidate order parameter $\Phi_{\mathcal M}=\sqrt n e^{i\theta}\chi$ against
Roots A--C; compute the stable-phase spectrum; integrate out the internal
sector and verify the absence of background leaks; derive
$\ell_*$ from the base-medium record dynamics; verify that the stable phase
contains no negative-energy physical branch; carry at least one working toy
instantiation through its quadratic Hessian, spectrum, projectors and
record/internal split.

\medskip\noindent\textit{Root A.}
Derive one common external record carrier from the stable phase; compute
$\mathrm{Spec}(\mathcal L_{\mathcal M_0})$ and prove one gapless record
branch; derive phase locking of stable tails to $\nu_0$; prove the
phase/acoustic branch gives only the common cone, not a scalar graviton;
prove every long-range stable record family uses the coefficients
$C_t:C_0:C_x=1:\rho:\rho^2$.

\medskip\noindent\textit{Root B.}
Derive record-embedding first-class ADM closure; construct $\Gamma_{\mathcal R}$
and the presymplectic form $\Omega_{\mathcal R}$; derive the embedding vector
fields $\delta_\xi$ and the local generators $D,H$; derive the Noether split
$J_\xi=C_\xi+dQ_\xi$; verify B1--B5 and prove the anomaly gate $A[N,M]=0$;
derive the construction bridge
$\mathcal D_{\mathcal M}\to S_{\rm eff}^{\rm rec}\to S_{\mathcal R}$;
exclude unsuppressed lapse/shift scalar-vector kinetic terms as a derived
Dirac result rather than as a gauge assumption; compute brackets from
$\Omega_{\mathcal R}$ with Dirac brackets where needed; derive the
hypersurface-deformation algebra; derive the DeWitt coefficient $\lambda=1/2$;
prove $\Delta_{\mathcal M}=0$ or suppressed with explicit structural or scale
tests; give a linearized hyperbolicity certificate, match the principal symbol
to the record cone, and exclude ghost/gradient instabilities; verify
constraint preservation under evolution and matter-coupled Dirac closure.

\medskip\noindent\textit{Root C.}
Verify C1--C5 in the base-medium matter sector; prove $S_u=\rho_E$ for every
stable record matter family with \(\rho_E=T_{\mu\nu}n^\mu n^\nu\) in the ADM
normal frame; prove metric-exhaustion of local $u$-dependence; prove the
absence of explicit species-private $u$-coupling; prove
$\sigma_{\rm species}^{\rm long,local}=0$; derive the universal gravitational
tail projection $q_A\propto E_A/c^2$ and the far-tail monopole
$Q_A^{(0)}=\eta E_A/c^2$ in the regime \(r\gg R_{\rm source}\) and
\(r\gg\ell_*\); verify that photons and traceless fields source through energy
density rather than trace; handle quantum matter Hamiltonian expectation
values and vacuum subtractions consistently with the cosmological term; audit
compact-object sensitivities using
\(\alpha_A=q_A/(E_A/c^2)\) so that no leading scalar dipole loss appears in
binary-pulsar systems; include self-gravitational binding energy in the
strong-equivalence/Nordtvedt audit.

\medskip\noindent\textit{Factorization and protection.}
Derive $Q_{AB}^{\mu\nu}=G^{\mu\nu}K_{AB}$ from the record kinetic kernel;
prove the stable record phase has only one observable principal tensor; derive
non-common mode elimination; prove the observable tensor basis gate; eliminate
unsuppressed $U^\mu U^\nu$ and flow/vorticity tensors; derive local flow
decoupling and the preferred-frame PPN null target; prove no physical
low-energy birefringence; derive one-cone clock universality; prove the
failure matrix $B_{AB}^{\mu\nu}$ vanishes on physical principal modes.

\medskip\noindent\textit{Composite metric and tensor sector.}
Derive the composite map $g_{\rm rec}^{\mu\nu}=\mathcal G^{\mu\nu}[\Phi_{\mathcal M}]$;
prove $\delta g_{\rm rec}$ contains a protected $h_{ij}^{TT}$ sector; close the
tensor kinetic-rank test (exactly two positive luminal TT modes and no extra
tensor/scalar/vector radiation); prove amplitude and internal modes are
gapped, constrained, gauge, confined or suppressed.

\medskip\noindent\textit{Radiation and detection.}
Separate propagation from emission.  For propagation, derive scalar/vector
constraints and tensor/shear radiation; derive lapse/shift no-kinetic
structure from $\mathcal M$; prove first-class constraint closure removes
scalar/vector radiation; prove pure common-mode breathing is locally
self-calibrated and detector-null; derive the detector-visible TT/shear or
defect projection for GW signals.  For emission, derive tensor quadrupole
energy loss, radiation reaction and compact-binary waveform phase from the
same record metric and total-energy source; prove
$G_{\rm Newton}=G_{\rm radiation}=G_{\rm record}$; derive
$P_{TT}$, $dE_{\rm orbit}/dt=-P_{TT}$ and the angular-momentum flux for
eccentric systems; build the leading chirp mass from $m_A=E_A/c^2$; run the
PSR~B1913+16-style binary-pulsar timing MVP; and exclude massive vector modes,
massive spin-2 leakage, a second metric tensor mode and any hidden
scalar/vector monopole or dipole energy-loss channel in the tested local
sector.

\medskip\noindent\textit{Observational audit package.}
Map each local observable to a gate: Newtonian/orbital limit; gravitational
redshift; light bending and Shapiro delay; perihelion, ranging and PPN
\(\beta\); frame dragging and preferred-frame null tests; binary-pulsar
orbital decay; GW propagation; compact-binary waveform phase; and finite
strong-field tests such as ISCO, shadow, ringdown, strong lensing and
late-inspiral behavior.  Separate GR-like local-core recovery from
RMG-distinctive deviation-window observables.  The execution sequence is:
first build \(MVP\_Local\_Core\_Proof\_Tasks\), then execute the Root~A toy
spectrum/one-cone test, the Root~B linearized ADM closure audit, the Root~C
total-energy/no-dipole audit, and finally the PSR~B1913+16-style numerical
emission comparison.

\medskip\noindent\textit{Foundational and extension programs.}
Derive Operational Relational Covariance from microscopic $\mathcal M$; derive
the record/internal projection $\Pi_{\mathcal R}$ from $\mathcal M$; derive
the flow/vorticity gate and the MOND reentry function $\mathcal F$; derive
the compact-object branch and the strong-field projection gate; derive the
cosmological background state and the matter sector from $\mathcal M$; state
the quantum-extension boundary, namely which quantum claims are outside the
local-gravity MVP and which must be derived later from the base-medium record
action.

\section{Pass/Fail Gates}

\begin{gate}[Pass condition]
The framework passes if $\mathcal M$ derives $S_{\rm EH}+S_{\rm matter}$ as
the low-energy fixed point, with positive physical Hamiltonian, universal
metric coupling, and exactly two vacuum tensor gravitational degrees of
freedom.
\end{gate}

\begin{gate}[Fail conditions]
The framework fails in the tested local sector if any of the following
occurs:
\begin{itemize}
\item observable preferred frame, species-dependent metric coupling, extra
  scalar/vector radiation, wrong gravitational-wave speed, negative-energy
  physical modes, or non-closing ADM constraints;
\item factorization failure $Q_{AB}^{\mu\nu}\neq G^{\mu\nu}K_{AB}$, with the
  failure matrix $B_{AB}^{\mu\nu}$ producing species-dependent cones, a
  preferred-frame tensor, or a geometry cone distinct from the matter cone;
\item composition-dependent gravitational projection $q_A/(E_A/c^2)$, or
  long-range tails requiring distinct spacetime carriers;
\item independent $u^\mu_{\mathcal M}$ in local operators, or low-energy
  kinetic terms for lapse or shift;
\item residue $\Delta_{\mathcal M}$ leaving an unsuppressed preferred-frame
  term, extra operational tensor, non-ADM constraint bracket, or scalar/vector
  propagating mode;
\item observed interferometer signal explained only by pure common-mode
  breathing that should be self-calibrated, or unsuppressed breathing/scalar
  polarization in local polarization tests;
\item record dynamics that do not yield exactly two positive, luminal,
  common-cone TT modes, or that leave an unsuppressed scalar/vector,
  massive spin-2, negative-energy or extra shear channel;
\item binary-pulsar timing or compact-binary waveforms requiring an
  energy-loss law different from the protected tensor channel, or requiring
  an unsuppressed scalar dipole/monopole radiation channel;
\item GW emission with \(G_{\rm radiation}\neq G_{\rm Newton}\), outgoing
  tensor flux not balanced by \(dE_{\rm orbit}/dt=-P_{TT}\), missing angular
  momentum flux for eccentric timing systems, or a chirp mass not built from
  the Root~C total-energy masses \(m_A=E_A/c^2\);
\item unsuppressed MOND-type correction in solar-system, binary-pulsar or
  local GW tests, or modification of $\gamma=1$, $\beta=1$, $c_{\rm GW}=c$,
  universal free fall, or the two-tensor-mode vacuum sector;
\item compact-object sector hiding energy from gravity, creating an
  unsuppressed breathing/scalar radiation channel, defining a species-private
  external cone, altering $c_{\rm GW}=c$, or forcing shadow, ringdown,
  binary-pulsar or strong-field lensing deviations already excluded by
  observation.
\end{itemize}
\end{gate}

\section*{Current Status}

\[
\boxed{
\text{RMG-1 has a conditional leading-order local GR core, not yet a closed theory.}
}
\]

Principal theorem to be derived:
\[
\boxed{
\mathcal M
\Rightarrow
\text{exact ORC/no-extra-tensor}
\Rightarrow
Q_{AB}^{\mu\nu}=G^{\mu\nu}K_{AB}
\Rightarrow
\Delta_{\mathcal M}=0
\Rightarrow
\text{ADM/Einstein-Hilbert low-energy fixed point}.
}
\]

\end{document}
